# CFD 設定可調 B 階段 §7 本機驗證（2026-09-24）

依 `docs/plans/building-energy-cfd-b-engine-params.md` 第 7 節，在開發機的本機 docker 跑 AIJ Case C 與實案。

- 引擎版本：main `6a18fbd`（B1a，#942），在固定於這個 commit 的獨立 worktree 執行。
- 求解環境：OpenFOAM v2412（`opencfd/openfoam-default:2412`）、8 進程，不設 docker CPU 上限，與 E4、S5b-2 相同。
- 每個變體各跑一次；endTime 600，未收斂時自動延長一次到 1,200 步。
- 沒有動 181、沒有部署，也沒有改 service。

## 結論

- **AIJ：** V1、V2 都通過 owner 訂的五項門檻。格數約為 E4 的 36%，耗時約 40%，準確度與 E4 相當。
- **實案：** 放粗 1 層、2 層都通過準確度門檻（峰值差 −1.2%、−1.1%，面積平均差 +0.06%、+0.18%）。
  - 但格數只降到 55.7%、50.6%，耗時只降到 55.8%、53.6%，都沒有達到「至少減半」。
  - 原因是這棟建物的等向加細盒很大，放粗只能減少盒外的背景格，詳見下方「實案為什麼省不到一半」。

## AIJ Case C（1D、WD 0，對照 E4）

bbox 盒、背景格 1.5 m（放粗前的格距，與 E4 相同）、表面與區域加細等級沿用 E4。V2 的地面帶取 0.2H，說明見下方。

| 變體 | 格數 | 耗時（秒） | 步數 | 收斂 | hit rate | FAC2 | FB | NMSE | RMSE |
|---|---|---|---|---|---|---|---|---|---|
| E4（不放粗，2026-09-22） | 5,152,214 | 6,201.9 | 1,200 | 否 | 0.400 | 0.850 | −0.0012 | 0.1435 | 0.2479 |
| V1：外圍放粗 1 層 | 1,809,238（35.1%） | 2,512.9（40.5%） | 1,200 | 否 | 0.467 | 0.850 | −0.0002 | 0.1342 | 0.2396 |
| V2：V1 加上游地面帶 0.2H | 1,853,618（36.0%） | 2,485.3（40.1%） | 1,200 | 否 | 0.400 | 0.858 | +0.0179 | 0.1325 | 0.2359 |

| 門檻（owner 訂的值） | V1 | V2 |
|---|---|---|
| \|FB\| ≤ 0.05 | 通過（0.0002） | 通過（0.0179） |
| hit rate ≥ 36% | 通過（46.7%） | 通過（40.0%） |
| FAC2 ≥ 0.80 | 通過（0.850） | 通過（0.858） |
| 格數不超過 E4 的一半 | 通過（35.1%） | 通過（36.0%） |
| 耗時不超過 E4 的一半 | 通過（40.5%） | 通過（40.1%） |

- **收斂：** 三者都在 600 步未收斂，延長到 1,200 步仍未收斂，條件相同。
- **網格品質：** V1、V2 的 checkMesh 都是 `Mesh OK.`，最大非正交角 29.64°（E4 為 29.70°），最大偏斜度 0.353。
- **hit rate：** 三者分別是 0.400、0.467、0.400。120 個量測點的標準誤約 ±4.4 個百分點，差距最多約 1.5 個標準誤，分不出高低。能說的是放粗沒有讓準確度變差，不能說變好。
- **地面帶：** V2 只比 V1 多 44,380 格（+2.5%），各項指標都在誤差範圍內，看不出好處。
- **V2 的帶高 0.2H（3 m）怎麼選的：** 放粗後背景格是 3 m，帶等級的格子是 0.75 m。3 m 的帶會把 0 到 3 m 的地面加細到 0.75 m，高度是行人面（1.5 m）的 2 倍；帶頂離它還要加細的兩級格心都是半格，不受捨入影響。

## 實案（S5b 用過的外殼，0°，對照同機重跑的基準）

等向盒、背景格 3 m（放粗前的格距）、表面與區域加細等級沿用 S5b-2 的 3 m 層。行人面指標用 `converge` 同一套函式，面積加權並裁到 bbox 外擴 3H。

| 變體 | 格數 | 耗時（秒） | 步數 | 收斂 | 行人面峰值 U_max | 差異 | 3H 內面積平均 U_mean | 差異 |
|---|---|---|---|---|---|---|---|---|
| S5b-2 3 m 層（2026-09-22，只供參考） | 4,455,873 | 3,146.4 | 640 | 是 | 3.1951 | — | 1.9931 | — |
| 基準：今天重跑 | 5,392,753 | 4,187.4 | 663 | 是 | 3.2185 | — | 2.0585 | — |
| 外圍放粗 1 層 | 3,006,356（55.7%） | 2,334.7（55.8%） | 642 | 是 | 3.1799 | −1.20% | 2.0599 | +0.06% |
| 外圍放粗 2 層 | 2,730,141（50.6%） | 2,244.7（53.6%） | 670 | 是 | 3.1823 | −1.13% | 2.0622 | +0.18% |

| 門檻（owner 訂的值） | 放粗 1 層 | 放粗 2 層 |
|---|---|---|
| 行人面峰值差 3% 以內 | 通過（−1.20%） | 通過（−1.13%） |
| 建物外 3H 範圍內的面積平均風速差 5% 以內 | 通過（+0.06%） | 通過（+0.18%） |
| 格數至少減半 | 未通過（55.7%） | 未通過（50.6%） |
| 耗時至少減半 | 未通過（55.8%） | 未通過（53.6%） |

- **收斂：** 三者都在 600 步未收斂、觸發延長，並在延長段收斂（663、642、670 步）。
- **網格品質：** 三者的 checkMesh 都回報 `Failed 1 mesh checks`，原因是少數高偏斜面：基準 4 個、最大偏斜度 8.08；放粗兩個變體各 3 個、最大 5.22。S5b-2 的 3 m 層同樣有 4 個、最大 8.36。
  - 不放粗的網格本來就有，所以不是放粗造成的。
  - 這些面推測在外殼表面的加細處。這是推論：沒有檢查 `skewFaces` 的位置。
  - 最大非正交角三者都是 64.56°。
- **行人面：** 三者的取樣面積都是 96,075 m²，取樣範圍相同。

### 實案為什麼省不到一半

- 這棟建物在 0° 的等向加細盒名目體積約 875 萬 m³。上游端超出 5H 的入口約 51 m，所以在計算域內的部分約 775 萬 m³。
- 以背景格體積 26.39 m³ 推算，基準的 5,392,753 格中：
  - 盒內約 235 萬個 1.5 m 格，約占 44%；
  - 盒外約 297 萬個 3 m 背景格，約占 55%；
  - 其餘約 7 萬來自表面加細。
- 放粗只會減少盒外的格子。盒內的格子、維持原解析度的第 1 層外殼（約 22 萬），以及表面加細（約 8 萬）都不會減少。
  - 因此在 0°，放粗最多只能降到基準的約 49%。
  - 放粗 2 層的 50.6% 已經接近這個下限，再加層數也無法明顯低於一半。
  - 以上是用本目錄的數字推算的，不是實跑結果。
- 放粗 1 層的實際數字：背景格從 3,263,586 降到 418,584，但總格數只從 5,392,753 降到 3,006,356。
- AIJ 的方塊群用 bbox 盒，盒子小，放粗後省得到 6 成以上。
- 所以放粗能省多少，取決於加細盒占計算域的比例。這個外殼的等向盒特別大，原因如下（由盒子座標推算）：
  - 舊定義以 bbox 中心為圓心，半徑是 bbox 外接圓的 123.9 m。
  - 現行定義以頂點重心為圓心，這個重心偏離 bbox 中心約 65 m，所以到最遠頂點的半徑變成 188.9 m。

### 基準為什麼比 S5b-2 多 21% 格

- 外殼相同：STL 的 sha256 都是 `a823c461…`，完整值記在 `b-s7-summary.json`，和 S5b-2 證據的 `mesh_convergence.json` 相同。blockMesh 也相同（背景格 3,263,586），差別只在等向加細盒。
- `29e4eb9`（S5b-2 自審，已在 main）把等向盒改成「外殼頂點重心為圓心、最遠頂點距離為半徑」，對每個風向都相同；S5b-2 的網格是在這之前跑的。
- 以現行定義，這棟建物在 0° 的盒子離 bbox 上游約 7.2H、下游約 4.6H；S5b-2 當時約 2.6H、3.6H。這些距離會隨風向改變。
- 所以實案一律和今天重跑的基準比，S5b-2 的數字只供參考。

## 比較條件與限制

- **每項只跑一次。** 各變體是否收斂已在上表註明。
- **hit rate 的鑑別力有限。** 36% 的門檻只比 E4 的 40% 低約一個標準誤。
- **S5b-2 的面積平均風速本身沒有網格收斂**（只有峰值收斂），所以 5% 門檻的鑑別力有限。
- **耗時是在共用主機上量的，只能當近似值：**
  - E4 當時沒有記錄主機負載，所以 AIJ 的耗時比較是跨日、負載不明的比較。AIJ 只有格數比例（35～36%）不受負載影響。
  - AIJ 兩個變體只記錄了起跑和結束的瞬間 CPU：V1 為 65% 與 18%，V2 為 13% 與 95%。
  - 實案從 09:07 UTC 起每 30 秒取樣一次主機 CPU，平均值：基準 68.3%（只涵蓋後半段）、放粗 1 層 70.5%、放粗 2 層 70.5%。放粗 2 層在 10:44～10:50 UTC 有 5 筆 100%。開發機有 20 個邏輯核心，8 個 OpenFOAM 進程本身約占 40%。
  - 另一個 session 約 09:05 UTC 起部署 181，並在 09:09～09:24 UTC 於本機 Chrome 跑 E2E（見 `docs/evidence/architecture-deepening-v3-2026-09-24/README.md`）。這段時間只和實案基準（08:33～09:43 UTC）重疊，放粗兩個變體沒有碰到。
  - 實案的耗時比例與格數比例幾乎一樣（55.8% 對 55.7%），表示耗時跟著格數走。格數比例不受主機負載影響，單看格數也沒有減半。
- **只有 0°。** 加細盒相對計算域的位置會隨風向改變，其他風向能省多少格數沒有驗證。

## 檔案

| 檔案 | 內容 |
|---|---|
| `b-s7-summary.json` | 各變體的格數、耗時、步數、收斂、指標、門檻判定、主機負載，以及 E4 與 S5b-2 的基準值 |
| `aij_v1/`、`aij_v2/` | `aij_case_c_comparison.json`（逐點比較）、`aij_case_c_scatter.svg`、`case_excerpt.json` |
| `real_base/`、`real_n1/`、`real_n2/` | `case_excerpt.json`：checkMesh 與求解日誌結尾、`run_summary`、背景網格、加細區、行人面與表面壓力指標 |

不放外殼 STL、OpenFOAM 輸出與 AIJ 量測 CSV，也不寫專案名稱。本機路徑以 `<work>`、`<repo>` 取代。

實案摘錄不另列 `case_meta.domain`，但 checkMesh 日誌結尾本來就有計算域外框。這一行和已公開的 S5b-2 證據（`cfd-s5b2-2026-09-22/convergence_isotropic/case_h3p0_excerpt.json`）相同，放粗變體只有 max 側因補齊格數而略大。

## 重現

- **AIJ：** 在 `tools/cfd` 執行

  ```bash
  python -m bimcfd aij-case-c --data-dir <AIJ Case C 資料夾> --out <輸出> --run-id <id> --center 1D --wind-direction 0 --cell 1.5 --refinement-box bbox --np 8 --outer-coarsening-levels 1
  ```

  V2 另加 `--ground-band-height-h 0.2`。

- **實案：** `CaseParams(wind_from_degrees=0, true_north_degrees=0, background_cell_m=3.0, n_procs=8, refinement_box_mode="isotropic", assumptions=["true_north_default_direction"])`，放粗變體另加 `outer_coarsening_levels=1` 或 `2`。`assumptions` 只是紀錄用，不影響數值。
  - 每個變體走 CFD Case Run 的 `solve_case`、`record_case`。
  - 行人面指標用 `convergence.plane_metrics`，裁到 `plane_clip_box`；表面壓力的 `p_min`、`p_max` 用 `convergence.surface_pressure_metrics`。
  - 這和 `run_convergence_study` 每一層做的事相同（`cfd_pipeline/convergence.py`），只是單一格大小。
  - 外殼與前處理輸入是私有資料，不在 repo 內。
