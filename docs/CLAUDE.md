# docs/ — Claude Mirror Entry

本檔是 [`docs/AGENTS.md`](AGENTS.md) 的 Claude 鏡像入口。完整規則以 sibling `AGENTS.md` 為準。

衝突時依根目錄 [`CLAUDE.md`](../CLAUDE.md) §1 優先序解析：

```
使用者最新明確指令 > 根目錄 AGENTS.md / repo-local boundary > 根目錄 CLAUDE.md > OpenSpec artifacts > installed skills / wiki
```

## Verify 入口

無自動化 link checker；PR 描述列出新增 / 修改的 doc path 與「同步來源」即視為 verify pass。`docs/` 內任何文件不得視為程式碼 / API 行為的權威。
