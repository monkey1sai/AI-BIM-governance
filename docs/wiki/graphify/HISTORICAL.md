# Historical Graphify Snapshot

This Graphify output predates the worker-only runtime and may still mention
retired local services such as `_s3_storage`, `_conversion-service`, and
`_conversion-server`.

Use it only as exploration context. Current service boundaries and contracts are
defined in `AGENTS.md`, `README.md`, and `docs/contracts/worker-api.md`.
