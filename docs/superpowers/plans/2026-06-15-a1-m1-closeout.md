# A1/M1 收尾包 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `#a1` 從靜態版型變成 reducer 驅動的五步治理閉環,並讓失敗結果可逐規則展開命中構件(GUID+名稱+樓層),正式收掉 M1/A1 DoD。

**Architecture:** 三層縱切 + 一個純函式狀態機。後端 governance-service 加一支 `GET /api/rule-runs/{id}/failures`(按規則分組、分頁、開 IFC model 一次補 name/type/storey);coordinator 加一條透傳 proxy;前端加 `getFailures` client、純 reducer `a1Machine.ts`、共用元件 `RuleFailuresDrawer`,並把 `A1GovernanceWorkbenchPage` 重構成 stepper(步驟1 收進既有 file-library picker、吸收既有 rule-run 呼叫序、步驟5 串 Excel+BCF 匯出)。`IssuesRuleCenterPage` 仍服務 `#issues` 路由(不刪),其扁平失敗表升級為共用 drawer。

**Tech Stack:** governance-service = FastAPI + host-native Python 3.12 + ifcopenshell(pytest);bim-review-coordinator = Express + TypeScript(node fetch passthrough);web-viewer-sample = React 18 + TypeScript + Vitest + Playwright。

**關鍵現況事實(已逐行查證,動手前必讀):**
- `rule_results` 表只持久化 `id/rule_run_id/ifc_guid/usd_prim_path/rule_code/severity/status/message/evidence_json`(db.py:28-39 + complete_run insert db.py:85-101)。**`ifc_type`/`ifc_name` 沒進 DB** → failures 端點必須開 model 補 name/type/storey。
- `RuleResult` dataclass(rule_engine/models.py:12-31)有 `ifc_type`/`ifc_name` 但無 storey;storey 只能用 `_spatial_chain`(app.py:123)在 request 期算。
- 內部 status 是 `fail`(非 `failed`);`_STATUS_NORMALIZE`(app.py:276)做外部→內部轉換。failures 端點查 DB 要用 `"fail"`。
- `store.get_run(run_id)["ifc_source_path"]` 拿得到 model 路徑(欄位存在,只是 `get_rule_run` JSON 沒回傳它)。`open_model` 無快取 → 一次 request 開一次、loop by_guid 複用同一 handle。
- coordinator proxy 無 router mount,全路徑硬寫在 `governanceProxy.ts`;`queryString(request.originalUrl)` 原樣透傳整串 query;`/failures` 比照 `/results`(governanceProxy.ts:201-207),無需 SSRF/auth 額外守門。
- `EdgeConsole.tsx`:`case "a1"`(:55)、`case "issues" → IssuesRuleCenterPage`(:75)、`case "demo-control" → RealIfcConsolePage`(:85)皆獨立路由 → 從 #a1 移除內嵌 slice 不影響其獨立頁。
- **BCF 匯出契約(步驟5,逐行查證)**:`governanceClient.bcfExportUrl`(governanceClient.ts:155)簽名為 `{model_version_id?, status?}`,**不以 runId 索引**;後端 `GET /api/bcf/export`(bcf/api.py:19)匯的是 **formal issue(`kind=issue` 且有 `ifc_guid`)**,且 `rule-run/:id/export?fmt=bcf` 刻意回 **400**「BCF 匯出請先 from-rule-run 建 issue,再 GET /api/bcf/export」(app.py:294)。coordinator `/api/governance/bcf/export` 透傳已存在(governanceProxy.ts:282)。→ **步驟5 BCF 必排在步驟4 建 issue 之後**,gating 用 `issueCount`(非 runId);本輪只串 UI,不動 BCF writer 本體(spec §3 非目標)。
- **file-library picker(步驟1,逐行查證)**:三層 fsTree 選擇器(project→model→version,`filesTree()` → `GET /api/governance/files/tree`)已存在於 `IssuesRuleCenterPage`(pages.tsx:608-741);spec §4.1 要求本輪「收進 stepper」。→ Task 6 把它原樣搬入 #a1 步驟1,手動輸入路徑保留為 fallback;**不可**退化成純文字 `<input>` 讓使用者手貼路徑。
- 誠實鐵律:3D highlight(`<Btn prov="p1" disabled>` pages.tsx:228/784)維持待建,不做假按鈕。

**驗證指令(各層 canonical,動手前先跑一次拿 baseline):**
- governance-service(cwd `governance-service/`):`"/c/Program Files/Python312/python.exe" -m pytest tests/ -v`(**非 .venv**,需 host ifcopenshell)
- bim-review-coordinator(cwd `bim-review-coordinator/`):`npm run build && npm test`
- web-viewer-sample(cwd `web-viewer-sample/`):`npm run build`、`npx vitest run src/console/<file>`
- e2e(cwd `web-viewer-sample/`):先 `npm run build:ui` + 重啟 :8004 coordinator 服務新 dist-ui,再 `npx playwright test e2e/a1-m1-closeout.spec.ts`

**GitNexus 紀律(每個改 symbol 的 task 內建):** 改 `A1GovernanceWorkbenchPage` / `IssuesRuleCenterPage` / `LifecycleStrip` / `governanceProxy` 等 symbol 前先 `gitnexus_impact({target, direction:"upstream"})` 回報 blast radius;HIGH/CRITICAL 先停下回報;commit 前 `gitnexus_detect_changes`。index stale 先 `npx gitnexus analyze`。

---

## File Structure

**Create:**
- `governance-service/tests/test_rule_failures.py` — failures 端點 + storey helper 測試
- `web-viewer-sample/src/console/a1Machine.ts` — 純 reducer + 型別 + UI 步驟對映
- `web-viewer-sample/src/console/a1Machine.test.ts` — reducer 轉移單元測試
- `web-viewer-sample/src/console/RuleFailuresDrawer.tsx` — 共用「逐規則展開命中構件」元件
- `web-viewer-sample/e2e/a1-m1-closeout.spec.ts` — 端到端

**Modify:**
- `governance-service/app.py` — 加 `_storey_from_element` helper + `GET /api/rule-runs/{run_id}/failures`(插在 export 端點後 ~line 305)
- `bim-review-coordinator/src/routes/governanceProxy.ts` — 加 `/api/governance/rule-runs/:runId/failures` 透傳(插在 :216 後)
- `web-viewer-sample/src/console/governanceClient.ts` — 加 `getFailures` + `RuleFailureRow`/`RuleFailuresResponse` 型別
- `web-viewer-sample/src/console/pages.tsx` — `LifecycleStrip` 改吃 state;`A1GovernanceWorkbenchPage` 重構為 stepper(步驟1 搬入 file-library picker、步驟5 加 BCF 匯出、移除 IssuesRuleCenterPage 內嵌);`IssuesRuleCenterPage` 失敗表(786-797)換成 `RuleFailuresDrawer`
- `web-viewer-sample/src/console/console.test.tsx` — 更新 A1 斷言

---

## Task 1: governance-service failures 端點 + storey enrichment

**Files:**
- Create: `governance-service/tests/test_rule_failures.py`
- Modify: `governance-service/app.py`(加 `_storey_from_element`;加 `get_rule_run_failures`,插在 line 305 `export_rule_run` 之後)

- [ ] **Step 1: 先跑 baseline**

Run: `cd governance-service && "/c/Program Files/Python312/python.exe" -m pytest tests/ -v`
Expected: 既有測試全綠(確認環境 OK 再動)。

- [ ] **Step 2: 寫失敗測試 `tests/test_rule_failures.py`**

```python
"""A1 failures 端點：按規則分組 + 分頁 + 開 model 補 name/type/storey。
storey helper 直接對合成 model 驗(W-001 在 L1、無名牆無樓層)。"""
from __future__ import annotations

import importlib

import pytest
from fastapi.testclient import TestClient


@pytest.fixture()
def client(tmp_path, monkeypatch):
    monkeypatch.setenv("GOV_DB_PATH", str(tmp_path / "gov.db"))
    import app as app_module

    importlib.reload(app_module)
    return TestClient(app_module.app)


def test_storey_from_element(synthetic_model):
    from app import _storey_from_element

    wall_ok = next(w for w in synthetic_model.by_type("IfcWall") if w.Name == "W-001")
    assert _storey_from_element(wall_ok) == "L1"
    unnamed = next(w for w in synthetic_model.by_type("IfcWall") if not w.Name)
    assert _storey_from_element(unnamed) is None


def _run(client, ifc_path) -> str:
    rid = client.post("/api/rule-runs", json={"ifc_source_path": ifc_path}).json()["rule_run_id"]
    for _ in range(50):
        if client.get(f"/api/rule-runs/{rid}").json()["status"] in ("succeeded", "failed"):
            break
    return rid


def test_failures_endpoint_groups_paginates_enriches(client, synthetic_ifc_path):
    rid = _run(client, synthetic_ifc_path)
    body = client.get(f"/api/rule-runs/{rid}/failures").json()
    assert body["rule_run_id"] == rid
    assert body["total"] >= 1
    # 每筆都有 enrichment 欄位(值可能為 null，但 key 必在)
    for f in body["failures"]:
        assert "ifc_guid" in f and "ifc_name" in f and "ifc_type" in f and "storey" in f
        assert f["ifc_guid"]  # 誠實:失敗構件必有真實 ifc_guid


def test_failures_rule_filter_and_pagination(client, synthetic_ifc_path):
    rid = _run(client, synthetic_ifc_path)
    code = client.get(f"/api/rule-runs/{rid}/failures").json()["failures"][0]["rule_code"]
    one = client.get(f"/api/rule-runs/{rid}/failures", params={"rule": code, "limit": 1, "offset": 0}).json()
    assert one["rule_filter"] == code
    assert all(f["rule_code"] == code for f in one["failures"])
    assert len(one["failures"]) <= 1


def test_failures_unknown_run_404(client):
    assert client.get("/api/rule-runs/nope/failures").status_code == 404
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `cd governance-service && "/c/Program Files/Python312/python.exe" -m pytest tests/test_rule_failures.py -v`
Expected: FAIL —— `ImportError: cannot import name '_storey_from_element'` 與 `/failures` 回 404(端點不存在)。

- [ ] **Step 4: GitNexus impact(若 governance-service 有 index)後實作**

先 `gitnexus_impact({target: "get_rule_run_results", direction: "upstream"})` 回報(新端點不改既有 symbol,預期 LOW)。在 `governance-service/app.py` line 305(`export_rule_run` return 之後)插入:

```python
def _storey_from_element(el: Any) -> Optional[str]:
    """從構件的空間容納鏈取 containing storey 名稱;無樓層 → None(誠實,不捏造)。"""
    for node in _spatial_chain(el):
        if node.get("ifc_type") == "IfcBuildingStorey":
            return node.get("name")
    return None


@app.get("/api/rule-runs/{run_id}/failures")
def get_rule_run_failures(
    run_id: str,
    rule: Optional[str] = Query(None),
    limit: int = Query(50),
    offset: int = Query(0),
):
    run_row = store.get_run(run_id)
    if not run_row:
        raise HTTPException(status_code=404, detail="rule run not found")
    rows = store.get_results(run_id, "fail")  # 內部 status 為 'fail'(非 'failed')
    if rule:
        rows = [r for r in rows if r.get("rule_code") == rule]
    total = len(rows)
    page = rows[offset : offset + limit]

    # name/type/storey 未持久化 → 開 model 一次(無快取),loop 複用同一 handle 補齊。
    model = None
    ifc_path = run_row.get("ifc_source_path")
    if page and ifc_path and os.path.exists(ifc_path):
        try:
            model = open_model(ifc_path)
        except Exception:  # noqa: BLE001 - 開檔失敗則降級為無 enrichment,不整包失敗
            model = None

    failures = []
    for r in page:
        guid = r.get("ifc_guid")
        name = ifc_type = storey = None
        if model is not None and guid:
            try:
                el = model.by_guid(guid)
            except Exception:  # noqa: BLE001
                el = None
            if el is not None:
                ifc_type = el.is_a()
                name = getattr(el, "Name", None)
                storey = _storey_from_element(el)
        failures.append({
            "ifc_guid": guid,
            "ifc_name": name,
            "ifc_type": ifc_type,
            "storey": storey,
            "severity": r.get("severity"),
            "rule_code": r.get("rule_code"),
            "message": r.get("message"),
            "usd_prim_path": r.get("usd_prim_path"),
        })
    return {
        "rule_run_id": run_id,
        "rule_filter": rule,
        "limit": limit,
        "offset": offset,
        "total": total,
        "failures": failures,
    }
```

- [ ] **Step 5: 跑測試確認通過**

Run: `cd governance-service && "/c/Program Files/Python312/python.exe" -m pytest tests/test_rule_failures.py -v`
Expected: PASS(4 個測試)。

- [ ] **Step 6: 全套回歸 + detect_changes + commit**

Run: `cd governance-service && "/c/Program Files/Python312/python.exe" -m pytest tests/ -v`
Expected: 全綠(既有測試不壞)。然後 `gitnexus_detect_changes`(scope 應只含 app.py + 新測試)。

```bash
git add governance-service/app.py governance-service/tests/test_rule_failures.py
git commit -m "feat(governance): A1 failures 端點(按規則分組/分頁 + 開 model 補 name/type/storey)"
```

---

## Task 2: coordinator failures 透傳 proxy

**Files:**
- Modify: `bim-review-coordinator/src/routes/governanceProxy.ts`(插在 line 216 `/export` route 之後、line 218 `// A2 ...` 註解之前)

- [ ] **Step 1: baseline build**

Run: `cd bim-review-coordinator && npm run build && npm test`
Expected: 綠(拿 baseline)。

- [ ] **Step 2: GitNexus impact 後加路由**

先 `gitnexus_impact({target: "registerGovernanceProxy", direction: "upstream"})` 回報(新增 route 不改既有,預期 LOW)。在 `governanceProxy.ts:216` 後插入(形狀逐字比照 `/results`):

```typescript
  app.get("/api/governance/rule-runs/:runId/failures", (request, response) => {
    void forward(
      response,
      "GET",
      `/api/rule-runs/${encodeURIComponent(request.params.runId)}/failures${queryString(request.originalUrl)}`,
    );
  });
```

- [ ] **Step 3: build typecheck**

Run: `cd bim-review-coordinator && npm run build`
Expected: PASS(無型別錯)。

- [ ] **Step 4: 既有 proxy 測試(若存在)加一案,否則靠 build + Task 8 E2E**

檢查 `bim-review-coordinator/src/routes/governanceProxy.test.ts` 是否存在:
- 存在 → 比照既有 `/results` 透傳 case 加一個 `/failures` case(斷言 forward 被以 `GET /api/rule-runs/<id>/failures?...` 呼叫)。
- 不存在 → 此 route 為 4 行 passthrough、與 `/results` 同形,由 `npm run build` 型別檢查 + Task 8 E2E(真打 :8004 → :49102)覆蓋;**在本 task commit message 註明「無 proxy 單元測試,passthrough 由 E2E 覆蓋」**(誠實,不假裝有測)。

- [ ] **Step 5: test + detect_changes + commit**

Run: `cd bim-review-coordinator && npm test`
Expected: 綠。`gitnexus_detect_changes` 應只含 governanceProxy.ts(+ 測試)。

```bash
git add bim-review-coordinator/src/routes/governanceProxy.ts
git commit -m "feat(coordinator): 透傳 GET /api/governance/rule-runs/:id/failures(比照 /results,無新守門)"
```

---

## Task 3: 前端 governanceClient.getFailures

**Files:**
- Modify: `web-viewer-sample/src/console/governanceClient.ts`(型別插在 `RuleResultRow`(:44)後;方法插在 `getResults`(:79)後、`exportUrl`(:80)前)

- [ ] **Step 1: 加型別 + 方法**

先 `gitnexus_impact({target: "governanceClient", direction: "upstream"})` 回報(加欄位,預期 LOW)。

在 `RuleResultRow` interface(line 44 `}` 之後)插入:

```ts
export interface RuleFailureRow {
  ifc_guid: string | null;
  ifc_name: string | null;
  ifc_type: string | null;
  storey: string | null;
  severity: string;
  rule_code: string;
  message: string;
  usd_prim_path: string | null;
}

export interface RuleFailuresResponse {
  rule_run_id: string;
  rule_filter: string | null;
  limit: number;
  offset: number;
  total: number;
  failures: RuleFailureRow[];
}
```

在 `getResults`(:76-79)之後、`exportUrl`(:80)之前插入:

```ts
  // A1 失敗構件抽屜資料源：按規則分組 + 分頁 + 後端開 model 補 name/type/storey(GUID 為主鍵必有)。
  getFailures: (id: string, rule?: string, limit = 50, offset = 0) => {
    const qs = new URLSearchParams();
    if (rule) qs.set("rule", rule);
    qs.set("limit", String(limit));
    qs.set("offset", String(offset));
    return jsonFetch<RuleFailuresResponse>(`/api/governance/rule-runs/${id}/failures?${qs.toString()}`);
  },
```

- [ ] **Step 2: build typecheck**

Run: `cd web-viewer-sample && npm run build`
Expected: PASS。

- [ ] **Step 3: commit**

```bash
git add web-viewer-sample/src/console/governanceClient.ts
git commit -m "feat(web): governanceClient.getFailures + RuleFailureRow/Response 型別"
```

---

## Task 4: 純 reducer a1Machine.ts(TDD)

**Files:**
- Create: `web-viewer-sample/src/console/a1Machine.ts`
- Create: `web-viewer-sample/src/console/a1Machine.test.ts`

- [ ] **Step 1: 寫失敗測試 `a1Machine.test.ts`**

```ts
import { describe, expect, it } from "vitest";
import { a1Reducer, initialA1State, uiSteps, type A1State } from "./a1Machine";
import type { RuleRunStatus } from "./governanceClient";

const fakeRun = (status: RuleRunStatus["status"]): RuleRunStatus => ({
  rule_run_id: "rr_1", status, score: 80, rule_set: "default", model_version_id: null,
  summary: { total: 10, passed: 7, failed: 3, errored: 0, target_summary: {}, warnings: [] },
});

describe("a1Reducer 六態轉移", () => {
  it("PICK_FILE 進 picked;空路徑回 idle", () => {
    expect(a1Reducer(initialA1State, { type: "PICK_FILE", ifcPath: "x.ifc" }).step).toBe("picked");
    expect(a1Reducer(initialA1State, { type: "PICK_FILE", ifcPath: "" }).step).toBe("idle");
  });

  it("沒選檔 RUN 不前進(守門)", () => {
    expect(a1Reducer(initialA1State, { type: "RUN" }).step).toBe("idle");
  });

  it("picked→running→scored 全鏈", () => {
    let s: A1State = a1Reducer(initialA1State, { type: "PICK_FILE", ifcPath: "x.ifc" });
    s = a1Reducer(s, { type: "RUN" });
    expect(s.step).toBe("running");
    s = a1Reducer(s, { type: "RUN_PROGRESS", run: fakeRun("running") });
    expect(s.step).toBe("running");
    s = a1Reducer(s, { type: "RUN_DONE", run: fakeRun("succeeded"), failed: [] });
    expect(s.step).toBe("scored");
    expect(s.run?.rule_run_id).toBe("rr_1");
  });

  it("RUN_FAIL 留在 running 並可重試,不前進 dot", () => {
    let s: A1State = a1Reducer(initialA1State, { type: "PICK_FILE", ifcPath: "x.ifc" });
    s = a1Reducer(s, { type: "RUN" });
    s = a1Reducer(s, { type: "RUN_FAIL", error: "boom" });
    expect(s.step).toBe("running");
    expect(s.runError).toBe(true);
    expect(s.error).toBe("boom");
  });

  it("scored→issued→delivered", () => {
    let s: A1State = { ...initialA1State, step: "scored", ifcPath: "x.ifc", run: fakeRun("succeeded") };
    s = a1Reducer(s, { type: "CREATE_ISSUES_OK", issueCount: 3 });
    expect(s.step).toBe("issued");
    expect(s.issueCount).toBe(3);
    s = a1Reducer(s, { type: "EXPORT_OK" });
    expect(s.step).toBe("delivered");
    expect(s.exported).toBe(true);
  });

  it("重跑(RUN)清下游記分但保留已開 issue artifact", () => {
    let s: A1State = { ...initialA1State, step: "issued", ifcPath: "x.ifc", run: fakeRun("succeeded"), failed: [], issueCount: 3 };
    s = a1Reducer(s, { type: "RUN" });
    expect(s.step).toBe("running"); // dot 回到檢核
    expect(s.run).toBeNull();       // 下游記分清空(新檢核將覆蓋)
    expect(s.issueCount).toBe(3);   // 但已落地 artifact 保留可見
  });

  it("uiSteps 反映五點:idle 全 future、delivered 末點 current", () => {
    expect(uiSteps(initialA1State)).toEqual(["future", "future", "future", "future", "future"]);
    const picked = a1Reducer(initialA1State, { type: "PICK_FILE", ifcPath: "x.ifc" });
    expect(uiSteps(picked)).toEqual(["current", "future", "future", "future", "future"]);
    const delivered: A1State = { ...initialA1State, step: "delivered" };
    expect(uiSteps(delivered)).toEqual(["done", "done", "done", "done", "current"]);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd web-viewer-sample && npx vitest run src/console/a1Machine.test.ts`
Expected: FAIL —— 模組不存在。

- [ ] **Step 3: 實作 `a1Machine.ts`**

```ts
// A1 五步治理閉環狀態機(純函式,無 React/IO)。對齊 docs/plans 互動規格 IX-A1(B.1)。
// 證據型更新(PATTERN-EVIDENCE-UPDATE,B.0 mode 1):state 只在伺服器確認後前進;
// 重跑清下游記分但保留已落地 artifact(rule-run/issue/匯出),不樂觀更新、不刪伺服器產物。
import type { RuleRunStatus, RuleResultRow } from "./governanceClient";

export type A1Step = "idle" | "picked" | "running" | "scored" | "issued" | "delivered";

export interface A1State {
  step: A1Step;
  ifcPath: string;
  run: RuleRunStatus | null;     // 含 rule_run_id;runId = run?.rule_run_id
  failed: RuleResultRow[];
  issueCount: number | null;     // artifact:保留至新一輪 CREATE_ISSUES_OK 覆蓋
  exported: boolean;             // artifact
  error: string | null;
  runError: boolean;             // RUN_FAIL:可重試
}

export const initialA1State: A1State = {
  step: "idle", ifcPath: "", run: null, failed: [],
  issueCount: null, exported: false, error: null, runError: false,
};

export type A1Event =
  | { type: "PICK_FILE"; ifcPath: string }
  | { type: "RUN" }
  | { type: "RUN_PROGRESS"; run: RuleRunStatus }
  | { type: "RUN_DONE"; run: RuleRunStatus; failed: RuleResultRow[] }
  | { type: "RUN_FAIL"; error: string }
  | { type: "CREATE_ISSUES_OK"; issueCount: number }
  | { type: "EXPORT_OK" }
  | { type: "RESET" };

const STEP_ORDER: A1Step[] = ["idle", "picked", "running", "scored", "issued", "delivered"];

export function a1Reducer(state: A1State, event: A1Event): A1State {
  switch (event.type) {
    case "PICK_FILE":
      // 換檔 = 全新一輪(連 artifact 一起清,因為換了模型)。
      return { ...initialA1State, step: event.ifcPath ? "picked" : "idle", ifcPath: event.ifcPath };
    case "RUN":
      if (!state.ifcPath) return state; // 守門:沒選檔不可跑
      // 重跑:清下游記分(新檢核將覆蓋),保留 issueCount/exported 為歷史 artifact。
      return { ...state, step: "running", run: null, failed: [], error: null, runError: false };
    case "RUN_PROGRESS":
      return state.step === "running" ? { ...state, run: event.run } : state;
    case "RUN_DONE":
      return { ...state, step: "scored", run: event.run, failed: event.failed, runError: false, error: null };
    case "RUN_FAIL":
      return { ...state, step: "running", runError: true, error: event.error };
    case "CREATE_ISSUES_OK":
      if (!["scored", "issued", "delivered"].includes(state.step)) return state;
      return { ...state, step: state.step === "scored" ? "issued" : state.step, issueCount: event.issueCount };
    case "EXPORT_OK":
      if (!["scored", "issued", "delivered"].includes(state.step)) return state;
      return { ...state, step: "delivered", exported: true };
    case "RESET":
      return initialA1State;
    default:
      return state;
  }
}

export type StepDot = "done" | "current" | "future";

// 五個 UI 步驟(上傳/檢核/記分/開Issue/匯出)對映機器狀態:
// picked→ui0、running→ui1、scored→ui2、issued→ui3、delivered→ui4 為各步「current」。
export function uiSteps(state: A1State): StepDot[] {
  const order = STEP_ORDER.indexOf(state.step); // idle0..delivered5
  const currentUi = order === 0 ? -1 : order - 1; // -1(idle) 或 0..4
  return [0, 1, 2, 3, 4].map((i) => (i < currentUi ? "done" : i === currentUi ? "current" : "future"));
}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd web-viewer-sample && npx vitest run src/console/a1Machine.test.ts`
Expected: PASS(全部)。

- [ ] **Step 5: commit**

```bash
git add web-viewer-sample/src/console/a1Machine.ts web-viewer-sample/src/console/a1Machine.test.ts
git commit -m "feat(web): A1 五步狀態機純 reducer a1Machine(idle→delivered + uiSteps,TDD)"
```

---

## Task 5: 共用元件 RuleFailuresDrawer

**Files:**
- Create: `web-viewer-sample/src/console/RuleFailuresDrawer.tsx`
- (測試併入 console.test.tsx Task 6,或本 task 加最小 smoke)

- [ ] **Step 1: 寫 smoke 測試(加到 `console.test.tsx` 既有 describe 內)**

在 `console.test.tsx` 既有 `describe` 內加(import 既有頂部已有 renderToString):

```tsx
  it("RuleFailuresDrawer:runId 為 null 時誠實提示、有失敗時列出可展開規則列", async () => {
    const { RuleFailuresDrawer } = await import("./RuleFailuresDrawer");
    const empty = renderToString(<RuleFailuresDrawer runId={null} failed={[]} />);
    expect(empty).toContain("尚無 rule-run"); // 誠實:沒有 run 不假裝有資料
    const failed = [
      { ifc_guid: "G1", usd_prim_path: null, rule_code: "ARC-DOOR-REQ-001", severity: "high", status: "fail" as const, message: "缺 FireRating" },
      { ifc_guid: "G2", usd_prim_path: null, rule_code: "ARC-DOOR-REQ-001", severity: "high", status: "fail" as const, message: "缺 FireRating" },
    ];
    const html = renderToString(<RuleFailuresDrawer runId="rr_1" failed={failed} />);
    expect(html).toContain("ARC-DOOR-REQ-001");
    expect(html).toContain("2"); // 該規則失敗筆數
    expect(html).toContain('data-testid="a1-fail-rule-ARC-DOOR-REQ-001"');
  });
```

- [ ] **Step 2: 跑確認失敗**

Run: `cd web-viewer-sample && npx vitest run src/console/console.test.tsx -t "RuleFailuresDrawer"`
Expected: FAIL —— 模組不存在。

- [ ] **Step 3: 實作 `RuleFailuresDrawer.tsx`**

```tsx
// A1 記分板「逐規則展開命中構件」抽屜(共用於 #a1 stepper 與 #issues)。
// 失敗規則清單由已抓到的扁平 failed 依 rule_code 分組(不另打 API);
// 展開某規則才 lazy getFailures 拿 storey-enriched 分頁明細(對齊 IX-A1-03)。
import { useCallback, useMemo, useState } from "react";
import { governanceClient, type RuleResultRow, type RuleFailureRow } from "./governanceClient";

const PAGE = 50;

function RuleRow({ runId, rule_code, count }: { runId: string; rule_code: string; count: number }) {
  const [open, setOpen] = useState(false);
  const [rows, setRows] = useState<RuleFailureRow[]>([]);
  const [total, setTotal] = useState<number | null>(null);
  const [loading, setLoading] = useState(false);
  const [err, setErr] = useState<string | null>(null);

  const loadMore = useCallback(async () => {
    setLoading(true); setErr(null);
    try {
      const res = await governanceClient.getFailures(runId, rule_code, PAGE, rows.length);
      setRows((cur) => [...cur, ...res.failures]);
      setTotal(res.total);
    } catch (e) {
      setErr(String(e));
    } finally {
      setLoading(false);
    }
  }, [runId, rule_code, rows.length]);

  const toggle = useCallback(() => {
    setOpen((o) => {
      const next = !o;
      if (next && rows.length === 0 && total === null && !loading) void loadMore();
      return next;
    });
  }, [rows.length, total, loading, loadMore]);

  return (
    <div className="ec-fail-rule" data-testid={`a1-fail-rule-${rule_code}`}>
      <button className="ec-btn" onClick={toggle} aria-expanded={open}>
        {open ? "▾" : "▸"} {rule_code} · {count} 筆失敗
      </button>
      {open && (
        <table className="ec-table" style={{ marginTop: 6 }}>
          <thead><tr><th>ifc_guid</th><th>名稱</th><th>樓層</th><th>severity</th></tr></thead>
          <tbody>
            {rows.map((r, i) => (
              <tr key={`${r.ifc_guid}-${i}`}>
                <td>
                  <code>{r.ifc_guid}</code>{" "}
                  <button className="ec-btn" data-testid="a1-fail-copy-guid"
                    onClick={() => { if (r.ifc_guid) void navigator.clipboard?.writeText(r.ifc_guid); }}>複製</button>
                </td>
                <td>{r.ifc_name ?? "—"}</td>
                <td>{r.storey ?? "—"}</td>
                <td>{r.severity}</td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
      {open && err && <p className="ec-warn-note">載入失敗:{err}</p>}
      {open && loading && <span className="ec-s">載入中…</span>}
      {open && total !== null && rows.length < total && (
        <button className="ec-btn" disabled={loading} onClick={() => void loadMore()}>載入更多({rows.length}/{total})</button>
      )}
    </div>
  );
}

export function RuleFailuresDrawer({ runId, failed }: { runId: string | null; failed: RuleResultRow[] }) {
  const groups = useMemo(() => {
    const m = new Map<string, number>();
    for (const r of failed) m.set(r.rule_code, (m.get(r.rule_code) ?? 0) + 1);
    return [...m.entries()].map(([rule_code, count]) => ({ rule_code, count })).sort((a, b) => a.rule_code.localeCompare(b.rule_code));
  }, [failed]);

  if (!runId || groups.length === 0) {
    return <p className="ec-note" data-testid="a1-fail-drawer-empty">尚無 rule-run 失敗構件(全過或尚未檢核)。</p>;
  }
  return (
    <div data-testid="a1-fail-drawer">
      {groups.map((g) => <RuleRow key={g.rule_code} runId={runId} rule_code={g.rule_code} count={g.count} />)}
    </div>
  );
}
```

- [ ] **Step 4: 跑確認通過**

Run: `cd web-viewer-sample && npx vitest run src/console/console.test.tsx -t "RuleFailuresDrawer"`
Expected: PASS。

- [ ] **Step 5: commit**

```bash
git add web-viewer-sample/src/console/RuleFailuresDrawer.tsx web-viewer-sample/src/console/console.test.tsx
git commit -m "feat(web): 共用 RuleFailuresDrawer(逐規則懶載入分頁 + GUID/名稱/樓層 + 複製)"
```

---

## Task 6: 重構 #a1 為 reducer 驅動 stepper

**Files:**
- Modify: `web-viewer-sample/src/console/pages.tsx`(`LifecycleStrip` :62-73 改吃 state;`A1GovernanceWorkbenchPage` :190-238 整段重寫,移除 IssuesRuleCenterPage 內嵌)
- Modify: `web-viewer-sample/src/console/console.test.tsx`(更新 A1 斷言)

- [ ] **Step 1: GitNexus impact(必跑,這是中央頁)**

`gitnexus_impact({target: "A1GovernanceWorkbenchPage", direction: "upstream"})` 與 `gitnexus_impact({target: "LifecycleStrip", direction: "upstream"})`,回報 blast radius。預期 d=1(`A1GovernanceWorkbenchPage`)= `EdgeConsole.renderBody`(:55)+ `console.test.tsx`。HIGH/CRITICAL 先停下回報。

**`LifecycleStrip` 有兩個 caller(已查證,非單一)**:`A1GovernanceWorkbenchPage`(:203)與 `ConversionSchedulingPage`(`#conv` 頁,亦傳 `steps`)。Step 2 對 `LifecycleStrip` 的改動**只新增 optional `statuses?` prop**;不傳 `statuses` 時 `cls(i)` 走 `return i === 0 ? "active" : ""` 維持舊行為 → `ConversionSchedulingPage` 不受影響(向後相容,不需改它)。改完後跑 `#conv` 既有 E2E(`minio-watch-auto-intake.spec.ts` / `conversion-artifact-id-sanitize.spec.ts`)當回歸網,確認 `#conv` 步驟列未壞。

- [ ] **Step 2: 改 `LifecycleStrip`(:62-73)吃 state**

把 `LifecycleStrip` 換成:

```tsx
function LifecycleStrip({ steps, statuses }: { steps: string[]; statuses?: ("done" | "current" | "future")[] }) {
  const cls = (i: number) => {
    const st = statuses?.[i];
    if (st === "done") return "done";
    if (st === "current") return "active";
    if (st === "future") return "";
    return i === 0 ? "active" : ""; // 無 statuses 時維持舊行為(向後相容)
  };
  return (
    <div className="ec-flow" style={{ margin: "8px 0 12px" }}>
      {steps.map((s, i) => (
        <span key={s} style={{ display: "flex", alignItems: "center", gap: 4 }}>
          <span className={`ec-flow-step ${cls(i)}`}><span className="ec-flow-n">{i + 1}</span>{s}</span>
          {i < steps.length - 1 && <span className="ec-flow-arrow">→</span>}
        </span>
      ))}
    </div>
  );
}
```

(注:`ec-flow-step` 既有支援 `active`;新增 `done` class 需在 `edge-console.css` 補一條樣式 —— 見 Step 4。)

- [ ] **Step 3: 重寫 `A1GovernanceWorkbenchPage`(:190-238)**

確認頂部 import 已含 `useReducer`/`useState`/`useCallback`/`useEffect`(`pages.tsx` 既用 React hooks);加 import(含 file-library 型別,picker 自 `IssuesRuleCenterPage` slice 搬入):

```tsx
import { a1Reducer, initialA1State, uiSteps } from "./a1Machine";
import { RuleFailuresDrawer } from "./RuleFailuresDrawer";
import { governanceClient, type RuleRunStatus, type FileProjectRow } from "./governanceClient";
```

> **spec §4.1 步驟1=既有 file-library picker(非純文字 input)**:spec 明寫「步驟1 選檔(既有 file-library picker,已存在於 Rule Center slice,本輪收進 stepper)」。下方重寫**把 `IssuesRuleCenterPage`(pages.tsx:608-741)的三層 fsTree 選擇器(`filesTree()` → project/model/version)原樣搬進 stepper 步驟1**,選定 version 時把 `version.path` 餵給 `PICK_FILE`;手動輸入路徑保留為 fallback(檔案庫離線時 graceful degrade,比照來源 slice 行為)。**不可**只放一個讓使用者手貼路徑的純 `<input>`——那偏離 spec 意圖、降低可用性。
>
> **保留口令(對齊 IssuesRuleCenterPage 既有實作,逐欄搬入,不得簡化掉)**:`fsTree`/`fsErr`/`selProject`/`selModel`/`selVersion` state、`loadFsTree`(useCallback)、`useEffect(() => void loadFsTree())` 初載、`resetVersionPick`(換 project/model 清掉先前由選擇器填入的路徑)、`fsModels`/`fsVersions` 衍生、`a1-fs-project`/`a1-fs-model`/`a1-fs-version`/`a1-fs-retry` testid、未載入前三層 select `disabled` + placeholder option 的誠實降級。版本選定後填 `pathInput` 並可直接 `PICK_FILE`。

把 `A1GovernanceWorkbenchPage`(:190-238)整段換成:

```tsx
export function A1GovernanceWorkbenchPage() {
  const [state, dispatch] = useReducer(a1Reducer, initialA1State);
  const [pathInput, setPathInput] = useState("C:\\Repos\\active\\iot\\AI-BIM-governance\\storage\\fixture-bytes.ifc");
  const [idsPath, setIdsPath] = useState("");
  const ui = uiSteps(state);
  const runId = state.run?.rule_run_id ?? null;

  // 步驟1:file-library picker(自 IssuesRuleCenterPage slice 原樣搬入;手動輸入為 fallback)。
  const [fsTree, setFsTree] = useState<FileProjectRow[] | null>(null);
  const [fsErr, setFsErr] = useState<string | null>(null);
  const [selProject, setSelProject] = useState("");
  const [selModel, setSelModel] = useState("");
  const [selVersion, setSelVersion] = useState("");
  const loadFsTree = useCallback(async () => {
    setFsErr(null);
    try {
      const t = await governanceClient.filesTree();
      setFsTree(t.projects);
    } catch (e) {
      setFsErr(String(e));
    }
  }, []);
  useEffect(() => { void loadFsTree(); }, [loadFsTree]);
  // 換 project/model 後,先前由選擇器填入的 pathInput 已不代表當前選擇 → 清掉(僅當仍等於上次選擇器值);手動輸入不受影響。
  const resetVersionPick = useCallback(() => {
    if (selVersion) setPathInput((cur) => (cur === selVersion ? "" : cur));
    setSelVersion("");
  }, [selVersion]);
  const fsModels = fsTree?.find((p) => p.project_id === selProject)?.models ?? [];
  const fsVersions = fsModels.find((m) => m.model_id === selModel)?.versions ?? [];

  const doRun = useCallback(async () => {
    if (!state.ifcPath) return;
    dispatch({ type: "RUN" });
    try {
      const { rule_run_id } = await governanceClient.createRuleRun({ ifc_source_path: state.ifcPath, ids_path: idsPath || undefined });
      let st: RuleRunStatus | null = null;
      for (let i = 0; i < 60; i++) {
        st = await governanceClient.getRuleRun(rule_run_id);
        dispatch({ type: "RUN_PROGRESS", run: st });
        if (st.status === "succeeded" || st.status === "failed") break;
        await new Promise((r) => setTimeout(r, 1000));
      }
      if (st && st.status === "succeeded") {
        const failed = await governanceClient.getResults(rule_run_id, "failed");
        dispatch({ type: "RUN_DONE", run: st, failed });
      } else {
        dispatch({ type: "RUN_FAIL", error: st ? `rule-run ${st.status}` : "no status" });
      }
    } catch (e) {
      dispatch({ type: "RUN_FAIL", error: String(e) });
    }
  }, [state.ifcPath, idsPath]);

  const makeIssues = useCallback(async () => {
    if (!runId) return;
    try {
      const { created } = await governanceClient.issuesFromRuleRun(runId);
      dispatch({ type: "CREATE_ISSUES_OK", issueCount: created });
    } catch { /* 後端離線:誠實不前進 */ }
  }, [runId]);

  const doExport = useCallback(async () => {
    if (!runId) return;
    try {
      const res = await fetch(governanceClient.exportUrl(runId));
      if (!res.ok) return;
      const blob = await res.blob();
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a"); a.href = url; a.download = `rule-run-${runId}.xlsx`; a.click();
      setTimeout(() => URL.revokeObjectURL(url), 0);
      dispatch({ type: "EXPORT_OK" });
    } catch { /* 誠實不前進 */ }
  }, [runId]);

  // 步驟5 BCF 匯出(spec §4.1/§4.3 要求)。BCF 契約 = 全域 formal issue(kind=issue 且有 ifc_guid)匯出,
  // 不以 runId 索引(governanceClient.bcfExportUrl 簽名為 {model_version_id?, status?});後端 /api/rule-runs/:id/export?fmt=bcf
  // 刻意回 400「BCF 匯出請先 from-rule-run 建 issue,再 GET /api/bcf/export」。故 BCF 必在步驟4 建 issue 之後才有東西可匯;
  // gating 用 issueCount(已開 issue artifact)而非 runId,對齊 issued→delivered 資料流。
  const doExportBcf = useCallback(async () => {
    if (!state.issueCount) return; // 無 formal issue → 後端 404,誠實不觸發
    try {
      const res = await fetch(governanceClient.bcfExportUrl());
      if (!res.ok) return;
      const blob = await res.blob();
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a"); a.href = url; a.download = `governance-issues.bcfzip`; a.click();
      setTimeout(() => URL.revokeObjectURL(url), 0);
      dispatch({ type: "EXPORT_OK" });
    } catch { /* 誠實不前進 */ }
  }, [state.issueCount]);

  return (
    <>
      <h1>A1 · 治理與模型檢核</h1>
      <p className="ec-lead">上傳/選取 IFC,跑自動規則檢核,直接產生 Issue 與 BCF/Excel。規則檢核在 governance-service(CPU)完成;3D 高亮需 GPU viewport(依 review session 派發,待建)。</p>

      <Panel title="A1 五步引導式流程" sub="整頁狀態機驅動;步驟依當前 state 亮燈(證據型更新,禁樂觀)" prov="asbuilt">
        <LifecycleStrip steps={["上傳模型", "自動檢核", "結果記分板", "開 Issue", "匯出 BCF"]} statuses={ui} />
        <div className="ec-grid" style={{ marginBottom: 8 }}>
          <Field k="rule_run_id" v={runId ?? "—"} prov="asbuilt" />
          <Field k="step" v={state.step} prov="asbuilt" />
          {state.issueCount !== null && <Field k="已開 issue(artifact)" v={String(state.issueCount)} prov="asbuilt" />}
        </div>

        {/* 步驟1:file-library picker(spec §4.1,自 IssuesRuleCenterPage 搬入)→ 選定 version 填路徑;手動輸入為 fallback → PICK_FILE */}
        <div className="ec-field" style={{ flexDirection: "column", alignItems: "stretch", gap: 6, marginBottom: 6 }}>
          <span className="ec-k">從檔案庫選擇 <ProvTag prov="asbuilt" /></span>
          {fsErr && (
            <span className="ec-warn-note" style={{ display: "inline-flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
              <span>檔案庫不可用({fsErr});可改用下方手動輸入路徑。</span>
              <Btn data-testid="a1-fs-retry" caption="GET /api/governance/files/tree" onClick={() => { void loadFsTree(); }}>重試載入檔案庫</Btn>
            </span>
          )}
          {!fsErr && !fsTree && <span className="ec-s">載入檔案庫中…(GET /api/governance/files/tree)</span>}
          <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}>
            <select data-testid="a1-fs-project" className="ec-btn" value={selProject} disabled={!fsTree}
              onChange={(e) => { setSelProject(e.target.value); setSelModel(""); resetVersionPick(); }}>
              <option value="">專案…</option>
              {(fsTree ?? []).map((p) => <option key={p.project_id} value={p.project_id}>{p.project_id}</option>)}
            </select>
            <select data-testid="a1-fs-model" className="ec-btn" value={selModel} disabled={!selProject}
              onChange={(e) => { setSelModel(e.target.value); resetVersionPick(); }}>
              <option value="">模型…</option>
              {fsModels.map((m) => <option key={m.model_id} value={m.model_id}>{m.model_id}</option>)}
            </select>
            <select data-testid="a1-fs-version" className="ec-btn" disabled={!selModel} value={selVersion}
              onChange={(e) => {
                const picked = e.target.value;
                if (picked) { setSelVersion(picked); setPathInput(picked); } else { resetVersionPick(); }
              }}>
              <option value="">版本…(選定填入路徑)</option>
              {fsVersions.map((v) => <option key={v.name} value={v.path}>{v.name}</option>)}
            </select>
          </div>
        </div>
        {/* 手動輸入路徑 fallback + 鎖定 → PICK_FILE */}
        <div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" }}>
          <input className="ec-btn" data-testid="a1-step-path" style={{ minWidth: 420 }} value={pathInput}
            onChange={(e) => setPathInput(e.target.value)} />
          <Btn data-testid="a1-step-pick" caption="鎖定此模型路徑(進入步驟2)" onClick={() => dispatch({ type: "PICK_FILE", ifcPath: pathInput })}>選取模型</Btn>
        </div>
        <div style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 6 }}>
          <input className="ec-btn" style={{ minWidth: 420 }} placeholder="(選填)buildingSMART IDS .ids 路徑" value={idsPath} onChange={(e) => setIdsPath(e.target.value)} />
        </div>

        {/* 步驟2:檢核 → RUN */}
        <div style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 8, flexWrap: "wrap" }}>
          <Btn primary data-testid="a1-step-run" disabled={state.step === "idle" || state.step === "running"}
            caption="POST /api/governance/rule-runs" onClick={doRun}>
            {state.step === "running" ? "檢核中…" : "執行規則檢核"}
          </Btn>
          {state.runError && <span className="ec-warn-note">檢核失敗(可重試):{state.error}</span>}
        </div>
      </Panel>

      {/* 步驟3:記分板 + 失敗構件抽屜 */}
      {state.run && (
        <Panel title="結果記分板" sub="真實 rule-run summary;點規則列展開命中構件(GUID/名稱/樓層)" prov="asbuilt">
          <div className="ec-grid" data-testid="a1-rulerun-scoreboard">
            <Metric value={state.run.summary?.total ?? "—"} label="評估構件" />
            <Metric value={state.run.summary?.passed ?? "—"} label="passed" />
            <Metric value={state.run.summary?.failed ?? "—"} label="failed" tone="warn" />
            <Metric value={state.run.score ?? "—"} label="score" />
          </div>
          <div style={{ marginTop: 12 }}>
            <RuleFailuresDrawer runId={runId} failed={state.failed} />
          </div>
        </Panel>
      )}

      {/* 步驟4/5 + 3D 待建 */}
      <Panel title="交付" sub="開 Issue / 匯出 BCF·Excel 走真實後端;3D 高亮待建(不提供假按鈕)" prov="asbuilt">
        <Btn data-testid="a1-step-issues" disabled={state.step === "idle" || state.step === "picked" || state.step === "running"}
          caption="POST /api/governance/issues/from-rule-run/:id" onClick={makeIssues}>失敗構件建 Issue</Btn>{" "}
        <Btn data-testid="a1-step-export" disabled={!runId || state.run?.status !== "succeeded"}
          caption="GET /api/governance/rule-runs/:id/export?fmt=excel" onClick={doExport}>匯出 Excel</Btn>{" "}
        {/* 步驟5 BCF(spec §4.1):gating 用 issueCount —— BCF 匯的是 formal issue,故須先步驟4 建 issue;非以 runId 索引。 */}
        <Btn data-testid="a1-step-export-bcf" disabled={!state.issueCount}
          caption="GET /api/governance/bcf/export(先建 issue;匯 kind=issue 且有 ifc_guid)" onClick={doExportBcf}>匯出 BCF</Btn>{" "}
        <Btn prov="p1" disabled caption="需 viewer DataChannel(highlightPrimsRequest)— 後續整合(M3/M4)">在 3D 高亮</Btn>
      </Panel>

      <section data-testid="a1-real-ifc-slice" className="ec-a1-inline-slice">
        <RealIfcConsolePage />
      </section>
    </>
  );
}
```

(`RealIfcConsolePage` 已在 pages.tsx 其他處 import/使用;若 A1 頁原先未 import 它,加 `import { RealIfcConsolePage } from "./RealIfcConsolePage";`。移除原本內嵌的 `<IssuesRuleCenterPage />` slice —— 它仍由 `#issues` 路由獨立服務。)

- [ ] **Step 4: 補 `edge-console.css` 的 `.ec-flow-step.done` 樣式**

在 `web-viewer-sample/src/console/edge-console.css` 既有 `.ec-flow-step.active` 規則附近加:

```css
.ec-flow-step.done { opacity: 1; border-color: var(--ec-ok, #2e7d32); }
.ec-flow-step.done .ec-flow-n { background: var(--ec-ok, #2e7d32); }
```

(若既有變數名不同,沿用既有 active 規則用的顏色變數;此為純視覺,E2E 不依賴顏色。)

- [ ] **Step 5: 更新 `console.test.tsx` A1 斷言(:348-362 對 `A1GovernanceWorkbenchPage` 的那段)**

移除 #a1 內嵌的 `IssuesRuleCenterPage` 後,該 slice 貢獻的字串會從 `renderToString(<A1GovernanceWorkbenchPage/>)` 消失。逐條處置(先改再跑,讓測試告訴你還缺哪條):

`保留`(stepper LifecycleStrip / Field / 仍內嵌的 RealIfcConsolePage 仍渲染):`"上傳模型"`、`"自動檢核"`、`"開 Issue"`、`"匯出 BCF"`、`"rule_run_id"`、`'data-testid="a1-real-ifc-slice"'`、`'data-testid="real-ifc-demo-control"'`。

`移除/替換`(來自已移除的 IssuesRuleCenterPage,新 #a1 不再渲染):刪掉 `'data-testid="a1-rule-center-slice"'`、`"A1 rule-run authority"`、`"governance-service :49102"`、`"review_session_id"`、`"viewer_url（/ui/open）"` 這幾條對 `a1` 變數的斷言(這些行為已移到 `#issues` 的 `IssuesRuleCenterPage`,其 honesty 由既有 :43-52 `IssuesRuleCenterPage` 測試覆蓋,不需在 #a1 重複)。

`新增`(stepper 新 testid;含 file-library picker 與 BCF):
```tsx
    expect(a1).toContain('data-testid="a1-fs-project"');   // 步驟1 file-library picker(spec §4.1,自 slice 搬入)
    expect(a1).toContain('data-testid="a1-fs-version"');   // picker 版本層(選定填路徑)
    expect(a1).toContain('data-testid="a1-step-run"');     // stepper 檢核鈕
    expect(a1).toContain('data-testid="a1-step-issues"');  // 開 Issue
    expect(a1).toContain('data-testid="a1-step-export"');  // 匯出 Excel
    expect(a1).toContain('data-testid="a1-step-export-bcf"'); // 匯出 BCF(spec §4.1 步驟5)
    expect(a1).toContain("POST /api/governance/rule-runs"); // 取代「governance-service :49102」:證 #a1 走真 proxy
```
(注:`a1-fs-project`/`a1-fs-model`/`a1-fs-version`/`a1-fs-retry` 現在同時出現在 #a1 與 #issues 兩頁;兩處測試各自斷言自己頁面渲染了 picker,testid 重複是預期——picker 在 stepper 與 Rule Center 都在。)

跑 `npx vitest run src/console/console.test.tsx -t "prototype 核心頁面"` 直到綠;**只動 A1(`a1` 變數)那幾行,viewer/conv/sessions/fleet/minio 的斷言不要碰**(它們的頁面未改)。

- [ ] **Step 6: 跑前端單元 + build**

Run: `cd web-viewer-sample && npx vitest run src/console/console.test.tsx && npm run build`
Expected: PASS + build 綠。

- [ ] **Step 7: detect_changes + commit**

`gitnexus_detect_changes` 應只含 pages.tsx + console.test.tsx + edge-console.css。

```bash
git add web-viewer-sample/src/console/pages.tsx web-viewer-sample/src/console/console.test.tsx web-viewer-sample/src/console/edge-console.css
git commit -m "feat(web): #a1 重構為 reducer 驅動五步 stepper + 失敗抽屜(移除內嵌 IssuesRuleCenterPage,改 #issues 獨立服務)"
```

---

## Task 7: #issues 失敗表升級為共用 drawer

**Files:**
- Modify: `web-viewer-sample/src/console/pages.tsx`(`IssuesRuleCenterPage` 的扁平失敗表 :786-797 換成 `RuleFailuresDrawer`)

- [ ] **Step 1: GitNexus impact**

`gitnexus_impact({target: "IssuesRuleCenterPage", direction: "upstream"})` 回報(d=1 = EdgeConsole `case "issues"` + A1 頁原內嵌已移除 → 預期 LOW)。

- [ ] **Step 2: 換掉扁平表**

確認 `RuleFailuresDrawer` 已 import(同檔 Task 6 已加;若無則加 `import { RuleFailuresDrawer } from "./RuleFailuresDrawer";`)。把 `IssuesRuleCenterPage` 內 :786-797 這段:

```tsx
        {failed.length > 0 && (
          <table className="ec-table" style={{ marginTop: 12 }}>
            <thead><tr><th>rule_code</th><th>severity</th><th>ifc_type</th><th>ifc_guid</th><th>usd_prim_path</th></tr></thead>
            <tbody>
              {failed.slice(0, 30).map((r, i) => (
                <tr key={i}>
                  <td>{r.rule_code}</td><td>{r.severity}</td><td>{(r as { ifc_type?: string }).ifc_type ?? ""}</td>
                  <td>{r.ifc_guid}</td><td>{r.usd_prim_path ?? <span className="ec-warn-note">null（未對映）</span>}</td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
```

換成:

```tsx
        {run?.status === "succeeded" && (
          <div style={{ marginTop: 12 }}>
            <RuleFailuresDrawer runId={runId} failed={failed} />
          </div>
        )}
```

- [ ] **Step 3: 前端單元 + build**

Run: `cd web-viewer-sample && npx vitest run src/console/console.test.tsx && npm run build`
Expected: PASS + 綠。若既有測試斷言舊扁平表欄位(`usd_prim_path` 表頭等),更新為 drawer 的 testid `a1-fail-drawer`/`a1-fail-drawer-empty`。

- [ ] **Step 4: detect_changes + commit**

```bash
git add web-viewer-sample/src/console/pages.tsx
git commit -m "refactor(web): #issues 失敗扁平表改用共用 RuleFailuresDrawer(逐規則+樓層)"
```

---

## Task 8: Browser E2E

**Files:**
- Create: `web-viewer-sample/e2e/a1-m1-closeout.spec.ts`

前置(乾淨環境必做,靠人工/指揮官紀律):`cd web-viewer-sample && npm run build:ui` → 重啟 :8004 coordinator 服務新 dist-ui,`BIM_FILE_LIBRARY_ROOT` 指含 fixture 的 storage;governance-service :49102 已起。

- [ ] **Step 1: 寫 spec(skip-gate 比照 a2-version-diff-selector.spec.ts)**

```ts
import { test, expect } from "@playwright/test";

// A1/M1 收尾端到端:#/a1 reducer stepper 走真 rule-run → 記分 → 展開失敗規則看 GUID/名稱/樓層 → 開 Issue → 匯出。
// 服務這頁的是 coordinator 已 build 的 dist-ui(npm run build:ui),非 playwright.config fresh viewer。
// 前置缺失 → conditional skip(誠實,不假跑)。本 repo 無 Playwright CI job,不會 false-green。
const COORDINATOR = process.env.E2E_COORDINATOR_BASE_URL || "http://127.0.0.1:8004";

test.describe("A1/M1 收尾:#a1 五步 stepper + 失敗抽屜", () => {
  test.setTimeout(180_000);

  test.beforeEach(async ({ request, page }) => {
    // 守門(1):governance files/tree 可用(代表 :49102 + proxy 活著)。
    let apiOk = false;
    try {
      const res = await request.get(`${COORDINATOR}/api/governance/files/tree`);
      apiOk = res.ok();
    } catch { apiOk = false; }
    test.skip(!apiOk, "governance proxy 未備妥(需 :49102 + coordinator proxy)");

    // 守門(2):dist-ui 是本 branch(#/a1 有 stepper testid a1-step-run)。
    let uiOk = false;
    try {
      await page.goto(`${COORDINATOR}/ui/#/a1`);
      await page.getByTestId("a1-step-run").waitFor({ state: "visible", timeout: 15_000 });
      uiOk = true;
    } catch { uiOk = false; }
    test.skip(!uiOk, "coordinator dist-ui 非本 branch(#/a1 缺 a1-step-run):需 npm run build:ui 後重啟 :8004。");
  });

  test("選模型 → 自動亮步驟2 → 檢核 succeeded → 展開失敗規則看 GUID/名稱/樓層 → 開 Issue → 匯出", async ({ page }) => {
    // beforeEach 已停在 #/a1。步驟1:鎖定 fixture 路徑(input 預設已是 fixture-bytes.ifc)→ PICK_FILE。
    await page.getByTestId("a1-step-pick").click();
    // 自動亮步驟2:檢核鈕 enable(reducer 進 picked)。
    await expect(page.getByTestId("a1-step-run")).toBeEnabled({ timeout: 5_000 });

    // 步驟2:檢核 → 等記分板出現(succeeded 後 RUN_DONE)。
    await page.getByTestId("a1-step-run").click();
    await page.getByTestId("a1-rulerun-scoreboard").waitFor({ state: "visible", timeout: 120_000 });

    // 步驟3:展開第一條失敗規則(若有失敗) → 斷言抽屜出現、有「複製」鈕與樓層欄。
    const drawer = page.getByTestId("a1-fail-drawer");
    const empty = page.getByTestId("a1-fail-drawer-empty");
    const sawDrawer = await Promise.race([
      drawer.waitFor({ state: "visible", timeout: 10_000 }).then(() => true, () => false),
      empty.waitFor({ state: "visible", timeout: 10_000 }).then(() => false, () => false),
    ]);
    if (sawDrawer) {
      // 點第一條規則展開 → 命中構件列出 + 複製鈕可見(GUID/名稱/樓層欄存在)。
      await page.locator('[data-testid^="a1-fail-rule-"] button').first().click();
      await expect(page.getByTestId("a1-fail-copy-guid").first()).toBeVisible({ timeout: 15_000 });
    }

    // 步驟4:開 Issue(enable 於 scored 後)→ 建 formal issue。
    await expect(page.getByTestId("a1-step-issues")).toBeEnabled({ timeout: 5_000 });
    await page.getByTestId("a1-step-issues").click();

    // 步驟5(Excel):匯出鈕 enable(succeeded 後)。
    await expect(page.getByTestId("a1-step-export")).toBeEnabled({ timeout: 10_000 });

    // 步驟5(BCF,spec §4.1/§2.4 明列):BCF 鈕 gating = 已開 issue(issueCount>0),建 issue 後 enable → 觸發 BCF 匯出。
    // BCF 匯的是 formal issue(kind=issue 且有 ifc_guid),不以 runId 索引(後端 /api/bcf/export)。
    await expect(page.getByTestId("a1-step-export-bcf")).toBeEnabled({ timeout: 10_000 });
    await page.getByTestId("a1-step-export-bcf").click();
    // EXPORT_OK 後 step 進 delivered(末步點亮);證 BCF 觸發真的推進狀態機到交付。
    await expect(page.getByText("delivered")).toBeVisible({ timeout: 10_000 });

    await page.screenshot({ path: "../artifacts/e2e/a1-m1-closeout-flow.png", fullPage: true });
  });

  test("重跑檢核 → 下游(Issue/匯出旗標)清空,但 rule-run artifact(已開 issue)仍可見", async ({ page }) => {
    // spec §6.3:重跑路徑必須先把下游真的「做出來」(開 Issue + 匯出 → delivered),
    // 才能斷言重跑後「下游旗標清空、已產出 artifact 保留」。不可像舊版那樣重跑前從未開 issue/匯出
    //(那只證「可重跑不崩」,無法證 artifact 保留,違反 spec §6.3)。

    // 先把完整下游做出來:檢核 → 開 Issue → 匯出 BCF(進 delivered)。
    await page.getByTestId("a1-step-pick").click();
    await page.getByTestId("a1-step-run").click();
    await page.getByTestId("a1-rulerun-scoreboard").waitFor({ state: "visible", timeout: 120_000 });
    await expect(page.getByTestId("a1-step-issues")).toBeEnabled({ timeout: 5_000 });
    await page.getByTestId("a1-step-issues").click();
    // 已開 issue 後:artifact Field 出現(「已開 issue(artifact)」),且 BCF 鈕 enable。
    await expect(page.getByText("已開 issue(artifact)")).toBeVisible({ timeout: 10_000 });
    await expect(page.getByTestId("a1-step-export-bcf")).toBeEnabled({ timeout: 10_000 });
    await page.getByTestId("a1-step-export-bcf").click();
    await expect(page.getByText("delivered")).toBeVisible({ timeout: 10_000 }); // 下游已推進到交付

    // 重跑:再按檢核 → reducer 進 running、清下游記分/匯出旗標,但保留 issueCount artifact。
    await page.getByTestId("a1-step-run").click();
    // 斷言(a) 下游旗標清空:delivered 文字消失(step 已回退,不再是 delivered)。
    await expect(page.getByText("delivered")).toBeHidden({ timeout: 10_000 });
    // 斷言(b) rule-run artifact 仍在:已開 issue artifact Field 持續可見(reducer 不因 RUN 清掉已落地產物)。
    await expect(page.getByText("已開 issue(artifact)")).toBeVisible();
    // 重跑收斂後記分板重建(證可重跑且不崩)。
    await page.getByTestId("a1-rulerun-scoreboard").waitFor({ state: "visible", timeout: 120_000 });
    await page.screenshot({ path: "../artifacts/e2e/a1-m1-closeout-rerun.png", fullPage: true });
  });
});
```

- [ ] **Step 2: 跑 E2E(前置就緒時)**

Run: `cd web-viewer-sample && npx playwright test e2e/a1-m1-closeout.spec.ts`
Expected: 前置就緒 → PASS + 截圖落 `artifacts/e2e/a1-m1-closeout-*.png`;前置缺失 → conditional skip(誠實計 pass)。**回報實際是 PASS 還是 skip,不可把 skip 當通過宣稱。**

- [ ] **Step 3: commit**

```bash
git add web-viewer-sample/e2e/a1-m1-closeout.spec.ts
git commit -m "test(web): A1/M1 收尾 E2E(stepper 五步 + 失敗抽屜樓層 + 重跑路徑)"
```

---

## Task 9: 收尾驗證 + PR

- [ ] **Step 1: 三層全套驗證(逐一回報 PASS/SKIP)**

```bash
cd governance-service && "/c/Program Files/Python312/python.exe" -m pytest tests/ -v
cd ../bim-review-coordinator && npm run build && npm test
cd ../web-viewer-sample && npm run build && npx vitest run src/console/
```
Expected: 三層皆綠。E2E 依 Task 8 結果(就緒 PASS / 缺前置 skip,如實回報)。

- [ ] **Step 2: GitNexus detect_changes 終檢**

`gitnexus_detect_changes({scope: "compare", base_ref: "main"})` —— 確認改動範圍 = app.py / test_rule_failures.py / governanceProxy.ts / governanceClient.ts / a1Machine.ts(+test)/ RuleFailuresDrawer.tsx / pages.tsx / console.test.tsx / edge-console.css / e2e spec,無外溢。commit 後若 hook 未自動更新,`npx gitnexus analyze`(保留 embeddings 加 `--embeddings`)。

- [ ] **Step 3: 開 PR**

```bash
git push -u origin feat/a1-m1-closeout
gh pr create --base main --title "feat: A1/M1 收尾包(#a1 五步 stepper 狀態機 + 失敗構件抽屜含樓層)" --body "$(cat <<'EOF'
## 摘要
收掉 M1/A1 DoD:把 #a1 從靜態版型變成 reducer 驅動的五步治理閉環(idle→picked→running→scored→issued→delivered),並讓失敗結果可逐規則展開命中構件(GUID+名稱+樓層)。

spec:docs/superpowers/specs/2026-06-15-a1-m1-closeout-design.md
plan:docs/superpowers/plans/2026-06-15-a1-m1-closeout.md

## 改動(三層縱切)
- governance-service:`GET /api/rule-runs/:id/failures`(按規則分組/分頁 + 開 model 一次補 name/type/storey;DB 未持久化這些欄位)
- coordinator:`/api/governance/rule-runs/:id/failures` 透傳(比照 /results,無新守門)
- web-viewer-sample:`getFailures` client、純 reducer `a1Machine`、共用 `RuleFailuresDrawer`、`#a1` stepper 重構、`#issues` 失敗表升級

## 驗證
- governance pytest / coordinator build+test / web build+vitest 全綠
- Browser E2E:#a1 五步 + 失敗抽屜樓層 + 重跑(就緒則 PASS,附截圖 artifacts/e2e/a1-m1-closeout-*)

## 誠實標記
- 3D highlight 維持待建(p1 disabled,需 M3/M4)
- 構件無容器時樓層顯「—」(不捏造)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4: 顧 CI** —— 等 GitHub Actions;pr-review-agent 綠(或如 memory 所述非 required check 時依紀律處置);CI 綠後依對抗修復迴圈紀律 merge。

---

## Self-Review(對照 spec 逐項)

| spec 要求 | 對應 task |
|---|---|
| §2.1 五步 reducer 狀態機 + 圓點 + 自動亮步驟2 + 重跑清下游保留 artifact | Task 4(reducer)+ Task 6(wire) |
| §2.2 記分板逐規則展開 GUID+名稱+樓層、懶載入分頁、複製、全過不可展開 | Task 1(端點)+ Task 3(client)+ Task 5(drawer)+ Task 6/7(掛載) |
| §2.3 3D highlight 待建、樓層 null 降級「—」 | Task 6(p1 disabled)+ Task 1/5(storey null → 「—」) |
| §2.4 Browser E2E 全鏈(含「匯出 BCF」)+ 重跑路徑 | Task 8(happy-path 觸發 `a1-step-export-bcf` 至 delivered;rerun 先 issue+BCF 再重跑) |
| §4.1 步驟1=既有 file-library picker 收進 stepper(非純文字 input) | Task 6 Step 3(自 IssuesRuleCenterPage 搬入三層 fsTree picker,手動輸入為 fallback) |
| §4.1/§4.3 步驟5 = exportUrl(Excel)+ bcfExportUrl(BCF)→ delivered | Task 6 Step 3(`doExportBcf` + `a1-step-export-bcf` 鈕,gating=issueCount)+ Task 8(E2E 觸發 BCF) |
| §4.1 reducer 吸收既有呼叫序、client 原封複用、移除無條件 active | Task 4 + Task 6 |
| §4.2 後端 failures 端點 + spatial-chain 補樓層 + coordinator proxy + client + UI | Task 1/2/3/5/6/7 |
| §5 錯誤處理(RUN_FAIL 可重試、storey null、全過、重跑、禁樂觀、分頁去重) | Task 4(reducer)+ Task 1(null)+ Task 5(loading 鎖) |
| §6 三層測試 + E2E | Task 1/4/5(單元)+ Task 8(E2E)+ Task 9 |
| §7 中央頁回歸 → GitNexus impact + 既有 E2E 回歸網;storey 當頁 enrichment | 每個改 symbol task 內建 impact;Task 1 僅當頁 enrichment |

**Placeholder scan:** 無 TBD/TODO;所有 code step 含完整 code;測試含真實斷言。
**型別一致性:** `RuleFailureRow`/`RuleFailuresResponse`(Task 3)↔ `getFailures`(Task 3)↔ drawer 用法(Task 5)↔ 端點回傳(Task 1)欄位一致(ifc_guid/ifc_name/ifc_type/storey/severity/rule_code)。reducer `A1Event`/`A1State`(Task 4)↔ A1 頁 dispatch(Task 6)事件名一致。
**已知風險:** 大型真 IFC 每次展開 open_model 無快取可能略慢(lazy + spinner 緩解;快取為 YAGNI,留 follow-up)。`edge-console.css` 變數名需現場確認(純視覺,E2E 不依賴)。`LifecycleStrip` 為 `#a1` 與 `#conv`(`ConversionSchedulingPage`)共用元件,改 signature 只加 optional `statuses?`(向後相容,`#conv` 不傳故不受影響),Task 6 Step 1 已標記並以 `#conv` E2E 當回歸網。
