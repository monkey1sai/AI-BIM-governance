// Moved to src/coordinatorClient (docs/architecture/coordinator-browser-client-adr.md). This re-export keeps the console
// importers working until the import codemod (ADR §4, bullet 2).
export * from "../coordinatorClient";
