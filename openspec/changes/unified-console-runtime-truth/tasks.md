# Tasks：unified-console-runtime-truth

**勾選規則**：UI 相關 task（§1–§3、§5 semantic、§6）只能以 canonical-linux `:8004/ui` 截圖＋同分鐘 API JSON 對照勾選；本機通過只能註記「本機綠，待 181」，不得打勾。每個 task 附最小驗證指令；指令 cwd 為 repo root（或對應 worktree）；vitest／tsc 指令 cwd 為 `web-viewer-sample`。編輯任何 symbol 前先 `gitnexus impact <Symbol> -d upstream -r AI-BIM-governance`，HIGH／CRITICAL 先回報。§0 裁決未寫入前，對應段落 HELD（0.1／0.4 → §5；0.2 → §4.2；0.3 → §4.4；0.5 → 側欄入口；0.6 → §3.2）。

## 0. Owner 裁決 gate（2026-08-25 owner 已全數裁決；§5 解除 HELD）

- [x] 0.1 D1 裁決：owner 在本行下方寫「D1 裁決：P｜H，日期」。驗證：`rg -n "D1 裁決" openspec/changes/unified-console-runtime-truth/tasks.md`
  - D1 裁決：P（六屏改以產品面誠實 offline／empty 狀態為 golden，owner 明示後雙旗標 rebaseline），2026-08-25 owner 口令「D1–D4 的選擇建議選項」。
- [x] 0.2 D2 裁決：conversion 控制路由瀏覽器授權（T4 operator token（建議）｜T2 獨立 allowlist env｜T1 LAN 匿名（CSRF-only，速率限制唯一緩解）｜T4+T2）；速率限制 N（預設 10/分鐘）。驗證：`rg -n "D2 裁決" openspec/changes/unified-console-runtime-truth/tasks.md`
  - D2 裁決：T4（沿用既有 Kit mutation dev token 型 operator token；per-route wrapper；速率限制 N=10/分鐘），2026-08-25 owner 口令。
- [x] 0.3 D3 裁決：canonical-linux 是否設 `ENABLE_DEV_ROUTES=false`（compose 透傳＋`.env.example` parity）；若否，R5 授權主張降級並於 proposal Risks 揭露。驗證：`rg -n "D3 裁決" openspec/changes/unified-console-runtime-truth/tasks.md`
  - D3 裁決：是——canonical-linux 設 `ENABLE_DEV_ROUTES=false`（compose 透傳＋`.env.example` parity），2026-08-25 owner 口令。
- [x] 0.4 D4 裁決：`align-frontend-design-system-reference` 是否需先 thaw，或本 change 適用 `console-design-token-authority:60-64` carve-out。驗證：`rg -n "D4 裁決" openspec/changes/unified-console-runtime-truth/tasks.md`
  - D4 裁決：適用 `console-design-token-authority:60-64` carve-out，不 thaw `align-frontend-design-system-reference`，2026-08-25 owner 口令。
- [x] 0.5 canon 側欄是否新增「完整工具」入口；若是，依 R-A1 開提案 PR（不自行編輯 docs/plans）。驗證：本 change 實作 PR 內 `git diff --name-only origin/main -- docs/plans` 為空
  - 裁決：不新增側欄「完整工具」入口。owner 2026-08-25：「完整工具只是保留之前的功能，但真實操作頁面已經收斂到新需求上」——即「完整工具 ↗」僅作舊功能保留；本 change 的 unified 頁本身即真實操作面（R3 以 `api` 為首選、`nav` 為過渡）；canon 不改、無 R-A1 提案。
- [x] 0.6 R6 放置位置：確認 `workspace.a4.default` 捕捉路由是否等於 `#a4`；若等於，owner 裁決放置方式。驗證：`rg -n "workspace.a4.default" docs/plans/design-system-reference.manifest.json web-viewer-sample/e2e`
  - 裁決：同意預設方案——A4 頁首說明只在非 pinned 路由的 unified `#a4` 頁首渲染，`workspace.a4.default` pinned digest 不變，2026-08-25 owner 口令。

## 1. web-viewer-sample：真值綁定（R1、R2）

- [ ] 1.1 impact 分析：`gitnexus impact UnifiedShell -d upstream -r AI-BIM-governance`；同樣對 `HomePage`、`PipelinePage`、`OpsPage`、`coordinatorClient` 執行並記錄 blast radius
  - 本機完成（slice 1）：五個 UI symbol（`UnifiedShell`／`HomePage`／`PipelinePage`／`OpsPage`／`coordinatorClient`）LOW；`jsonGet` **HIGH**（16 個同檔 caller、0 process），coordinator 2026-08-25 追認續行（spec-to-done：HIGH 非停下點），補強＝error message 逐字不變＋既有 client 測試全綠；PR body 揭露。
- [x] 1.2 端點欄位 shape 盤點（十個端點皆已存在：`app.ts:1363,2374,2399,2462,3215,3779,3785`、`routes/governanceProxy.ts:223`）：逐一記錄欄位與型別對映；缺欄位者畫面標 `data-state="unavailable"`，不新增端點。驗證：盤點表附於 PR 描述；`rg -n "runtime/status|external/ifc-ready|conversion/records|callback-outbox/summary|governance/issues|governance/rule-runs|minio-watch/status|minio/objects|kit/health|kit/instances/current" bim-review-coordinator/src`
  - 本機完成：盤點表附於 PR body（十端點皆存在，無新增），待 181 隨 slice 1 勾選（commit `57d29d3`）
  - **181 canonical-linux 收斂（evidence-only closeout, 2026-08-31, evidenceHead `d04de191ec48d4e34c6744f9201d5e37a4f11b6c`）**：十端點皆於部署後 `:8004` 實測回應 200（`/api/minio/objects` 除外之九端點同分鐘批次；`/api/minio/objects` 因即時列舉 1745 物件需較長 timeout，緊接著同一輪次抓取），欄位 shape 與盤點表相符。去識別化後 JSON 存於 `docs/evidence/unified-console-runtime-truth-s3-closeout/task-6.2-endpoint-*.json`＋`task-6.2-minio-objects-summary.json`；截圖對照見 `task-6.2-screenshot-vs-json-correlation.md`；擷取時間軸見 `task-6.2-capture-log.txt`。
- [x] 1.3 共用 poller store `useCoordinatorStatusStore`（單一 in-flight／指數退避 ≤60s／hidden 暫停），沿用 `coordinatorClient`，以 `ConsoleDataProvider` 介面注入。驗證：`npx vitest run src/console/unified/coordinatorStatusStore.test.ts`；`npx tsc --noEmit`
  - 本機綠，待 181（slice 1，commit `57d29d3`）
  - **181 canonical-linux 收斂（2026-08-31）**：三輪獨立同批次擷取（~11:41／~11:42／~11:49 UTC）皆回傳一致的即時數值（非症狀描述中的舊 fixture 定值 1/1/12/2），證明 `#home`／`#pipeline`／`#runtime` 確實經共用 poller 打真端點而非顯示 fixture；見 `task-6.2-capture-log.txt`、`task-6.2-screenshot-vs-json-correlation.md`。
- [x] 1.4 `#home` 四 KPI＋六 svc-dot 綁定；`data-prov="asbuilt"`＋`data-state`；offline 顯示 `—`／未連線。驗證：`npx vitest run src/console/unified/homeLiveBinding.test.tsx`
  - **owner 裁決 Q3a（2026-09-01）：`fixture`／`live`／`redirect` 屬容器（非 badge）prov 值，明示 carve-out 允許——F-C 駁回據此解除（badge 面七值 enum 不變，容器值另立白名單）。KPI／svc-dot 綁定證據原本成立，重勾。**
  - **r2 獨立 verifier 駁回（F-C，2026-09-01 fail-closed 改回未勾）：181 DOM 實測 `{asbuilt:13, demo:1, fixture:1}` 中的 `fixture` **不在** canonical 七值 enum `asbuilt|artifact|demo|p1|p15|p3|p4` 內，下方 181 收斂註記先前誤稱「canonical 七值內」（本行更正該敘述）。KPI／svc-dot 綁定證據本身成立；待 owner 裁決：更正後重勾，或宣告 fixture 容器值為 non-badge carve-out。**
  - 本機綠，待 181（slice 1，commit `57d29d3`）
  - 揭露（P5 c1）：`#home` 應用啟動器容器（`HomePage.tsx`）原標 `data-prov="fixture"`（非 canonical 七值），本切片改為 `demo`（設計原型示範內容）；A1–A4 逐 badge 的 prov／`LIVE` 文字屬 §2.3 切片。
  - **181 canonical-linux 收斂（2026-08-31）**：`task-6.2-screen-home.png` 實測四 KPI（轉檔中 0／ready 12・failed 0、現有 SESSIONS 0、未結 ISSUE 0、OUTBOX 待送 36・attempts 0/5）與同批次 JSON 逐項相符（見 correlation table）；六 svc-dot（bim-review-coordinator／governance-service／conversion authority／Kit signaling·WebRTC／kit-manager-api／MinIO watch）皆渲染。DOM 查詢確認 `#home` 上 `[data-prov]` 值集合為 `{asbuilt:13, demo:1, fixture:1}`（更正：`fixture` 不在 canonical 七值內，見上方 F-C 駁回註記；無 `LIVE`），見 `task-2.1-3.2-canonical-dom-audit.md`。
- [x] 1.5 `#pipeline` 五段＋治理／報表列綁定；RVT 段退役標示；outbox 只用 `/api/callback-outbox/summary`。驗證：`npx vitest run src/console/unified/pipelineLiveBinding.test.tsx`
  - 本機綠，待 181（slice 1，commit `57d29d3`；has_source_ifc 逐物件觸發列表隨 D2 授權於 §4.2／§2.4 落地，本 slice 為 disabled＋原因）
  - **181 canonical-linux 收斂（2026-08-31）**：`task-6.2-screen-pipeline.png` 實測五段（件 ifc-ready 0、資料夾 7／含 source IFC 3、轉檔記錄 12 ready・0 running・0 failed、REVIEW SESSIONS 0、CALLBACK OUTBOX 36）＋治理／報表列（rule-runs 0、未結 issue 0）與同批次 JSON 逐項相符，「7／3」直接由當輪 `/api/minio/objects` 1745 筆物件依頂層資料夾聚合算出，見 correlation table 與 `task-6.2-minio-objects-summary.json`。
- [x] 1.6 `#runtime` 真值 OpsPage（Kit instance／GPU 未取得／服務健康／事件誠實停用）。驗證：`npx vitest run src/console/unified/opsLiveBinding.test.tsx`
  - 本機綠，待 181（slice 1，commit `57d29d3`）
  - **181 canonical-linux 收斂（2026-08-31）**：`task-6.2-screen-runtime.png` 實測 Kit Instance（`kit_local_001`／idle／control not_sent／opened 0）與同批次 `/api/kit/instances/current` 逐字相符；GPU Fleet 顯示「未取得」（非捏造數值）；服務健康六項渲染；structLog 顯示「事件流未提供」誠實停用文案，DOM 確認 `data-action="disabled"`＋`data-prov="p1"`，見 `task-2.1-3.2-canonical-dom-audit.md`。
- [x] 1.7 頂列 GPU chip 綁定，移除字面 `82%`（`UnifiedShell.tsx:143`）。驗證：`npx vitest run src/console/unified/topbarGpuChip.test.tsx`；`rg -n "82%" web-viewer-sample/src/console/unified` 為空
  - 本機綠，待 181（slice 1）：production 檔（排除 `*.test.*`）`rg -n "82%" web-viewer-sample/src/console/unified` 為空；三個測試檔含該字面僅作負向 oracle（斷言渲染輸出不含 `82%`）。
  - **181 canonical-linux 收斂（2026-08-31）**：對 `#runtime`（亦逐頁通用，頂列固定顯示）即時 DOM 查詢 `document.body.innerText.includes('82%')` → `false`；頂列 GPU chip 文字為「GPU 未取得」（誠實未取得狀態，非捏造百分比），見 `task-2.1-3.2-canonical-dom-audit.md`。
- [x] 1.8 假資料 export 退出 production 顯示路徑（`initialIntake`／`initialConv`／`initialSessions`／`initialOutbox`／`initialIssues`／`alerts`／`services`／`failDefs`／`diffDefs`／`fedMembers`／`stageTree`；D1=P 移到 test-only、D1=H 只由 preview provider 載入）；i18n／導覽／style helper 保留。驗證：`npx vitest run src/console/unified/fixtureNotInProduction.test.ts`；`npm run build:ui` 後 `rg -c "GPU/Stream 82%" dist-ui` 為 0
  - **owner 裁決 Q2b（2026-09-01）：`#a1` inline 捏造 telemetry（`WorkspacePage.tsx:166,205,208`、`fixtures.ts:115`、`docks.tsx:92,218`，另含 P5 FG 補列：`#a3` 示意圖素材像素內烙印的捏造串流 chip「Streaming ⚡ 28 ms」——design-assets vp 靜態圖素材需重製；P5 CR2 補列：`#a3` 另兩枚無條件捏造合規 chip「✓ Coordinate Check OK」「✓ 單位 m · CRS 一致」（`docks.tsx:210-211` 靜態 JSX）；更正：原列 `docks.tsx:218` 位於 a3Built 條件分支、capture 時 a3Built=false 未渲染）明列 **slice debt**，由 §2／§3 承接切片連同 `fixtureNotInProduction.test.ts` FORBIDDEN 清單強化一併處理；本 task 憑完整揭露重勾（six-export 範圍宣稱已於 F3 修正收斂）。**
  - **r2 獨立 verifier 駁回（F-B，2026-09-01 fail-closed 改回未勾）：`task-6.2-screen-a1.png` 顯示 `#a1` 仍渲染 SLICE2_DEBT 之外的 inline 寫死捏造值（`WorkspacePage.tsx:166` 「12.48M tris · 1.17 GB」、`:205` openedStageResult ✓、`:208` lease: editor · spectator ×1、`fixtures.ts:115`、`docks.tsx:92,218`），而同分鐘 JSON `sessions.count=0`。待 owner 裁決：把這些字串納入 `fixtureNotInProduction.test.ts` FORBIDDEN 清單並修 production（§2/§3 切片），或明列為 slice debt 後重勾。**
  - 本機綠，待 181（slice 1，commit `57d29d3`；6 個 export（`initialIntake`／`initialConv`／`initialSessions`／`initialOutbox`／`alerts`／`services`）已移 test-only；`initialIssues` 留在 production 供 Issues/BCF dock（§2）種入（P3 f1：移走會讓 workspace.a3 failure semantic case 空掉），`failDefs`／`diffDefs`／`fedMembers`／`stageTree` 由 docks／WorkspacePage 續用，以 fixtureNotInProduction.test.ts ratchet 釘住，§2／§3 切片承接）
  - **181 canonical-linux 收斂（2026-08-31）**：canonical 部署頁實測顯示真值（轉檔中 0／ready 12／failed 0／現有 SESSIONS 0／未結 ISSUE 0／OUTBOX 36），與症狀描述所載舊 fixture 定值（轉檔中 1／Sessions 1／Issue 12／Outbox 2，日期 2026-07-14）完全不同。
  - **範圍修正（round-2 verification F3，2026-08-31，evidenceHead `d04de191ec48d4e34c6744f9201d5e37a4f11b6c`）**：上一則的用語過寬。實測只證明本 task 明確列出的六個 export（`initialIntake`／`initialConv`／`initialSessions`／`initialOutbox`／`alerts`／`services`）已退出 `#home`／`#pipeline`／`#runtime` 這三個 canonical 渲染路徑，不是「假資料 export 確實已退出此部署的 production 顯示路徑」這句無限定敘述。`fixtureNotInProduction.test.ts` 自身宣告的 `SLICE2_DEBT` 明確保留 `failDefs`／`diffDefs`／`fedMembers`（`docks.tsx`）、`stageTree`（`WorkspacePage.tsx`）、`initialIssues`（`UnifiedShell.tsx`）留在 production；本輪 `task-6.2-screen-a1.png` 實測 `#a1` 的 RULE-RUN 面板仍渲染 fixture `failDefs`（顯示「RULE-RUN #88 · 失敗 18 / 嚴重 18 / 高 32」＋四筆具名 finding：FD-4F-02 防火門／W-2F-15 牆／P-2F-M32 管線／D-2F-09 風管），而同批次 `task-6.2-endpoint-governance-rule-runs.json` 顯示 `total=0`——證明 `failDefs` 這個 SLICE2_DEBT carve-out 確實仍在渲染假資料，非本 task 已消除的範圍。此修正只收斂上一則新增用語的過寬幅度，不變更 slice-1（line 44）已揭露的 carve-out 範圍，該範圍續由 §2／§3 切片承接。

## 2. web-viewer-sample：控制項與 badge（R3）

- [x] 2.1 全部控制項標 `data-action` ∈ {api, nav, disabled}；`disabled` 附合法 `data-prov`（七值）＋`aria-describedby` 原因。驗證：`npx vitest run src/console/unified/buttonInventory.test.tsx`
  - **owner 裁決 Q3a（2026-09-01）：prov 容器值 carve-out 解除 enum 爭點；`#a1` 一處 disabled 元素 prov/aria 未入 audit 實錄與 `#a4` 零 `[data-action]`（A4 查詢控制項在盤點面外）均以揭露列 known gap（A4 承接於其後續切片）。重勾。**PR #734 review T2 補列（2026-09-01）：另有多處 `onClick` 互動 div 未標 `data-action`（`UnifiedShell.tsx:155,179` logo 導航／語言切換、`WorkspacePage.tsx:156` member 列、`docks.tsx:98,124,172` rule 開關／finding 列／diff 列），`buttonInventory` 的 `checkControlInventory()` 只驗 `if (action)` 內元素故未攔截——與上列 known gap 同族，入 §2 slice debt（補標 `data-action`＋測試改為掃 `onClick`/`cursor:pointer` 全集），本 task 維持 owner 揭露＋入帳處置模式重勾，owner 可否決。****
  - **r2 獨立 verifier 駁回（2026-09-01 fail-closed 改回未勾）：(a) 下方註記「每一個 disabled 元素皆帶 data-prov（七值內）」超出 audit 實錄——`task-2.1-3.2-canonical-dom-audit.md:40` 對 `#a1` 的 disabled:1 未記錄該元素的 data-prov 與 aria 文字；(b) `#a4` 零 `[data-action]` 元素，A4 查詢控制項整批在盤點面之外。待補齊 audit 或 owner 裁決範圍後重勾。**
  - 本機綠，待 181（新增 `buttonInventory.test.tsx` 6/6 tests 全通）
  - **181 canonical-linux 收斂（2026-08-31）**：即時 DOM 查詢 `#home`／`#pipeline`／`#a1`／`#a2`／`#a3`／`#runtime` 六屏的 `[data-action]` 元素，值集合僅見 `{api, nav, disabled}` 三種（無其他值）；每一個 `disabled` 元素皆帶 `data-prov` 與可解析的 `aria-describedby`（更正：`#a1` 一處 disabled 元素的 prov/aria 未入 audit 實錄，「七值內」宣稱超出實錄，見上方駁回註記）（實測文字見下列各屏），無缺欄位案例，見 `task-2.1-3.2-canonical-dom-audit.md`。
- [x] 2.2 A1–A3 dock 導向真頁（`#a1-workbench`／`#version-diff`／`#federation`）；移除 `docks.tsx:168,191,223,229` 的 local-state 假成功 toast。驗證：`rg -n "u.toast" web-viewer-sample/src/console/unified/docks.tsx` 逐項對應到 fetch 呼叫或已刪除；`npx vitest run src/console/unified/buttonInventory.test.tsx`
  - 本機綠，待 181（已移除全部假 toast，導航至真頁）
  - **181 canonical-linux 收斂（2026-08-31）**：`#a2`「計算差異」與 `#a3`「Build Federated USD」即時 DOM 查詢皆為 `data-action="nav"`，頁面內無 toast 元素（`[data-testid*="toast"]`／`.toast`／`[role="status"]` 皆無匹配），見 `task-2.1-3.2-canonical-dom-audit.md`。
- [x] 2.3 側欄與啟動器 badge 依 `data.ts` `A1A10.prov`，移除寫死 `LIVE`（`fixtures.ts:156-165`）；A5–A10 控制項 `disabled` 附承接 change 名稱與 `p3`／`p4`。驗證：`npx vitest run src/console/unified/dockBadgeProv.test.tsx`；`rg -n '"LIVE"' web-viewer-sample/src/console/unified` 為空
  - **owner 裁決 Q3a（2026-09-01）：同 1.4 carve-out——`fixture`（容器 production 9 處；含測試檔共 14——P5 FC 更正）／`live`（`docks.tsx:66`）／`redirect`（`docks.tsx:229`）為容器值白名單；badge 面（本 task 標的）無寫死 `LIVE` 之證據不變。重勾。**
  - **r2 獨立 verifier 駁回（F-C，2026-09-01 fail-closed 改回未勾）：下方註記「canonical 七值內」同 1.4 為誤稱（`fixture` 不在 enum）；另有未揭露的 `data-prov="redirect"`（`docks.tsx:229`，a4-dock 容器）與 production 內 9 處 `data-prov="fixture"`（P5 FC 更正：原記 14 處含 5 處 `*.test.*` 檔，依本檔「production 檔（排除 `*.test.*`）」慣例為 9）。待 owner 裁決 fixture/live/redirect 是否為可接受的 non-badge 容器值，或更正後重新 DOM audit 再勾。**
  - 本機綠，待 181（新增 `dockBadgeProv.test.tsx` 3/3 tests 全通；`LIVE` 字串歸零）
  - **181 canonical-linux 收斂（2026-08-31）**：`#home` 即時 DOM `[data-prov]` 值集合為 `{asbuilt:13, demo:1, fixture:1}`——更正：`fixture` 不在 canonical 七值內（見上方 F-C 駁回註記）；無 `LIVE` badge 值。誠實揭露一處表面命中：`#a1` 頁存在字面子字串 `LIVE`，出自 `A1DockLive.tsx:89` 的「LIVE 後端 · 治理檢核」文案（誠實標示真後端連線，語意與本 task 要移除的「不論真假一律寫死 LIVE」badge 相反，非 `fixtures.ts:156-165` 目標範圍），詳見 `task-2.1-3.2-canonical-dom-audit.md` 的完整揭露段落。
- [x] 2.4 導向後仍受 IP 守門的動作（`#conv` prioritize／retry、`#minio` 觸發）在 D2 落地前於 UnifiedConsole 側 `disabled` 附「需 allowlist 來源」。驗證：`npx vitest run src/console/unified/buttonInventory.test.tsx`
  - 本機綠，待 181（`buttonInventory.test.tsx` 通過）
  - **181 canonical-linux 收斂（2026-08-31）**：`#pipeline`「觸發轉檔」即時 DOM 查詢為 `data-action="disabled"`、`data-prov="p1"`，`aria-describedby` 解析文字逐字為「需 allowlist 來源：瀏覽器授權（D2＝T4 operator token，tasks §4.2）落地前停用；請至 #minio 由 allowlist 來源觸發。」，與本 task 要求文字相符，見 `task-2.1-3.2-canonical-dom-audit.md`。

## 3. 3D 工作區與 A4（R4、R6）

- [x] 3.1 A1 離線視區標籤「no-GPU 示意／示範圖」（`data-prov="demo"`），移除捏造串流指標；有 session 時 anchor `target=_blank` 指向 `/ui/open?session=<id>`；不自動 claim；無轉檔按鈕。驗證：`npx vitest run src/console/unified/a1OfflineViewport.test.tsx`
  - **owner 裁決 Q2b＋Q4a（2026-09-01）：捏造 telemetry 列 slice debt（同 1.8，含 P5 FG 補列的 `#a3` 素材烙印 chip）；anchor 存在性斷言（`expect(handoffLink).toBeTruthy()`）由 Lane F 小任務補強（已登記）；「有 session→anchor」子句以 zero-positive-evidence 列 known gap（canonical sessions.count=0 分支未觸發）。憑揭露重勾。**
  - **r2 獨立 verifier 駁回（F-B/F-D，2026-09-01 fail-closed 改回未勾）：(a) 「有 session → anchor `/ui/open?session=<id>`」子句零正向證據——canonical `sessions.count=0` 該分支從未渲染，且 `a1OfflineViewport.test.tsx:77-82` 將斷言包在 `if (handoffLink)` 內、無存在性斷言，測試空洞通過（Lane F 補 `expect(handoffLink).toBeTruthy()` 可修）；(b) 「移除捏造串流指標」的驗證 regex 只掃 FPS/ms，未涵蓋 F-B 所列 inline 寫死值。**
  - 本機綠，待 181（新增 `a1OfflineViewport.test.tsx` 3/3 tests 全通）
  - **181 canonical-linux 收斂（2026-08-31）**：`#a1` 即時 DOM 文字逐字含「no-GPU 示意／示範圖（非即時渲染）」；`\d+\s*ms\s*[·,]\s*\d+\s*FPS` 樣式的捏造串流指標於全頁文字中無匹配；同批次 `runtime/status` 顯示 `sessions.count=0`，故無 session 時正確不渲染 `a[href*="/ui/open"]` handoff anchor（無 session 分支未被觸發，非缺陷）；見 `task-2.1-3.2-canonical-dom-audit.md`。
- [x] 3.2 A4 頁首說明（依 0.6 裁決位置）：用途／輸入來源／空表原因（取自 `/api/external/ifc-ready` 與 `/api/governance/search/llm-status`）／下一步。驗證：`npx vitest run src/console/unified/a4Header.test.tsx`；`pwsh scripts/tests/verify-design-system-reference.ps1 -VerifyOrigin`（a4 pinned digest 不變）
  - 本機綠，待 181（新增 `a4Header.test.tsx` 1/1 test 通過，design gate 26 golden 全數 MATCH）
  - **181 canonical-linux 收斂（2026-08-31）**：`task-6.2-screen-a4.png`＋即時頁面文字證實 A4 頁首含用途（語意查詢）、輸入來源（ifc-ready／llm-status 相關文字）、空表原因（`disabled`／`unconfigured` 等狀態說明）；design gate pinned digest 不受本次 evidence-only closeout 影響（未改動任何 production/design 檔），沿用既有 `-VerifyOrigin` 通過紀錄（`design-system-reference-verify-origin.txt`：13 screens／26 golden files）。

## 4. bim-review-coordinator（R5、D3）

- [x] 4.1 impact：`gitnexus impact rejectIfIpNotAllowed -d upstream -r AI-BIM-governance`；列出全部呼叫點（`app.ts:1616,1642,1678,1728`、`lineageSourceBundleRoutes.ts:523,573`）並確認本 change 不改 helper 本體
  - **impact 紀錄調和（coordinator 2026-08-26，P5 h23）**：下列「LOW／impactedCount 2／direct 1」來源為未 tracked 的早期索引（推定為改動前樹；range 內無該索引 artifact，不宣稱 base 索引）；唯一 tracked 紀錄為 verification.txt 的 subject 樹輸出；本 PR head 的索引（`docs/evidence/unified-console-runtime-truth-s2a/verification.txt`）對同一 symbol 回 `impactedCount 0`——因四個直接呼叫點改經 `createConversionControlGuard` deps、helper 仍以 closure 注入 lineage deps（app.ts `registerLineageSourceBundleRoutes`），GitNexus 對巢狀 closure 的 upstream 解析為 0；兩份數字描述的是改動前後兩個樹，皆為 LOW，非矛盾。
  - **4.1 實測（2026-08-25，本 task 重跑確認）**：`gitnexus impact rejectIfIpNotAllowed -f bim-review-coordinator/src/app.ts -d upstream -r AI-BIM-governance` → `risk: "LOW"`、`impactedCount: 2`、`summary.direct: 1`（`createCoordinatorApp`）。`rg -n "rejectIfIpNotAllowed" bim-review-coordinator/src` 確認：函式本體（`app.ts:1608`）逐字未改；四條 conversion 控制路由（prioritize／retry／watch／trigger）已改呼叫新 guard `rejectIfConversionControlUnauthorized`（見 4.2），不再直接呼叫 `rejectIfIpNotAllowed`；唯一仍在用的呼叫點是經 `deps` 注入的 lineage 兩條路由（`lineageSourceBundleRoutes.ts:523,573`）與 `app.ts:4292` 的注入行本身。行為逐字不變由 `tests/lineage/conversion-control-auth-pins.test.ts`（5 tests，見 4.3）與本 task 的真 HTTP 探針共同證實。
- [x] 4.2 依 D2 以 per-route wrapper 為四條 conversion 控制路由實作授權（T4：header token 同型比對 `isKitMutationAuthorized`；T2：`CONVERSION_TRIGGER_IP_ALLOWLIST`＋fail-fast 守衛＋compose 透傳＋parity；T1：同 origin＋短效 token＋速率限制）；`/api/external/*` 不變。驗證：`.venv/Scripts/python -m pytest bim-review-coordinator/tests/test_conversion_control_auth.py -q`（含：無憑證且非 allowlist → 403；速率限制 → 429；`/api/external/ifc-ready` 回應逐字不變）
  - **PR 範圍揭露（coordinator 2026-08-25，P5 f7）**：§4 各子彈引用的 `web-viewer-sample/e2e/dev-routes-disabled-operator-token.spec.ts`（test 1／2／3）、branch coordinator :8005＋Playwright 瀏覽器證據，以及 `#demo-control`／`#a1-workbench` 前端變更，**不在後端 PR（分支 `codex/openspec/unified-console-runtime-truth-s2a`）內**；它們屬姊妹前端 PR（分支 `codex/openspec/unified-console-runtime-truth-s2`），本 PR 可獨立驗證的證據只有 `bim-review-coordinator` 的 vitest／build 與 `docs/evidence/unified-console-runtime-truth-s2a/verification.txt`。
  - **4.2 實測（2026-08-25）**：已依 D2=T4 裁決實作 `bim-review-coordinator/src/services/conversionControlAuthorization.ts`（`isOperatorTokenPathEnabled`／`SlidingWindowRateLimiter`／`createConversionControlGuard`）並接上四條 conversion 控制路由。本 repo 為 TypeScript／vitest 專案，上方「驗證：」欄位的 pytest 指令不適用；實際指令：`npx vitest run tests/services/conversionControlAuthorization.test.ts`（6 tests）與 `npx vitest run tests/conversion-control-auth.test.ts`（28 tests）皆綠，涵蓋無憑證 403、token 路徑未啟用時 fail-closed（預設 `dev-token` 不解鎖）、錯 token 403、正確 `x-operator-token`／相容 `x-dev-token` 授權通過、速率限制第 11 次 429＋`Retry-After`、allowlist 路徑不計速率、空 allowlist 全放行；`/api/external/ifc-ready` 授權不受影響（見 4.3）。本 task 另以真 process（branch coordinator :8005，`EXTERNAL_INTAKE_IP_ALLOWLIST=10.0.0.0/8`、ephemeral `DEV_AUTH_TOKEN`）＋真 HTTP 重驗四路由 token 路徑與速率限制，結果一致（`web-viewer-sample/e2e/dev-routes-disabled-operator-token.api.spec.ts`）。舊 r4 指令未帶 `E2E_REQUIRE_REAL=1`；r5 已由下方「r5 evidence 安全修補」的 ownership-gated compatibility runner 取代，並要求同 process 注入一致的 `E2E_DEV_AUTH_TOKEN`、API trace off。
- [x] 4.3 lineage 釘樁：`lineageSourceBundleRoutes.ts` 兩條路由（`legacy-unmanaged/preview`／`confirm`）授權行為變更前後逐字相同；prioritize／retry／watch 三條路由的既有 allowlist 行為釘樁。驗證：`.venv/Scripts/python -m pytest bim-review-coordinator/tests/lineage -q`；`bim-review-coordinator` 對應 vitest：`npx vitest run tests/lineage`
  - **4.3 實測（2026-08-25）**：`tests/lineage/conversion-control-auth-pins.test.ts`（5 tests）於 4.2 落地後全綠（release range 內無 baseline 執行 artifact，不宣稱 baseline 綠；「逐字不變」由 `rejectIfIpNotAllowed` 函式體在 base 與 subject 逐字相同、且 range diff 未觸及 `lineageSourceBundleRoutes.ts` 與 `/api/external/*` handler 支撐），證明 lineage `legacy-unmanaged/preview`／`confirm`（經 `deps.rejectIfIpNotAllowed` 注入）與 `/api/external/ifc-ready` webhook 面授權逐字不變、operator token 對兩者皆無解鎖效果。本 repo 無 python 測試樹對應 `bim-review-coordinator/tests/lineage`；上方「驗證：」欄位僅 `npx vitest run tests/lineage` 適用本 change（含此釘樁檔，全綠）。本 task 另以真 HTTP 對 branch coordinator :8005 重驗 lineage 兩路由「帶 operator token 仍 403、body 逐字相同」，結果一致（同上 e2e spec test 3）。
- [ ] 4.4 D3：canonical env `ENABLE_DEV_ROUTES=false`（compose `environment:` 透傳＋`.env.example` parity guard，沿用 PR #693 模式）；受影響 Edge Console 頁在 404 時誠實顯示「dev routes 已關閉」。驗證：`npx vitest run tests/env-example-parity`（或既有 parity 測試名）；`.venv/Scripts/python -m pytest bim-review-coordinator/tests/test_dev_routes_disabled.py -q`
  - **canonical 路徑補強（coordinator 2026-08-25，P5 c11／c12）**：dockerized coordinator 的 env 只來自 compose `environment:`，變數來源是 owner 私有 `.env.web-plane.host-kit`，其 missing-key merge 的 key source 是根目錄 `.env.web-plane.host-kit.example`（非 `bim-review-coordinator/.env.example`）。本 PR 已在 `.env.web-plane.host-kit.example` 宣告 `ENABLE_DEV_ROUTES=`（空）與 `DEV_AUTH_TOKEN=`，在 `.env.web-plane.host-kit.canonical-linux.example` 宣告 `ENABLE_DEV_ROUTES=false` 與 `DEV_AUTH_TOKEN=`（不放真值），parity 測試同時釘住兩個 key source 與 compose coordinator 區塊。**owner 部署前置（未做則 D3／T4 在 181 形同未落地）**：⚠ 部署順序（P5 h33）：`ENABLE_DEV_ROUTES=false` 應在前端姊妹 PR（`#demo-control`／`#a1-workbench` 的「dev routes 已關閉」誠實狀態）合併並部署到 181 **之後**再設，否則 `#demo-control` 期間會把 404 JSON 當空清單、顯示 `runtime: storage_empty` 假空狀態（不崩潰，但屬誤導性空狀態；main 現況 `RealIfcConsolePage.tsx` 不檢查 `r.ok`），A1 workbench 只是不標〔測試資料〕；⚠ 副作用（P5 h32）：`DEV_AUTH_TOKEN` 同時守 `POST /api/kit/instances/current/open|close`（`isKitMutationAuthorized`），輪替為非預設值後，仍以 `x-dev-token: dev-token` 呼叫 Kit open／close 的 operator 腳本會開始 403（web viewer 不送該 header，無 UI 影響）。私有 `.env.web-plane.host-kit` 設 `ENABLE_DEV_ROUTES=false` 且 `DEV_AUTH_TOKEN` 為非預設值；`compose.runtime-manager.yml` 以 `${DEV_AUTH_TOKEN:-dev-token}` 透傳，缺值時 token 路徑靜默關閉（只剩 allowlist）。驗收探針：自非 allowlist 來源帶錯誤 `x-operator-token` 應回 `operator token invalid`（證明 token 路徑啟用）。
  - **4.4 實測（2026-08-25，本 task 更新；取代先前「.env.example parity 仍紅」揭露，該狀況已解決）**：先前揭露的 `bim-review-coordinator/.env.example` 重複宣告鍵（L66 過期 `ENABLE_DEV_ROUTES=true`／PR #222 舊值 vs L75 owner 追加空值）已依 owner 指示移除（該修改在姊妹分支為 commit `2ce102e`，不在本 PR range；本 PR 的等價變更折在 `5a05927` 的 range diff：`-ENABLE_DEV_ROUTES=true`／`+ENABLE_DEV_ROUTES=`）過期行，只留空值宣告；`npx vitest run tests/env-example-dev-routes-parity.test.ts`（cwd `bim-review-coordinator`）現況隨 4.5 全量一併驗證為綠（見下）。prefix gate（`app.ts` `/api/dev` 整組 404）與 compose `environment: ENABLE_DEV_ROUTES: ${ENABLE_DEV_ROUTES:-}` 透傳落地不變。
  - 本 task 已於本機 branch coordinator（:8005，`ENABLE_DEV_ROUTES=false`）＋真瀏覽器（Playwright，viewer :5180）驗證受影響 Edge Console 兩頁（`#demo-control`／`#a1-workbench`）在 dev routes 404 時皆顯示誠實 note（非崩潰、非假資料）：`web-viewer-sample/e2e/dev-routes-disabled-operator-token.spec.ts` test 1／2（與 test 3 同一次執行，`cd web-viewer-sample && E2E_COORDINATOR_BASE_URL=http://127.0.0.1:8005 npx playwright test e2e/dev-routes-disabled-operator-token.spec.ts` → `3 passed`）。執行條件的偏離見下一則。
  - **前端半部 PR（slice 2 前端，2026-08-26）**：後端 PR #699 合併後，本分支併入 main（`ecc2dd9`）並以隔離 HEAD stack（host-native branch coordinator :8006、`ENABLE_DEV_ROUTES=false`、viewer :5180）重取 P4 瀏覽器證據：`docs/evidence/unified-console-runtime-truth-s2/summary.json`＋目前實際存在的兩張截圖（`unified-console-runtime-truth-s2-demo-control.png`、`unified-console-runtime-truth-s2-a1-workbench.png`）。先前列出的 `*-after-refresh.png`／`*-after-retry.png` 已不在 current evidence bundle，不得再當作可開啟 artifact。attempt 1 的 executor 指出 spec 檔頭誤稱 `ifc-runtime-state` 不存在、未綁定 404 body、未點 refresh；spec 已補強（綁定 `GET /api/dev/ifc-sources` → 404 `{detail:"dev routes disabled"}`、斷言 `runtime: dev_routes_disabled` 且非 `storage_empty`、點 refresh 後 notice／disabled 維持）並以後續 attempt 重取證據。canonical-linux 181 未部署本 bundle，UI task 仍待 §6 驗收；`#conv`／`#minio` 的 `/api/dev/conversions` 消費者（既有「未取得」誠實分支、`ConversionHistoryPanel`「請重試」措辭）列前端 follow-up（P5 r6）。
  - **r4 E2E compatibility 歷史（2026-08-25）**：branch-wide `playwright.config.ts` 在 `E2E_REQUIRE_REAL=1` 時要求 isolated-stack manifest，且當時的 isolated `testMatch` 不收本 spec；r4 因此以未帶該旗標的命令配合 spec 內 fail-not-skip `preflight()` 取得舊證據。r5 已由現行 plan Task 5A 與下方「r5 evidence 安全修補」取代：使用 narrow gitignored config 保留 `E2E_REQUIRE_REAL` reporter、現行 test IDs、ephemeral `E2E_DEV_AUTH_TOKEN`，並讓 API probe trace 固定關閉。r4 結果只作歷史，不得充當 r5 exact-head evidence。
  - **速率視窗可重複執行性修復（task#4 quality 修復，2026-08-25）**：上述「可重現指令」原本**無法安全重跑**。初版 spec test 3 假設 operator token 速率視窗是空的，寫死「錯 token 第 1 次、四路由第 2–5 次、補滿到第 10 次、第 11 次 429」這組絕對次數；但 `SlidingWindowRateLimiter`（`bim-review-coordinator/src/services/conversionControlAuthorization.ts:25-46`）只以來源 IP 為 key、四條 conversion 控制路由共用同一視窗、實例在 `app.ts:1633` 建立一次並活在 coordinator process 生命週期內且**無 reset hook**，而 branch coordinator 是長生命週期行程，不隨每次 playwright run 重啟。**實測重現**：同一條指令在 14 秒內重跑，test 3 斷在 `expect(wrongToken.status()).toBe(403)` 收到 `429`（`3 passed` → `1 failed／2 passed`，其間未改任何碼或 env）——對「重跑一次確認還是綠的」這個再自然不過的動作而言，是看起來像 regression 的假紅，正好會蛀掉本節其餘敘述所倚賴的可重現性保證。**修法（只動測試檔，未動任何生產碼）**：語意斷言（錯 token 403、四路由授權通過）改經 `withRateWindowRetry()` 送出——遇 429 依 `Retry-After` 等殘量離開視窗後重試，等待次數有上限，逾限**直接 fail 並說明原因**（不吞、不 skip，維持本檔「前置不齊備就大聲失敗」的一致設計）；速率限制本身改成「連打到 429 為止」（上限 `OPERATOR_TOKEN_RATE_LIMIT + 1` 次，視窗全空時必定收斂），不再斷言「剛好第 11 次」這個絕對次數。**修後實測連續三次執行**：`3 passed (6.2s)`（視窗乾淨、零額外等待）→ `3 passed (53.8s)` → `3 passed (49.5s)`（後兩次各自動等一次視窗到期，test 3 分別 47.6s／44.6s）；coordinator 不可達的負向態仍為 `3 failed`、0 skipped、訊息逐項指出缺哪一項前置。偏離理由與設計同步寫在該 spec 檔頭。
  - **r6 evidence 安全修補與 HELD（2026-08-27）**：`E2E_DEV_AUTH_TOKEN` 不再有 source fallback；缺值時 API preflight fail closed。API probe 已移到 `dev-routes-disabled-operator-token.api.spec.ts`，以獨立檔案／worker scope固定 `trace: off`、`screenshot: off`、`video: off`；原 UI spec 只含兩個 browser tests 並明確 `trace: on`；舊 API trace/report 已進 recoverable quarantine，不得發布。r6 attempt2 在 `subjectHead=8081c2a12308adcb2b04b566c87e168d7c77be9c` 記錄 3/3、UI trace 2、API trace 0、secret scan 235 files／3 secrets／0 hit；但 P5 security review 發現 cleanup 在 identity check 後仍用 PID stop，無法證明 exact handle-bound ownership，且 evidence attempts 已達 2/2，因此 r6 於 P4 HELD。`s2-r5.local/playwright.config.ts` 不提供 r6 的分離 API project/output、artifact cardinality、exact-head 與 cleanup gates，禁止作為 r6／successor 的重跑入口；現行 API spec 的 file-level `test.use` 仍會關閉 trace／screenshot／video。新 HEAD 必須建立經 review、綁定 immutable SHA 的 successor runner，並重新取得證據。任何 token 值不得寫入 tasks、trace、report 或 PR。
  - **checkbox 仍不打勾**：本行敘述涵蓋「canonical env `ENABLE_DEV_ROUTES=false`」——這是只能在 canonical-linux 實際部署後才能觀測的事實，本機模擬環境變數不等於 canonical 部署已套用該值；依本檔第 1 行勾選規則精神，打勾留待 §6 canonical-linux 部署驗收（6.1／6.4）一併確認後由該處收斂。
- [x] 4.5 coordinator 全量：`.venv/Scripts/python -m pytest bim-review-coordinator/tests -q` 與 `npx vitest run`（cwd `bim-review-coordinator`）
  - **4.5 實測（2026-08-25）**：coordinator 全量 `npm run build`（tsc，無輸出／exit 0）＋ `npx vitest run`（cwd `bim-review-coordinator`）＝**100 test files／1573 tests all passed**（P5 round-1 修補後重跑；唯一數字來源＝`docs/evidence/unified-console-runtime-truth-s2a/verification.txt`）（含 `tests/env-example-dev-routes-parity.test.ts`）。（姊妹分支 s2 的證據，本 PR 不含前端變更）前端受影響檔 `npx tsc --noEmit`（無輸出）＋ 指定 vitest 檔案清單（`RealIfcConsolePage`／`coordinatorClient.*`／`modelData`／`ConversionPage`／`console`／`A1IssueSnapshot`）＝**14 test files／212 tests all passed**（cwd `web-viewer-sample`）。上方「驗證：」欄位的 `.venv/Scripts/python -m pytest bim-review-coordinator/tests -q` 指令不適用（`bim-review-coordinator` 為純 TypeScript／vitest 專案，無 python 測試樹，本 change 全程沿用 plan 開頭宣告的 Tech Stack）；本 change 的實際全量以此 npm／npx 指令組為準。

## 5. design gate／rebaseline／semantic cases／既有測試（R8；HELD 直到 0.1 與 0.4）

- [ ] 5.1 更新 `src/console/EdgeConsole.sharedstatus.test.tsx:54-67`：改為「`#home` 經共用 poller 呼叫 runtimeStatus，同端點單一 in-flight」。驗證：`npx vitest run src/console/EdgeConsole.sharedstatus.test.tsx`
  - 本機綠，待 181（slice 1，commit `57d29d3`）
- [ ] 5.2 更新 `src/console/unified/a1DockLive.test.tsx:100-104`：liveBackend 時 fixture 區塊由真值取代（不再斷言 `A1_Tower_v12.ifc` 與 `data-prov="fixture"` 根）。驗證：`npx vitest run src/console/unified/a1DockLive.test.tsx`
  - 本機綠（解凍 fixture 斷言），待 181；「真值取代」正向斷言隨 §2.2／§3.1 落地（slice 1，commit `57d29d3`）
- [ ] 5.3 更新 `src/console/unified/unified.test.tsx`（KPI 標籤斷言改為 mock API 值與 offline 狀態）。驗證：`npx vitest run src/console/unified/unified.test.tsx`
  - 本機綠，待 181（slice 1，commit `57d29d3`）
- [ ] 5.4 更新 `web-viewer-sample/e2e/design-system-semantic-cases.ts`：`kpi-conv-val`／`kpi-outbox-val`／`svc-dot` 等 fixture 值斷言改為誠實狀態斷言；因 `implemented_case_ids`＝`required_case_ids` 且每屏執行（`design-system-visual.spec.ts:182-184,217`），以全屏規模評估；登記 bootstrap ledger（`verification_mechanism_paths` ⊆ changed paths）。驗證：依 `scripts/verification-manifest.json:423-436` 的 `design-semantic-visual` 項目執行
  - 本機：12/13 屏 semantic_parity=1（home／pipeline／ops／workspace.a1／a2／a4／concept.a5–a10 全綠；`workspace.a3.default:failure` 曾因 af88ecd 把 initialIssues 移到 test-only 而空掉（**是本 slice 造成，非 pre-existing**；P3 final review f1），於 d9b40de 修復後 13/13——本機 console 輸出未 tracked（`artifacts/*.txt` 受 .gitignore 排除，range 內不可查證），承重驗證為 CI required check `design-semantic-visual` 於 PR 上重跑；PR body concerns 為揭露管道）；pixel 三屏（home／pipeline／ops）預期紅（**57d29d3 時點、#698 之前**的敘述；#698 後三屏為 reference_missing 不進 pixel 比對，見 5.5 三段路線）；`web-viewer-sample/e2e/**` 非 `Get-SelfReferentialMechanismPaths` 機制面，bootstrap ledger 未登記（PR body `Self-referential bootstrap: no`），待 181（slice 1，commit `57d29d3`）
- [ ] 5.5 D1=P：owner 明示後執行 `node web-viewer-sample/scripts/capture-design-system-reference.mjs --rebaseline --confirm-rebaseline`（非 `canonical_product_surface` 屏重拍；`workspace.a4.default` digest 不變）。D1=H：實作 design-preview harness＋capture 機制變更＋production bundle 不可達測試＋bootstrap ledger。驗證：`pwsh scripts/tests/verify-design-system-reference.ps1 -VerifyOrigin`
  - **落地實況（coordinator 2026-08-25／26，owner 明示「rebaseline go」）**：雙旗標 capture 腳本只重拍設計原型（authoring origin 未變 → golden 不變），無法表達產品在 503 stub 下的 offline 態；依 `console-design-token-authority` 與 PR #429 先例，三屏改以 `design-system-visual.spec.ts` 的 actual 截圖升格為 `baseline_provenance.authority=canonical_product_surface`（manifest approval 記 owner 明示）。`pwsh scripts/tests/verify-design-system-reference.ps1 -VerifyOrigin` 於 4e73c10：`[design-reference-gate] passed — 13 screens, 26 golden files, source=2f414d9d4bd96adbd3102e417f2465eb4aa609b0c3a12d118f41c1ccff30c9c8`；`npm run test:visual:design-system` 乾淨樹 13/13 passed、三屏 diff 0。**PR 切分（owner 核准三段路線，2026-08-26）**：change-scope 政策（`reference_authority_mixed_fail_closed`）禁止產品與 golden 同 PR；reference-first 換 golden（PR #697，已關）又因 manifest／baseline 屬 `full_dispatch_globs` 而在 CI 以產品 offline golden 比對 main 的 fixture UI 而紅。故走三段：(1) 降級 PR **#698**（已合併）把三屏移出 manifest→`reference_missing`（10 屏／20 golden 不變；`-VerifyOrigin` 於 main：`passed — 10 screens, 20 golden files`）；(2) 本產品 PR（scope `mixed`，10 屏 pixel 比對；`#home`／`#pipeline`／`#runtime` 此刻**無** pixel golden，由 gate 誠實列為 reference_missing）；(3) 重新核准 PR：以合併後產品面的 actual 截圖升格三屏為 `canonical_product_surface`（待做，未完成前三屏 pixel 比對缺口持續存在）。上文「13 screens／13/13 屏」為 #698 之前在本分支（4e73c10）以升格 golden 所做的本機驗證，不是 main 現況；R-A2「快照只由 capture 腳本雙旗標寫入」的偏離（第二條路徑＝#429 型升格）屬既有治理欠帳（D-15），已揭露。
- [ ] 5.6 乾淨工作樹跑兩道 required gate（`design-semantic-visual`、`functional-runtime-conv`）；PR body `Design gate status` 逐字＝機器算出的 `mixed`。驗證：`git status --porcelain=v1 --untracked-files=all` 為空；gate 命令依 `scripts/verification-manifest.json:423-450`
  - 本機（4e73c10，#698 之前）：design-semantic-visual 13/13 屏 semantic_parity=1（a3 迴歸已於 d9b40de 修復）；pixel 三屏（home／pipeline／runtime.ops）以升格 golden 比對 diff 0。**#698 之後**：本分支併入 main 後 gate 以 10 屏比對（三屏 reference_missing），結果見 PR body `Design gate status`；functional-runtime-conv passed（FUNCTIONAL_COORDINATOR_PORT=18005 避開被占用之預設 8005，未動該既有程序）；結果見 PR body（slice 1，commit `57d29d3`）
- [ ] 5.7 前端全量：`npx tsc --noEmit && npx vitest run`（cwd `web-viewer-sample`）
  - 本機綠，待 181（slice 1，commit `57d29d3`；tsc exit 0、vitest 89 files/1175 tests passed、lint:baseline 0 regressions）

## 6. canonical-linux 部署驗收（R7）

- [x] 6.1 merge 後 owner inventory 執行 `pwsh scripts/dev/rebuild-test-deploy.ps1`；確認 `deploy-*` tag 指向本 change squash SHA。驗證：`git tag -l 'deploy-*' --sort=-creatordate | head -1` 的 tag 訊息 `deployed=` 與 squash SHA 一致
  - 6.1 實測（evidence-only closeout，2026-08-31）：`git tag -l 'deploy-*' --sort=-creatordate | head -1` → `deploy-20260831-639237709604722760-001`；tag 訊息 `deploy target=canonical-linux exit=0 snapshot=20260831T110240Z-effective-env.json deployed=a0ab7065131914e548e1d79a1c683c8b14b07de4`，與本輪 evidenceHead `a0ab7065131914e548e1d79a1c683c8b14b07de4`（= 本 change 目前 HEAD／`main` 併入後最新一次 canonical-linux 部署所指之 commit）逐字相符。證據：`docs/evidence/unified-console-runtime-truth-s3-closeout/task-6.1-deploy-tag-match.txt`。
- [x] 6.2 canonical-linux 證據包：`/ui`（無 hash）、`#home`、`#pipeline`、`#runtime`、`#a1`、`#a2`、`#a3`、`#a4` 截圖＋同分鐘十端點 JSON；去識別化後入檔（png 需 `git add -f`）或於 PR body 放摘要＋digest。驗證：每張截圖數值 vs JSON 逐項對照表
  - **6.2 實測（evidence-only closeout，2026-08-31，evidenceHead `d04de191ec48d4e34c6744f9201d5e37a4f11b6c`）**：canonical-linux 於本輪對 `http://<canonical-host>:8004` 直接以 node `fetch` 與 `npx playwright screenshot`（`page.screenshot`，不含網址列）實測可達（前一輪誤判「本 host 無 181 存取」已由本輪推翻；佔位符對照見 `task-6.2-6.5-deidentification-map.json`）。八屏截圖（`/ui` 無 hash、`#home`、`#pipeline`、`#runtime`、`#a1`、`#a2`、`#a3`、`#a4`，1440x900，2026-08-31T11:48:36Z–11:49:03Z）已以 `git add -f` 入檔為 `docs/evidence/unified-console-runtime-truth-s3-closeout/task-6.2-screen-*.png`。同分鐘十端點 JSON（九端點於 2026-08-31T11:49:17.302Z–11:49:17.370Z 一批擷取，`/api/minio/objects` 因即時列舉 1745 筆物件需較長 timeout，於 11:49:17Z 啟動、11:49:26Z 完成，緊接同一輪次）去識別化後存為 `task-6.2-endpoint-*.json`＋`task-6.2-minio-objects-summary.json`（bucket 真實 key／LAN IP／主機名均以 `<canonical-*>` 佔位（P5 FH 更正 2026-09-01：digest 對照已依 owner Q1a 剝除，現為純佔位符 legend），見 `task-6.2-6.5-deidentification-map.json`；三段獨立擷取（~11:41／~11:42／~11:49 UTC）數值完全一致，證明系統於整段擷取期間閒置、無漂移）。逐項對照表：`task-6.2-screenshot-vs-json-correlation.md`；補充 DOM 層級對照（`data-action`／`data-prov`／`aria-describedby`，供 §2／§3 task 佐證）：`task-2.1-3.2-canonical-dom-audit.md`；完整擷取時間軸：`task-6.2-capture-log.txt`。
- [ ] 6.3 LAN 瀏覽器依 D2 授權觸發轉檔實測：對 `has_source_ifc` 物件觸發 → 非 403 → job lineage 可見；再次觸發顯示冪等回應。驗證：截圖＋`/api/conversion/records` JSON
- [ ] 6.4 負向：無憑證且非 allowlist IP 的 `POST /api/conversion/trigger` 回 403；`/api/external/ifc-ready` 授權未變；D3 落地時 `POST /api/dev/conversions` 回 404。驗證：`curl -i` 三組請求輸出（去識別化）
- [x] 6.5 證據隱私：檔名／JSON 不含 LAN IP、主機名、bucket 真實 key，並通過 secret-pattern-scan。驗證：依 `scripts/verification-manifest.json:452` 執行
  - **owner 裁決 Q1a（2026-09-01）：採 digest 全剝除方案（已套用於 `task-6.2-6.5-deidentification-map.json` 與 `task-6.2-minio-objects-summary.json`）；剝除後重跑 `scan-secret-patterns.ps1` passed（`task-6.5-secret-pattern-scan-post-strip.txt`）且十組 digest 全 bundle grep 零命中。重勾。另 owner 裁決 Q5a：截圖與 JSON 的 3–5 分鐘窗以三次獨立 pass 值穩定論證視為滿足「同分鐘」意旨。**
  - **r2 獨立 verifier 駁回（F-A，2026-09-01 fail-closed 改回未勾）：de-identification map 的未加鹽截斷 sha256 digest 被 verifier 以 775 個候選一次反解出真實 LAN IP／bucket key／內部 docker 主機名（`task-6.2-minio-objects-summary.json` 亦內嵌同組 digest）。coordinator 已於 2026-09-01 依最保守選項將兩檔 digest 全數剝除（僅留佔位符），owner 可另選 repo 外加鹽方案；剝除後需重新 secret-scan＋verifier 覆核方可重勾。**
  - **6.5 實測（evidence-only closeout，2026-08-31）**：`pwsh scripts/tests/scan-secret-patterns.ps1`（`scripts/verification-manifest.json` 的 `secret-pattern-scan` 項目對應指令）→ `[scan-secret-patterns] passed`，輸出存於 `docs/evidence/unified-console-runtime-truth-s3-closeout/task-6.5-secret-pattern-scan.txt`。補充人工隱私 grep（該 ps1 的正則只涵蓋 AWS/GitHub/Slack 憑證樣式，不涵蓋 LAN IP／bucket key，故另跑）：對本輪新增全部檔名＋JSON 內容＋PNG 二進位逐一 grep 真實 LAN IP／真實 bucket key／已知真實專案名稱候選清單（候選值不逐字寫入本檔；佔位符 legend 見 `task-6.2-6.5-deidentification-map.json`——P5 FH2 更正 2026-09-01：原述之 digest 對照已依 Q1a 剝除），零命中；輸出存於 `task-6.5-manual-privacy-grep.txt`。**round-2 自我發現修正（2026-08-31）**：本節與上方 6.2 節先前版本曾在此 tasks.md 的敘述文字中直接寫出真實 LAN IP、真實 bucket key 與四個真實客戶專案名稱本身（即便逐一 grep 這些字面值於「其他」evidence 檔零命中，這句描述本身就是把它們寫進了本檔——tasks.md 屬 openspec/changes 路徑，同樣入公開 repo，且未被上述 grep 掃描到，因為 grep 目標是新增的 evidence 檔而非同時被編輯的 tasks.md 本身）；已於本輪全部改為佔位符／間接描述，不再於本檔重複真值，符合 repo「真實生產 metadata 永不入檔」既有邊界。去識別化對照表（P5 FH 更正 2026-09-01：原「佔位符 → sha256 前 16 碼 digest」設計已依 owner Q1a 剝除 digest，現為純佔位符 legend）：`task-6.2-6.5-deidentification-map.json`；**揭露（round-2 verification F4）**：該 digest 未加鹽，對低熵值（LAN IP／短專案名）可被持有候選清單者逐一雜湊比對回推真值，非真正不可逆指紋——此事項已由 owner 於 2026-09-01 裁決 Q1a＝整份剝除並已套用（P5 FH2 更正：本句其餘部分為 08-31 歷史紀錄，非現況），本輪只修正 note 措辭不再宣稱不可逆，未變更本 task 6.5 逐字驗收標準（檔名／JSON 不含真值、通過 secret-pattern-scan）的達成狀態，因該標準本身已達成且與此揭露無關。`/api/minio/objects` 完整 1745 筆物件清單刻意不逐字入檔（含真實客戶／專案資料夾名稱），改以聚合摘要（`task-6.2-minio-objects-summary.json`）承載可驗證的計數，符合本 repo「真實生產 metadata 永不入檔」既有邊界。

## 7. 文件與 closeout

- [ ] 7.1 更新 `docs/agents/repo-data-flow-and-ownership.md`（UI→coordinator 共用 poller、conversion 控制路由授權路徑、D3 dev routes）；`AGENTS.md` frontend Known gaps 表同步。驗證：`rg -n "useCoordinatorStatusStore|conversion/trigger|ENABLE_DEV_ROUTES" docs/agents AGENTS.md`
- [ ] 7.2 `docs/plans/NOW.md` 看板與 `openspec/lifecycle-ledger.json` 對帳（`task_ledger`、`current_slice`）；D1=H 或 semantic cases 變更時 bootstrap ledger 登記。驗證：`node scripts/tests/verify-openspec-repository-lifecycle.mjs`
- [ ] 7.3 `npx openspec validate unified-console-runtime-truth --strict` 與 `npx openspec validate --all --strict`
- [ ] 7.4 archive 前 MODIFIED delta 與 canonical 逐條義務對照（design §10 表逐列核對，非只比標題與 scenario 名）；標題 byte-exact。驗證：`rg -n "### Requirement: Product Governance Console Shell|### Requirement: A1-A10 Pages Preserve Prototype Intent" openspec/specs/unified-governance-console/spec.md`；`rg -n "Coordinator/Intake/Runtime 頁 SHALL 只打" openspec/specs/edge-console-operator-frontend/spec.md`
- [ ] 7.5 PR body 機器規則：`Design gate status: mixed`、bootstrap claim ⊆ changed paths、frontend Known gaps 列全 routes、`kit_gpu` 表＋Actions URL、裸 yes/no 欄位。驗證：pr-review-agent required check 綠
- [ ] 7.6 gitnexus：`gitnexus detect-changes --scope compare --base-ref main` 無非預期 symbol；merge 後 `git fetch --prune`
