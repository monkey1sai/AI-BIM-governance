# Tasks — minio-folderview-and-baseline-disclosure

對應 plan `docs/superpowers/plans/2026-06-24-minio-folderview-and-baseline-disclosure.md`（含 10-agent 交叉對抗修訂段）。本清單為該 plan 12 個 task 的簡要版。

- [ ] 1. 後端 `listMinioFolder`（S3 `Delimiter='/'` 逐層 list，additive）：`minioClient.ts` 加 `listMinioFolder` 回 `{ folders[]({prefix,has_source_ifc}), objects(當層直屬), prefix, count }`、`MinioObjectView` 加 `idempotency_key` 欄、`prefixHasSourceIfc` probe；while-loop 全拉處理 `IsTruncated`（超 1000 不截斷）；`listMinioObjects` 舊簽名與回應零改（`minio-objects-route.test.ts` 零改）+ 新增 `minio-folder-route.test.ts`。
- [ ] 2. `/api/minio/objects` route 加 `delimiter` 參數（additive）：帶 `?delimiter=/` 走 `listMinioFolder` 回 `folders[]`；不帶 delimiter 維持舊回應 byte-identical；未設定 MinIO 仍誠實回 `count:0 + note`、`folders:[]` 防前端 crash；回應永不夾帶 presigned URL。新增 `minio-objects-delimiter-route.test.ts`。
- [ ] 3. 後端 `triggerManualIntake` + `POST /api/conversion/trigger`（一鍵觸發，`x-dev-token` 守門）：新增獨立 `manualIntake.ts`（重用 `deriveIntakeFromKey`/`idempotencyKeyFor`/`createMinioS3Client`/`getSignedUrl`，**非 import** `triggerIntake` 私有 closure）；驗 key 規約防穿越、server-side 生 presigned 不外洩、upsert 持久 ledger（status=detected）、回 `{status, idempotency_key}`、冪等；拒無 auth → 401/403、key 含 `..` → 400。新增 `manual-intake.test.ts` + `conversion-trigger-route.test.ts`。
- [ ] 4. watcher tick 去重改持久 ledger（全自動 auto-enroll，非 additive）：把 ledger 查詢注入 `startMinioWatcher`；tick 對每個 `*/model.ifc` 算 `idkey`、查持久 ledger（無紀錄→觸發並落帳、有紀錄→skip）；移除 `isFirstRound` baseline 特例；重啟命中既有 ledger 紀錄→skip（不風暴）。實作前跑 GitNexus `impact({target:'startMinioWatcher'})`、commit 前 `detect_changes`；既有 watcher 測試 baseline/首輪不觸發斷言改寫 + 新增「既有無紀錄→自動觸發」「重啟命中 ledger→不重觸發」測試。
- [ ] 5. 前端 `coordinatorClient` client 方法：加 `getMinioFolder(prefix?)`（打 `?delimiter=/`，502 守門）、`triggerConversion(key)`（jsonPost 帶 `x-dev-token`、回 `{status, idempotency_key}`）；`MinioObject`/`MinioFolderNode`/`MinioFolderListing`/`ConversionRecord` 型別對齊後端新欄位（`idempotency_key`/`has_source_ifc`）。
- [ ] 6. 前端 `#minio`（`MinioDataPage`）逐層導覽：`buildMinioTree` 退役、改 `useState(currentPrefix)` 點資料夾換 prefix 重打 `getMinioFolder`；`folders[]` 渲染含『含 source IFC』badge；`.ifc` 列掛 ledger 衍生狀態 chip（讀 `getConversionRecords`、以 `idempotency_key` map lookup、無紀錄誠實標『未轉』）＋「觸發轉檔」鈕（intent→confirm→audited、成功 patch chip、失敗 inline error chip 不變）；葉層三段語意 badge；role/pending 誠實；empty 態分「MinIO 未設定」與「prefix 無物件」兩文案；頁首誠實字樣與不夾帶 presigned URL 保留。重寫 `MinioDataPage.test.tsx`（含原「不呼叫 getConversionRecords」改為會呼叫）/ `console.test.tsx` / `e2e/minio-closed-loop.spec.ts` 斷言。
- [ ] 7. 前端 `#conv`（`ConversionSchedulingPage`）baseline 揭露 + 文件三方同步 + browser E2E：`baseline_count`/`triggered_total` 拆獨立 Field + 解釋文案（baseline_count 純診斷·§3.4 auto-enroll 首輪即自動補轉 ledger 無紀錄者、一致性基準＝可解析 IFC 數 3 非物件總數 527）、列兩條 spec 認可補救（僅文字）＋ ledger 列一鍵觸發鈕；移除過時「重啟也救不了」警語（與 ledger 去重 auto-enroll 矛盾）；prototype HTML / closed-loop design / auto-intake design 三方文件同步（刪 NOT BUILT、改 raw-folder 逐層、supersede 註記）；隔離 branch stack 跑 gstack browser E2E 截圖（7 個專案資料夾逐層 + `geometries_chunks` 摺疊 + baseline/triggered 區分 + 不變量）。
