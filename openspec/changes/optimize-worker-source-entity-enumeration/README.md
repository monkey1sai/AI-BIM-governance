# optimize-worker-source-entity-enumeration

Optimize the _worker IfcOpenShell source entity enumeration phase so canonical 89MB IFC fixtures can progress past the current timeout blocker before the next batch baseline run.
