# Tasks — minio-folderview-and-baseline-disclosure

對應 plan `docs/superpowers/plans/2026-06-24-minio-folderview-and-baseline-disclosure.md`（含 10-agent 交叉對抗修訂段）。本清單為該 plan 12 個 task 的簡要版。

> **2026-07-29 frozen closeout reconciliation**：7/7 task 均為終局處置——全部行為已由 #259（trigger 後端）、#265（`d5e9286`，本 change 主體）、#303/#304（IA 合併重塑）落地或 supersede，本次 closeout **零新 production code**；各 task 尾端【】為 disposition 與佐證。兩處刻意偏離（頁面拆為 `modelData/*`、觸發鈕移單檔詳情面板）與 main 超出原文的強化（`400 invalid_delimiter`、indeterminate chip、資料夾快取揭露、watcher panel 移 `#/minio`）已同步修入本 change 兩份 delta。

- [x] 1. 後端 `listMinioFolder`（S3 `Delimiter='/'` 逐層 list，additive）：`minioClient.ts` 加 `listMinioFolder` 回 `{ folders[]({prefix,has_source_ifc}), objects(當層直屬), prefix, count }`、`MinioObjectView` 加 `idempotency_key` 欄、`prefixHasSourceIfc` probe；while-loop 全拉處理 `IsTruncated`（超 1000 不截斷）；`listMinioObjects` 舊簽名與回應零改（`minio-objects-route.test.ts` 零改）+ 新增 `minio-folder-route.test.ts`。【terminal：landed-on-main；successor=#265 `d5e9286`；佐證 `minioClient.ts:33/38/60/79/94` + `tests/minio-folder-route.test.ts`】
- [x] 2. `/api/minio/objects` route 加 `delimiter` 參數（additive）：帶 `?delimiter=/` 走 `listMinioFolder` 回 `folders[]`；不帶 delimiter 維持舊回應 byte-identical；未設定 MinIO 仍誠實回 `count:0 + note`、`folders:[]` 防前端 crash；回應永不夾帶 presigned URL。新增 `minio-objects-delimiter-route.test.ts`。【terminal：landed-on-main（超出原文強化：`400 invalid_delimiter` 白名單+CR/LF prefix 守門，`app.ts:2179-2244`；另 `560c84f` 加資料夾快取）；successor=#265】
- [x] 3. 【terminal：obsolete／superseded-by `minio-trigger-lifecycle-backend`（#259 `b660b1f`），原文已自述】【方向1 調和（PR 前）：後端 trigger 交由 main `minio-trigger-lifecycle-backend`（#259）已合併端點，**本項不再實作**——`manualIntake.ts` / `manual-intake.test.ts` / `conversion-trigger-route.test.ts` 已移除；下述為原規劃紀錄】後端 `triggerManualIntake` + `POST /api/conversion/trigger`（一鍵觸發，`x-dev-token` 守門）：新增獨立 `manualIntake.ts`（重用 `deriveIntakeFromKey`/`idempotencyKeyFor`/`createMinioS3Client`/`getSignedUrl`，**非 import** `triggerIntake` 私有 closure）；驗 key 規約防穿越、server-side 生 presigned 不外洩、upsert 持久 ledger（status=detected）、回 `{status, idempotency_key}`、冪等；拒無 auth → 401/403、key 含 `..` → 400。新增 `manual-intake.test.ts` + `conversion-trigger-route.test.ts`。
- [ ] 4. watcher tick 去重改持久 ledger（全自動 auto-enroll，非 additive）：把 ledger 查詢注入 `startMinioWatcher`；tick 對每個 `*/model.ifc` 算 `idkey`、查持久 ledger（無紀錄→觸發並落帳、有紀錄→skip）；移除 `isFirstRound` baseline 特例；重啟命中既有 ledger 紀錄→skip（不風暴）。實作前跑 GitNexus `impact({target:'startMinioWatcher'})`、commit 前 `detect_changes`；既有 watcher 測試 baseline/首輪不觸發斷言改寫 + 新增「既有無紀錄→自動觸發」「重啟命中 ledger→不重觸發」測試。【terminal：landed-on-main；successor=#265；佐證 `minioWatcher.ts:152-158/256-259/413-449`（ledger 水印必查、legacy fallback 已除；`isFirstRound` 僅餘 `baseline_count` 診斷用）+ `tests/minio-watch-ledger-wiring.test.ts`】
- [ ] 5. 前端 `coordinatorClient` client 方法：加 `getMinioFolder(prefix?)`（打 `?delimiter=/`，502 守門）、`triggerConversion(key)`（方向1：呼叫 main #259 已合併端點、`jsonPost` 只送 key、回 `{ifc_ready_job_id}`）；`MinioObject`/`MinioFolderNode`/`MinioFolderListing`/`ConversionRecord` 型別對齊後端新欄位（`idempotency_key`/`has_source_ifc`）。【terminal：landed-on-main；successor=#265；佐證 `coordinatorClient.ts:552/559/692/701` + `coordinatorClient.test.ts:260-398`】
- [ ] 6. 前端 `#minio`（`MinioDataPage`）逐層導覽：`buildMinioTree` 退役、改 `useState(currentPrefix)` 點資料夾換 prefix 重打 `getMinioFolder`；`folders[]` 渲染含『含 source IFC』badge；`.ifc` 列掛 ledger 衍生狀態 chip（讀 `getConversionRecords`、以 `idempotency_key` map lookup、無紀錄誠實標『未轉』）＋「觸發轉檔」鈕（intent→confirm→audited、走 main #259 端點、成功 `loadRecords()` 由 ledger 對齊 chip、失敗 inline error chip 不變）；葉層三段語意 badge；role/pending 誠實；empty 態分「MinIO 未設定」與「prefix 無物件」兩文案；頁首誠實字樣與不夾帶 presigned URL 保留。重寫 `MinioDataPage.test.tsx`（含原「不呼叫 getConversionRecords」改為會呼叫）/ `console.test.tsx` / `e2e/minio-closed-loop.spec.ts` 斷言。【terminal：landed-differently；successor=#265→#303（`04e66d6`）；`MinioDataPage` 拆為 `modelData/*`（ModelDataPage master-detail＋MinioTreePane＋ObjectDetailPane＋GlobalConversionPane＋useMinioFolder），觸發鈕移單檔詳情面板（`ObjectDetailPane.tsx:189`）——兩處刻意偏離已修入 delta；行為面（逐層導覽/badge/chip/兩種 empty 態/localeCompare）全數在 main】
- [x] 7. 【terminal：landed-differently；successor=#265→#303/#304；baseline 揭露落在 `#/minio` 的 `GlobalConversionPane`（`conv-baseline-count`/`conv-triggered-total`/三段解釋文案齊備），`ConversionSchedulingPage` 已退役、`#/conv` 現為轉檔歷史（`ConversionPage`）；「重啟也救不了」警語已不存在於任何 UI code；prototype HTML 已改名 `AI-BIM Console Hi-Fi.dc.html`（無 NOT BUILT）；browser E2E＝2026-07-29 closeout 修 stale IA 後實跑 `e2e/minio-closed-loop.spec.ts` + `e2e/minio-watch-auto-intake.spec.ts` 2 passed（STUB MINIO + real coordinator；證據 `artifacts/e2e/minio-folderview-{toplevel,leaf,conv}.png` + `minio-watch-auto-intake-conv.png`；真 MinIO 端點未在本環境驗證，fullCompletion=false 誠實揭露）】原文：前端 `#conv`（`ConversionSchedulingPage`）baseline 揭露 + 文件三方同步 + browser E2E：`baseline_count`/`triggered_total` 拆獨立 Field + 解釋文案（baseline_count 純診斷·§3.4 auto-enroll 首輪即自動補轉 ledger 無紀錄者、一致性基準＝可解析 IFC 數 3 非物件總數 527）、列兩條 spec 認可補救（僅文字）＋ ledger 列一鍵觸發鈕；移除過時「重啟也救不了」警語（與 ledger 去重 auto-enroll 矛盾）；prototype HTML / closed-loop design / auto-intake design 三方文件同步（刪 NOT BUILT、改 raw-folder 逐層、supersede 註記）；隔離 branch stack 跑 gstack browser E2E 截圖（7 個專案資料夾逐層 + `geometries_chunks` 摺疊 + baseline/triggered 區分 + 不變量）。
