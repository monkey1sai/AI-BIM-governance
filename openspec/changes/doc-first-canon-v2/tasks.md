# Tasks — doc-first-canon-v2

> **DoD 兩段化（全 task 通用）**：每條 task 的 DoD 區分 `draft-submitted`（AI 草稿已提交供審）與 `user-adopted`（使用者裁決採納／正本已改）。**本 change 的機器驗收 gate 只綁 `draft-submitted`；`user-adopted` 為追蹤狀態，明確排除於機器 gate 之外。** 本 change 完成語意＝全部 task 達 draft-submitted；archive 前 MUST 確認使用者對採納／退回已表態（讀正本 v2 version-bump commit 存在性，見 design.md），未表態不 archive。
> **採納訊號**：`user-adopted` 權威 artifact＝正本 v2 的 version-bump commit（merged、diff 觸及手寫正本、含版本號＋日期 bump、使用者核准/PR approve）；退回 artifact＝提案 PR 的使用者 close/comment 裁決記錄。

## 0. 前置 hard gate（critical-path，先於全部改寫 task）

- [x] 0.1 **Task 0 錨點可行性 spike（hard gate）**（2026-07-18 PASS,證據見 prep-evidence.md;補充:Hi-Fi 檔零 id 錨,植錨方案涵蓋雙檔）：以 repo 內實檔驗證 `.dc.html` 的錨點承載力。三段結論制入 design.md：(1) 章級穩定錨已證實存在（`.dc.html` 僅 `sec1..sec8`）、需求/區塊級錨不存在；(2) 首選＝v2 草稿自帶 `data-canon-id` 類細粒度穩定錨（屬提案文字一部分、隨使用者採納生效、與 R-A1 相容）；(3) sidecar anchor map 僅為使用者拒絕在正本植錨時的末位降級，design.md 明記其與 `file:line` 同屬易腐、不得作為 R-C1 主要載體。**DoD/draft-submitted**：植錨方案已入 v2 草稿或降級已記錄於 design.md，未完成即為早停 blocker。**DoD/user-adopted**：使用者接受植錨方案。
- [x] 0.2 **PF-1/PF-2 硬前置檢查**(2026-07-18 PASS:main 仍 v3 header @ spec.md:8,PF-2 未觸發;開 PR 前複跑)：實跑 `grep -n "Workflow v3 and product design artifacts have distinct, non-overlapping authority" openspec/specs/documentation-source-of-truth/spec.md` 確認 main 仍為 v3 header；若已變 v4 則本 change MODIFIED delta 先 rebase 對準 v4 再驗。**DoD/draft-submitted**：grep 輸出附於 PR body，header 狀態確認。
- [x] 0.3 **MODIFIED 範圍對齊檢查**(2026-07-18 PASS:2 MODIFIED header 逐字=main:8/31,其餘 4 條未觸碰,validate --strict 綠)：實跑 `openspec validate doc-first-canon-v2 --strict`＋diff，確認只 MODIFY `documentation-source-of-truth` 6 條中之 2 條（「Workflow v3…」「文件分工調整必須走 PR 治理流程」）、每條 header 逐字重現、其餘 4 條未觸碰；任何新增治理需求一律入 ADDED。**DoD/draft-submitted**：validate 通過＋範圍 diff 附 PR body。
- [x] 0.4 **R-A4 可回復基準建立＋dry-run restore**(2026-07-18 PASS:tag canon-v2-baseline-20260718 @ 0d24fb6,dry-run restore diff 空)：改寫前對兩份 `.dc.html`＋`docs-plans-README.md`＋`ai-bim-governance.css` 建立 git tag 或文件樹外 `.bak-<timestamp>`、回報路徑；執行一次 dry-run restore 證明 restore 後 diff 為空。**DoD/draft-submitted**：backup path/tag 記錄＋dry-run restore 驗證輸出附 PR body。

## 1. 變更控制 capability（design-canon-change-control）

- [x] 1.1 撰寫 `design-canon-change-control` spec delta（R-A1 手寫正本寫入邊界／R-A2 機器快照雙旗標／R-A3 support.js 禁改／R-A4 改版可回復）(2026-07-18 PASS:4 requirement/9 scenario,validate --strict 綠;3fc29a8 原「四項檢核零缺漏」結論已被三次 enforcement scenario 補強推翻——7d2f7e7 補 R-A3 support.js 對稱缺口、858f16f 補 R-A1「自行 merge」對稱缺口、本次補 R-A1「直接觸及正本檔即違規退回」direct-edit 對稱缺口;本輪驗證結果記錄,後續輪次可再補強)。**DoD/draft-submitted**：spec delta 完成＋validate 通過。**DoD/user-adopted**：使用者核准新 capability。
- [x] 1.2 撰寫 `AGENTS.md` 參照鏈修正草稿：指回 doc-first 權威序與 design-canon-change-control 條文（提案供審，不逕改 main）。(2026-07-18 PASS:`drafts/AGENTS-refchain.v2-draft.md` 3 段逐字對照——§3 Runtime/product behavior truth 定義句 AGENTS.md:132＋優先順序清單 AGENTS.md:134-142 兩段現行→建議＋1 段 design-canon-change-control R-A1 新增參照；已核對 `git diff main -- AGENTS.md` 為空、worktree 與 main 逐字相同；AGENTS.md 本體未改動)。**DoD/draft-submitted**：AGENTS.md 改寫草稿以 PR diff 提交、標「待使用者核准」。

## 2. documentation-source-of-truth MODIFIED＋ADDED（權威翻轉＋保存性）

- [x] 2.1 撰寫 MODIFIED「Workflow v3…」body：翻為 doc-first 權威序、內部裁決序、保留誠實鐵律半句＋非矛盾 scenario。**DoD/draft-submitted**：delta 完成＋header 逐字重現＋validate 通過。(2026-07-18 PASS,三點逐一驗證:(a) header @ delta:3 與 main:8 位元組級逐字一致(含 CRLF,經 python rstrip(\r)雙向比對確認);(b) body 含 doc-first 權威序 `docs/plans`=唯一需求權威／code 偏離=待修 implementation gap(delta:5)與內部裁決序 視覺=Hi-Fi+css 最高／行為=設計文件§01–§08 為權威(delta:7);(c) 誠實鐵律半句「亦不得以文件宣稱 runtime 已完成」原樣保留(delta:9)＋非矛盾 Scenario「誠實鐵律與 doc-first 非矛盾壓測」(delta:23-27)證明「標 planned」與「判待修 gap」並存無矛盾;三點皆已在原提案 commit 378a9c1 到位,本次為驗證非新寫,body 零修改;`npx openspec validate doc-first-canon-v2 --strict`綠)。
- [x] 2.2 撰寫 MODIFIED「文件分工調整必須走 PR 治理流程」body：加手寫正本面 cross-ref design-canon-change-control R-A1。**DoD/draft-submitted**：delta 完成＋header 逐字重現。(2026-07-18 PASS,兩點逐一驗證:(a) header @ delta:29 與 main:31 位元組級逐字一致(含 CRLF,經 python rstrip(\r)雙向比對確認);(b) body 含手寫正本面 cross-ref design-canon-change-control R-A1——第二段(delta:33)明文引用該 capability R-A1 並摘述其「不自行 merge、不原地編輯」約束，另附專屬 Scenario「AI 改寫手寫正本面」(delta:45-48)以 MUST/MUST NOT 語句重申，雙重承載且語意對齊 R-A1 原文「使用者專屬／AI 只能提案不自行 merge」(design-canon-change-control spec.md:5);兩點皆已在原提案 commit 378a9c1 到位,本次為驗證非新寫,body 零修改;`npx openspec validate doc-first-canon-v2 --strict`綠)。
- [x] 2.3 撰寫 ADDED R-B1（偏離處置序＋三態分類 code-defect/canon-defect/undecided）、R-B2（誠實鐵律非矛盾）、R-B3（carve-out 清單＋diff 斷言）、R-B4（需求正本邊界）、R-B5（R2 三態＋ProvTag 詞彙＋planned 三態 class）、R-B6（內嵌 viewport 防護＋spectator/issues 裁決）。**DoD/draft-submitted**：各 ADDED requirement ≥1 scenario＋validate 通過。(2026-07-18 PASS,六條逐一核對:R-B1 三態分類 code-defect/canon-defect/undecided 齊全＋cross-ref MODIFIED「Workflow v3…」＋2 scenario,對映裁1/裁2/風險表 canon-defect 出口,零修改;R-B2 誠實鐵律半句保留＋非矛盾證明＋1 scenario,零修改;R-B3 原僅列 §04 payload 委任/§01 鐵律1–3/README §3.4 凍結面/§07:575 deferral 四項,**缺誠實鐵律半句**——已補入 carve-out 清單第 5 項(canon 面＝README §3.5 誠實子句「不得以文件宣稱 runtime 已完成」,item 23/task 3.2 翻轉 §3.5 權威語意時 MUST 語意保留,斷言標的＝README §3.5 前後 diff;規範全文＋非矛盾證明 cross-ref R-B2、不重複文字)＋既有 scenario 覆蓋;R-B4 需求正本邊界(docs/plans=全部/外部 design repo=唯讀 authoring origin)＋1 scenario,對映裁4,零修改;R-B5 R2 三態(existing→整合/repo內可建→全棧/外接引擎才mock)＋ProvTag 7 值(asbuilt/artifact/demo/p1/p15/p3/p4)＋data-prov＋planned 三態 class enum(含 unclassified)＋OQ-1 asbuilt-partial 不採用＋2 scenario,對映裁6/裁8,零修改;R-B6 EmbeddedViewer iframe 跨-origin signaling* 消毒等價防護/KIT_SPECTATOR_COUNT 預設0/邀請連結真複製 navigator.clipboard/streamRole=spectator/issues 權威入口 unified #a1?dock=issues＋2 scenario,對映裁3/裁9/裁10,零修改;`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 2.4 **carve-out diff 斷言（R-B3 補償閘）**：對 carve-out 清單逐條列改寫前後語意等價檢核（行為面不變判準）；顯式化既有事實補列（item 14 類）不算誤動。**DoD/draft-submitted**：diff 斷言清單可執行、任一條被誤動即 blocker。(2026-07-18 PASS:新檔 `carve-out-assertions.md`,5 條逐條列原文位置(sec 錨/README 節/`data-canon-id`／`<!-- canon:* -->`錨)、判準、可執行檢核指令、備註,外加 §3 合併執行腳本;base 依 design.md §1.3 統一為`git show main:<path>`;基準態(Wave1/2 尚未動筆,僅 Task0 錨點植入)五項全數實跑 PASS——[1]§04 payload 委任 diff 空、[2a/2b]鐵律1/2 diff 空、[2c]鐵律3 must-preserve grep 鏈有輸出、[3]README§3.4 後端凍結面錨點無關全句 diff 空(＋三檔/埠/coordinator :8004·byte-identical 三條子 grep 佐證)、[4]README§3.5 誠實子句 grep 有輸出、[5]§07:575 deferral diff 空;item14/`:49100`類顯式化補列不算誤動之邊界已在 item1/3 備註明載且由「空 diff 天然涵蓋邊界」機制佐證;鐵律3(item15 改寫目標)與誠實子句(item23 改寫目標)為特例——預期部分改寫、僅保子句字面或等價改述,其餘三項(payload 委任/鐵律1-2/README§3.4/§07:575)要求逐字不變;blocker 處置序已寫入 §0 通則)。

## 3. Wave 1（P0，低風險高解毒——先清障使 Wave 2 建於已清障權威條文上）

> Wave 為優先序/所屬批次欄位，MUST NOT 使任一 task 失去獨立追溯性或被合併。

- [x] 3.1 **item 22（核心翻轉）**：§08 權威順序表改寫為 doc-first（現寫 code＋tests 第 1、書面需求第 4 → 翻為 docs/plans 需求第 1、code＋tests 為 runtime 現況查證面）。**DoD/draft-submitted**：§08 該區塊 draft diff＋Crosswalk 對號。(2026-07-18 PASS:draft `c8-authority-table`(draft:611-616)六列全換為 doc-first 序——1 docs/plans 需求正本(§01–§08;前端視覺/互動面=Hi-Fi+ai-bim-governance.css 最高)——依領域分工/2 tests/contracts/*.json(payload 委任,§04 保留)/3 AGENTS.md/OpenSpec(治理程序)/4 Prompt Board 文字(僅意圖參考)/5 應用場景圖(僅視覺上下文)/6 圖中示例數字(fixture);表頭「寫進 AGENTS.md」改參照語意(draft:609)、§08 尾 badge 同步改參照語意(draft:729);新增誠實半句「code+tests=runtime 現況的查證面;code 偏離本正本=implementation gap 待修,不得以文件宣稱 runtime 已完成」(draft:618);carve-out 五項迴歸複驗全 PASS(未觸碰任一 carve-out,§04 payload 委任/鐵律1-2/README§3.4/README§3.5 誠實子句/§07:575 diff 皆空或子句仍在);Crosswalk 對號＝design.md §4 既有列「22｜§08 權威順序表｜normative(doc) 翻轉｜Wave1｜裁1,2」,本次改寫手法與之一致;`npx openspec validate doc-first-canon-v2 --strict` 綠;task#7 review-fix(2026-07-18):移除誤植於本表的「使用者最新明確指令」列——該概念屬 canon:r-user-instruction-supremacy 獨立 meta-rule(docs-plans-README.v2-draft:37「使用者最新明確指令 > 本目錄一切文件」),非需求權威表列,混入牴觸 spec.md:5「唯一需求權威」並破壞 AGENTS-refchain 對照2 同構;docs/plans 回歸第 1 列,符合本 DoD 原文「翻為 docs/plans 需求第 1」)。
- [x] 3.2 **item 23（核心翻轉）**：docs-plans-README §3.2/§3.5「現況行為權威＝code＋tests；設計文件＝目標權威」語意改寫為 doc-first（需求權威＝docs/plans；code 偏離＝待修 gap）。**DoD/draft-submitted**：README 區塊 draft diff＋Crosswalk 對號。(2026-07-18 PASS:draft §3 兩點改寫——(a) 第 2 條權威順序(draft:38)六項全換為 doc-first 序,與 task#7(item 22)已定案 §08 `c8-authority-table`(draft:611-616)六列逐項對齊:1 docs/plans 需求正本(§01–§08;前端視覺/互動面=Hi-Fi+ai-bim-governance.css 最高,依領域分工)/2 tests/contracts/*.json(payload 委任,§04 保留)/3 AGENTS.md/OpenSpec(治理程序)/4 Prompt Board 文字(僅意圖參考)/5 應用場景圖(僅視覺上下文)/6 圖中示例數字(fixture);(b) 第 5 條(draft:41)翻為「需求權威=本目錄設計正本(doc-first);code+tests=runtime 現況查證面;code 偏離正本=implementation gap,列入 gap ledger 排修;不得以文件宣稱 runtime 已完成」,誠實子句字面全句保留(用語對齊 spec.md:20 R-B1「該偏離 SHALL 判為 implementation gap 並進 gap ledger 排修」);carve-out-assertions.md §3 合併執行區塊七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 皆 PASS,凍結面與誠實子句原文未動);唯一預期次要回聲漂移=carve-out 1 子檢核(b)因新第 2 條字面新增第三處 `tests/contracts/*.json` 出現,主檢核(a)§04 委任句本身 diff 仍空,依 carve-out-assertions.md §2 item 1 判準屬非 blocker 次要漂移,不誤動;§3.1(使用者指令最高)/§3.3(四條鐵律)/§3.4(後端凍結面)/§2(讀取路線)/§1(檔案清單)逐段比對 main 皆逐字未動(僅 Task 0 既有 `<!-- canon:* -->` 錨點,非本次新增);Crosswalk 對號＝design.md §4 既有列「23｜README §3.2/§3.5｜normative(doc) 翻轉｜Wave1｜裁1」,本次改寫手法與之一致)。
- [x] 3.3 **item 24（核心翻轉）**：刪除 §03 命名核對 carve-out「以程式碼為準，不回頭改碼」。**DoD/draft-submitted**：§03 該區塊 draft diff（刪除）＋Crosswalk 對號。(2026-07-18 PASS:draft `c3-naming-check`(draft:205-217)整塊改寫——(a) 標頭(draft:206)「命名核對(code-truth,對照 console/unified/*)」→「現況對照(doc-first,對照 console/unified/*)」;(b) 引言句(draft:208)刪除權威語句「以下以程式碼為準,不回頭改程式碼命名以遷就舊文件」,改為中性引導「以下逐條列現況對照」,前段「先前的理想化命名,與現行…不符」誠實描述原樣保留;(c) 四則刪除線事實比對(AppShell→EdgeConsole.tsx/UnifiedShell.tsx、UsdStageTreePane/ViewportLayer→WorkspacePage.tsx 內聯 JSX 頁內無 video、DockPanel→docks.tsx A1Dock..IssuesDock、六個 hooks→UnifiedShell.tsx UnifiedStateProvider/useUnifiedState)與 mappingCache 未接線事實(draft:209-213)逐字保留,均為仍成立之現況;(d) 新增收斂語句(draft:216,獨立 badge span,置於卡片外、緊接卡片後,樣式比照 §08 c8-authority-table 誠實半句 badge draft:618 同款):「上列為 2026-07-18 現況;理想 IA(含元件/hook 命名)為應建目標,收斂路徑見 §07 分期與 follow-up changes;命名遷移屬實作 PR 範疇」;§03:214 DataChannel stale 自糾句逐字未動(不在本 task,留給 task 3.7/Task 13,新增 badge 插入點在其後,未與之交錯);全文 grep 確認「命名核對」「code-truth」「以程式碼為準,不回頭改程式碼命名」三者歸零,「應建目標」與「現況對照(doc-first」皆存在;carve-out-assertions.md §3 合併執行五項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 皆未受本次編輯影響);Crosswalk 對號＝design.md §4 既有列「24｜§03 命名核對 carve-out｜normative(doc) 刪除｜Wave1｜裁1,2,24」,本次改寫手法(刪除權威語句、保留事實、改為 doc-first 應建目標框架)與之一致;`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 3.4 **item 9**：§08 R3 收斂至 `ProvTag` 詞彙＋7 值 `Prov`＋`data-prov=fixture|live`；`asbuilt-partial` 外接待決值以 §08 R3 內顯式 non-normative『Open Decision 註記塊』承載、normative enum 保持封閉。**DoD/draft-submitted**：§08 R3 區塊 draft diff＋normative enum 封閉檢查。(2026-07-18 PASS:draft `c8-r3-provenance`(draft:652-659)改寫——(a) 主段落(draft:654)`ProvenanceTag(mock | live)` 換成實碼詞彙:元件 `ProvTag`＋7 值 `Prov`(逐字列 asbuilt/artifact/demo/p1/p15/p3/p4,cross-ref `web-viewer-sample/src/console/data.ts:6`)＋unified 層 `data-prov="fixture|live"`;語意改述貼合任務原句「示意數字一律 fixture 並以 data-prov 標示;面板掛 ProvTag(7 值誠實分類);未接通 action 誠實停用並標 Concept Preview / Roadmap,不做假成功」,「不可標成 live production data」誠實語句原樣保留;(b) 緊接新增巢狀 `c8-r3-oq1-open-decision` 註記塊(draft:655-658,dashed border 視覺區隔於 R3 主體段落),標頭列同時掛中文標籤「OQ-1 · Open Decision(未裁決,預設不採用)」與右側 uppercase tag「non-normative」,內文:是否新增 `asbuilt-partial` 外接待決 `Prov` 值(失真項 9 遺留)——預設不採用、不併入 7 值集,normative enum 維持 7 值 `Prov` 封閉,擴充唯一路徑＝`design-canon-change-control` R-A1 提案＋使用者核准;(c) §08 共用元件清單 badge(draft:697)`ProvenanceTag`→`ProvTag(既有)`;**grep 驗證**:全檔 `ProvenanceTag`／`mock | live` 兩字樣皆 0 命中(exit=1);**normative enum 封閉檢查**:`asbuilt-partial` 全檔僅命中 1 次(draft:657,落於巢狀 Open Decision 塊內非 normative 列表),draft:654 之 7 值列表逐字仍為 `asbuilt/artifact/demo/p1/p15/p3/p4`(7 項、未被待決值污染),與 `web-viewer-sample/src/console/data.ts:6` 現況 `Prov` type union 7 值比對一致;div/span 標籤配對經 node 腳本核驗全檔平衡(275/275、527/527);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 皆未受本次編輯影響,`c8-r3-provenance` 本非五項 carve-out 之一);Crosswalk 對號＝design.md §4 既有列「9｜§08 R3｜normative(doc)＋non-normative Open Decision 塊｜Wave1｜1,6,8」,本次改寫手法(normative 詞彙收斂＋巢狀 non-normative OQ-1 塊)與之一致;`npx openspec validate doc-first-canon-v2 --strict` 綠;**gap fix(2026-07-18,fixer)**：item 9 的 F4 詞彙收斂原僅涵蓋 §08 R3(.dc.html),漏了姊妹 draft `docs-plans-README.v2-draft.md:39` §3.3 四條鐵律 R3 子句內同一 `ProvenanceTag(mock|live)` 殘留(若不補,採納後會原樣寫回真正 `docs/plans/docs-plans-README.md`、復燃 F4);本次補收斂該子句為 `ProvTag`／7 值 `Prov`／`data-prov`(語意鏡射 §08 R3「示意數字一律 fixture 並以 data-prov 標示;面板掛 ProvTag(7 值誠實分類);未接通 action 誠實停用並標 Concept Preview / Roadmap,不做假成功」),README draft 內 `ProvenanceTag`／`mock|live` 兩字樣歸零(grep exit=1);design.md §4 crosswalk item 9 錨點欄由「§08 R3」補為「§08 R3 ＋ README §3.3 R3」認領第二地點;§3.3(四條鐵律)非五項 carve-out 之一(carve-out 3/4＝README §3.4/§3.5,非 §3.3),carve-out-assertions.md §3 合併檢核七項改寫後仍全 PASS)。
- [x] 3.5 **item 4**：§07:576 `docs/ai_journal/changes.jsonl` 從未存在 → 刪除或改指實存留痕機制。**DoD/draft-submitted**：§07 區塊 draft diff。(2026-07-18 PASS:draft `c7-residual-badges` 容器內第 577 行 badge 原文「變更守則:gitnexus_impact → 修改 → 契約測試綠 → AI journal 留痕(docs/ai_journal/changes.jsonl)」——該路徑全庫不存在(僅 `docs/PROJECT_UNDERSTANDING.md:94` 與正本 `docs/plans/AI-BIM 前後端設計文件.dc.html:576` 兩處歷史殘留引用,依 task 範圍「只改 draft」不代為修改)——改寫為「變更守則:gitnexus_impact → 修改 → 契約測試綠 → 變更留痕(OpenSpec change 目錄 openspec/changes/&lt;id&gt;/ + PR body 的 AI Coding Governance 表)」,對映 repo 現行機制(`.github/PULL_REQUEST_TEMPLATE.md` §AI Coding Governance／`docs/agents/github-workflow.md:47` 七欄 body-evidence 表);draft 全文 grep `ai_journal` 歸零(exit=1);正本 `docs/plans/AI-BIM 前後端設計文件.dc.html:576` 逐字未動;carve-out-assertions.md §3 合併執行五項全 PASS(§04 payload 委任/鐵律1/鐵律2/README§3.4/§07:575 皆未受本次編輯影響,本次改動落於同容器內第 577 行、非任一 carve-out 錨點);`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 3.6 **item 5**：§07:578/§08:673 stale 編號（align tasks 2.4–2.8/3.4，現行 §2 只到 2.5、archive gate＝5.3）改寫；§07:579 stale「已知不一致」註記刪除。**DoD/draft-submitted**：§07/§08 區塊 draft diff。(2026-07-18 PASS:draft 兩處改寫＋一處刪除——(a) §07 `c7-residual-badges` 容器內倒數第二個 badge(實際 pre-edit line 579,tasks.md 本行標記 578,相差 1 因先前 wave task 累積編輯已使行號漂移——此漂移本身即是本 task 要修的「stale 編號」問題之同類現象,故以內容比對而非行號比對定位)「OpenSpec change align-frontend-design-system-reference 尚有 task 2.4–2.8 未完成,阻塞 archive(...)」改寫為「...尚未 archive;剩餘工作與 archive 前提以其 tasks.md 現行版本為準(歷史版本曾用不同任務編號,已重構),動前端視覺前先讀該檔(...)」,不再斷言任何具體任務編號——經查證上游 `openspec/changes/align-frontend-design-system-reference/tasks.md` 現行結構為 §1–§5(2.4/2.5 為 pin 相關 task,無 2.6–2.8)、archive gate 為 5.3,與 draft 原載「2.4–2.8/3.4」完全不同,證實原編號確已 stale;(b) §08 `c8-hifi-verification-chain` 驗證鏈段落(實際 pre-edit line 678,tasks.md 本行標記 673)「tasks.md 的 2.4–2.8(...)仍未全部打勾,3.4 明確以此為 archive 前提」同步改寫為相同不硬編編號句式,保留「AI coding agent 應先讀該 change 目前的 tasks.md,不要預設這條 gate 已經全線接通」操作指引;(c) §07 同容器內原倒數第一個 badge(實際 pre-edit line 580,tasks.md 本行標記 579)「已知不一致:...spec.md 仍引用已刪除的 docs/plans/TRUTH·TARGET·PROCESS·BACKLOG」整條刪除——經查證上游 `openspec/changes/align-frontend-design-system-reference/specs/documentation-source-of-truth/spec.md` 全文 grep TRUTH/TARGET/PROCESS/BACKLOG 已零命中,證實該不一致確已修復(#354)、註記本身 stale;grep 驗證 draft 全文「2.4–2.8」「3.4 為 archive 前提」「已知不一致」「TRUTH/TARGET/PROCESS/BACKLOG」四者皆歸零;div/span 標籤配對經 node 腳本核驗全檔平衡(275/275、528/528,較 task 3.4 基準 527/527 淨增 1——刪除 1 個舊 span、新增 2 個「現行版本」強調用巢狀 span);CRLF 行結尾全檔保留(python byte-level 核驗無 LF-only 行);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動落於同容器內第 579/580 行、非任一 carve-out 錨點,carve-out 5 備註本已預期本 task 觸及同容器 578/579 行);`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 3.7 **item 8**：§03:214 stale 自我糾錯刪除（§04:279 早標「已實作」）。**DoD/draft-submitted**：§03 區塊 draft diff（刪除）。(2026-07-18 PASS:draft `c3-naming-check`(現況對照塊)第 214 行 bullet 整句改寫——刪除「§04 DataChannel 表把 selectPrimsRequest / composeStageRequest 標「待補」與現況不符,兩者已在 Window.tsx + harness/fakeKit.ts 實作並有測試覆蓋」stale 自糾(§04:280 現行已標「已實作」,原糾錯指向空氣);保留「真正待補=commandRejected」事實,併入同塊一句「DataChannel 唯一待補=commandRejected(見 §04)」(commandRejected 於 §04:283、§07:569/578 三處既有描述皆語意一致、逐字未動,未受本次編輯影響);grep 驗證全檔「標「待補」與現況不符」歸零(exit 1)、`commandRejected` 命中數 4(214/283/569/578);div/span 標籤配對經 node 腳本核驗全檔平衡(275/275、524/524,較 task 3.6 基準 528/528 淨減 4——移除同一 bullet 內 4 對已無需求的 inline code `<span>` 標籤,開合成對);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 皆未受本次編輯影響,本次改動落於 §03 c3-naming-check 容器內第 214 行、非任一 carve-out 錨點);`npx openspec validate doc-first-canon-v2 --strict` 綠)。

## 4. Wave 2（結構性重寫——§03/§08 大改寫，建於 Wave 1 已清障權威條文上）

- [x] 4.1 **item 1**：§03 Route Map＋CH-G 收斂表改寫為「目標路由＋現況對照」，CH-G 標未做（`#/workspace`、`#/ops`、`#/app/:slug` 與七條舊 hash 重導全未落地，唯 `/ui/console`→301 真）。**DoD/draft-submitted**：§03 區塊 draft diff＋Crosswalk。(2026-07-18 PASS:draft `c3-route-map`(draft:155-163)與 `c3-legacy-route-convergence`(draft:165-172)兩表皆由原 2 欄/badge-wrap 改寫為 3 欄語意(Route/舊路由 | 目標 | 現況 2026-07-18);Route Map 6 列逐一依實碼查證分類——已達成(綠 `#4fd68a`):`#/home`(hash key `#home` → `UnifiedShell(page=home)`)、`#/pipeline`(`#pipeline` → `UnifiedShell(page=pipe)`)、`/ui/open?session=`(§01 鐵律 3 凍結,非 CH-G 範疇);未做(琥珀 `#e6b23e`):`#/workspace`(路由 key 不存在,實掛 `#a1..#a4` 四個獨立 hash key+`?dock=` 疊加覆寫查詢,非統一主鍵語法)、`#/ops`(路由 key 不存在,實掛 `#runtime`)、`#/app/:slug`(字面 key 不存在,A5–A10 實掛 `#a5..#a10`→`ConceptPage`,另有 `#app/<slug>` prefix 由 legacy `AppVisionPage` 承接=雙 concept surface,收斂列 follow-up `ch-g-route-convergence`);CH-G 表拆解原三個混合分組——七條舊 hash(`#coordinator/#conv/#minio/#gpu/#kit/#review/#viewer`)併一列標「未做:零 redirect」;新增 `#intake` 獨立列(已有 alias 但導至 legacy `#minio` 非 `#/pipeline`,非收斂目標);新增 `#runtime` 與 `#a1..#a4` 兩個獨立澄清列標「語意已達成、非經收斂重導」——此二者原表混入「舊 hash」分組(`#runtime · #gpu · #kit → #/ops`／`#review · #viewer · #a1..a4 → #/workspace?dock=…`)但實碼查證(`EdgeConsole.tsx` `renderUnified()`/`UNIFIED_WS_KEYS`/`UNIFIED_CONCEPT_KEYS`)證實兩者已是 unified 現行 hash key(渲染 `OpsPage`/`WorkspacePage`),非零 redirect legacy 頁,與其餘七條性質不同不應同歸類;`/ui/console→301` 已落地列不變;表外新增收斂 badge 明標「CH-G(URL 收斂)整體狀態:未做(目標)」;全部現況事實逐一比對 `web-viewer-sample/src/console/EdgeConsole.tsx`(renderUnified/renderBody switch、UNIFIED_WS_KEYS/UNIFIED_CONCEPT_KEYS、AliasRedirect)、`routing.ts`(PRODUCT_CONSOLE_ROUTES)、`unified/WorkspacePage.tsx`(dockFromHashQuery)、`bim-review-coordinator/src/app.ts:3703-3704`(`/ui/console` 301)實碼查證一致;carve-out-assertions.md §3 合併執行相關子集([1]§04 payload 委任/[2a]鐵律1/[2b]鐵律2/[2c]鐵律3 must-preserve/[5]§07:575 A5–A10 deferral)全 PASS(本次編輯僅動 §03 `c3-route-map`/`c3-legacy-route-convergence` 兩區塊,未觸及任一 carve-out 錨點);div/span 標籤配對經 node 腳本核驗全檔平衡(281/281、545/545);`git diff --cached --check` 乾淨無 trailing whitespace;Crosswalk 對號＝design.md §4 既有列「1｜§03 Route Map / CH-G｜normative(doc)｜2｜1,2」,本次改寫手法(目標/現況雙欄對照＋CH-G 未做標記)與之一致;follow-up `ch-g-route-convergence` 已預先具名於 design.md §6a,無需新增;`npx openspec validate doc-first-canon-v2 --strict` 綠;**gap fix(2026-07-18,fixer)**：CH-G `#intake` 列現況欄原括號句誤植「#/pipeline key 不存在」,與同表 Route Map `#/pipeline` 列(draft:159「已達成:hash key #pipeline 存在,渲染 UnifiedShell(page=pipe)+PipelinePage」)及實碼直接矛盾——`EdgeConsole.tsx:55` usePageHash 正規化(`.replace(/^#\/?console\/?/,"").replace(/^#\/?/,"")`)剝除可省略 `#/`,故 `#/pipeline` 與 `#pipeline` 正規化後同為鍵 `pipeline`;`EdgeConsole.tsx:108` `case "pipeline"` 確渲染 `UnifiedShell(page=pipe)+PipelinePage`,route key 確實存在(此偽斷言未見於 tasks.md 本列原摘要,係後續寫入 HTML 時新增);已將該欄改為「AliasRedirect 導至 legacy #minio,非收斂去向 #/pipeline(該 unified route key 已存在、見上表,惟 alias 尚未改指)」,移除偽斷言且與 draft:159 及本列摘要一致;lines 158/160/161 三處 legit `key 不存在`(#/workspace·#/ops·#/app:slug 三路由 key 真不存在)未動;grep `#/pipeline key 不存在` 歸零、div/span 平衡 281/281·545/545 不變、CRLF 保留、carve-out §3 七項全 PASS、`validate --strict` 綠)。
- [x] 4.2 **item 2**：§06 `LineagePublicationOutbox` 六態標 planned（coordinator 僅 3 態；六態只在 rvt-ifc-usdc-lineage 提案）。**DoD/draft-submitted**：§06 區塊 draft diff。(2026-07-18 PASS:draft `c6-callback-outbox-lineage-outbox`(draft:543)單行改寫——workflow 3 態(pending→sent|dead-letter)後綴插入巢狀 `<span style="color:#4fd68a">— 已實作</span>`;lineage 六態(DISABLED|PENDING|RETRYING|DELIVERED|DEAD_LETTER|CONFLICT)後綴插入巢狀 `<span style="color:#e6b23e">— planned(class: repo 內可建/全棧)——coordinator 現況僅 3 態 outbox,六態契約在 openspec/changes/rvt-ifc-usdc-lineage(未實作);實作=follow-up lineage-outbox-impl</span>`;原三行 enum 文字與 `<br>` 結構逐字保留、僅插入巢狀 span,手法比照既有 `c3-naming-check`(draft:210-216)巢狀彩色 span 慣例、色碼沿用全檔既定「綠=已達成/琥珀=未做」語意(c3-route-map draft:157-161);grep 驗證:「已實作」新增於 draft:543(既有 draft:282 一處未動,合計 2 處)、「planned(class: repo 內可建/全棧)」命中 1、「lineage-outbox-impl」命中 1;div/span 標籤配對經 python 腳本核驗全檔平衡(281/281、547/547,較 task 4.1 基準 545/545 淨增 2——新增 1 對 span×2 處);carve-out-assertions.md §3 合併執行相關子集([1]§04 payload 委任/[2a]鐵律1/[2b]鐵律2/[2c]鐵律3 must-preserve/[5]§07:575 A5–A10 deferral)全 PASS(本次編輯僅動 §06 `c6-callback-outbox-lineage-outbox` 卡,未觸及任一 carve-out 錨點);Crosswalk 對號＝design.md §3 24×11 矩陣既有列「2 §06 六態｜裁1,裁7」與 §6a follow-up 表既有列「lineage-outbox-impl｜rvt-ifc-usdc-lineage｜§06 六態；item 2」,`class: repo 內可建/全棧` 語意對映 R-B5(spec.md:100)R2 三態中段「正本有列＋repo 內可建（governance/coordinator 擴充）→後端＋前端一次建到位」;`npx openspec validate doc-first-canon-v2 --strict` 綠;**gap fix(2026-07-18,fixer)**：R-B5(spec.md:102)明定正本 v2 任一 `planned` 標記 SHALL 附 R2 三態 class 封閉列舉值 `{integrated-ready｜in-repo-fullstack-pending｜external-mock-legit｜not-built｜unclassified}` 之一;本 task 原插入的 `class: repo 內可建/全棧` 係中文改述、字面非該 5 個封閉 token 之一(逐字繼承自 Plan B `artifacts/spec-to-done/canon-v2-planB.md:17` 的需求原文,非 implementer 新增偏差),已改為封閉列舉值 `in-repo-fullstack-pending`——語意精確對映上引 spec.md:100 R2 中段「正本有列＋repo 內可建（governance/coordinator 擴充）→後端＋前端一次建到位」(in-repo＋fullstack＋pending);token 落於既為 JetBrains Mono monospace 的 `c6-callback-outbox-lineage-outbox` 卡片體內,比照同卡姊妹 enum(DISABLED|PENDING|…、pending→sent|dead-letter)採 inline monospace,不另加冗餘 span;驗證:grep `repo 內可建/全棧` 於 draft 歸零、`in-repo-fullstack-pending` 命中 1、git diff 恰 1 行(1 add/1 del,唯一差異即此 token)、div/span 平衡 281/281·547/547 不變、CRLF 保留、carve-out §3 合併執行五項全 PASS、`openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.3 **item 3**：§03:201 `resolveGovPanelState` gate 失真改寫（gate 只護 legacy overlay，unified docks 零 gate）＋「unified docks 接真 API＋gate」列 follow-up。**DoD/draft-submitted**：§03 區塊 draft diff。(2026-07-19 PASS:draft `c3-badge-spectator-gate`(draft:203)單一 badge 由單態全稱宣稱「spectator 一律 readonly:resolveGovPanelState 統一 gate 所有 Dock 的寫入行為」改寫為誠實雙態——「【目標】spectator 一律 readonly,resolveGovPanelState 統一 gate 全部 Dock 寫入。【現況】gate 僅接線 legacy viewer overlay 路徑(windowOverlayGlue→Window/GovernanceOverlay);unified docks 現為 fixture 殼、無真實寫入亦無 gate 接線——接真 API+gate=follow-up unified-docks-real-api」;follow-up 名稱之連字號後綴於 HTML 內文改為不加反引號(裸詞),核對全檔 rendered body 內文 grep 反引號字元「\`」歸零、確認本檔既有慣例本就不在 body 文字內用 markdown 反引號(技術符號一律用 monospace `<span>` 或裸詞,如 badge 202/204 之 HandoffButton/console/i18n.ts 亦裸詞無反引號),故本次改寫遵循既有排版慣例、非新增偏差;**現況真實性查證**(對主工作區 `web-viewer-sample/src/` 實碼,本 worktree 為 doc-only 不帶該目錄):`resolveGovPanelState`(governance/govPanelState.ts:20)唯一呼叫者為 `windowOverlayGlue.ts:25` 之 `deriveOverlayInputs`,而 `windowOverlayGlue` 唯一匯入者為 `Window.tsx:42`,同檔 `Window.tsx:41`/`:2668` 匯入並渲染 `GovernanceOverlay`——三者共同構成 legacy viewer overlay 路徑,與改寫後badge「windowOverlayGlue→Window/GovernanceOverlay」逐字對應;`console/unified/docks.tsx` 與 `WorkspacePage.tsx` 全文 grep `resolveGovPanelState`/`Gov` 系匯入均 0 命中(僅 `WorkspacePage.tsx:108` 一處 toast 提示字串內含「resolveGovPanelState gate」六字,屬 UI 文案非真實呼叫),證實 unified docks 現況確無真實 gate 接線,與改寫後badge「unified docks 現為 fixture 殼、無真實寫入亦無 gate 接線」逐字相符;follow-up `unified-docks-real-api` 沿用既有具名條目(design.md:115『unified-docks-real-api（雙軌收斂）| —（本 change 新提）| item 3/19』與 design.md:104 附記『unified docks fixture 殼（item 3/19）雖判 immediate-gap,其修復已具名排入 follow-up unified-docks-real-api（已認列、非靜默缺口）』),本 task 未新增 follow-up 條目、僅在 badge 內引用既有名稱;Crosswalk 對號＝design.md:60 24×11 矩陣「3 §03 gate 失真」列對映裁1(doc-first 總綱)/裁2(內部裁決序)/裁11,design.md §4 摘要 crosswalk(僅摘要 7/24 項,見「完整 24 列在交付 Crosswalk 檔」附註)未收錄本項屬正常缺列、非缺失;grep 驗證:舊句「spectator 一律 readonly:resolveGovPanelState 統一 gate 所有 Dock 的寫入行為」全檔歸零(exit 1)、新句四要素(【目標】/【現況】/windowOverlayGlue→Window\/GovernanceOverlay/follow-up unified-docks-real-api)逐一 grep 皆有輸出;div/span 標籤配對經 node 腳本核驗全檔平衡(281/281、547/547,與 task 4.2 基準相同——本次僅改單一 span 文字內容、未增減任一 div/span 標籤);CRLF 行結尾保留(僅單行文字替換,未動行結構);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §03 `c3-badge-spectator-gate` 單一 badge、非任一 carve-out 錨點);`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.4 **item 6**：§02 拓撲修正（governance-service＝host-native 非 Docker Web-Plane 容器 `compose.host-kit.yml:16`、MinIO＝外部非 compose service）。**DoD/draft-submitted**：§02 區塊 draft diff。(2026-07-19 PASS:draft §02 兩容器(`c2-docker-web-plane`/`c2-host-native-gpu`,draft:118-136)三處改寫——(a) governance-service 列自 Docker Web-Plane 欄移至 Windows Host-Native GPU 欄(緊接 conversion authority 之後),文字由「governance-service container」改「governance-service(host-native,不在任何 compose 內)」,dot 顏色 `#4fd68a` 與埠號 `:49102` 原樣保留;移動依據=`compose.host-kit.yml:16` 註解「governance-service 永遠 host-native（CPU ifcopenshell，:49102），不在任何 compose 內」(worktree 內實讀確認),並交叉核對 `compose.runtime-manager.yml` 全文 grep `minio|governance-service|govern` 僅命中 `MINIO_WATCH_*` 相關行、「governance」「govern」字面 0 命中,證實該 compose 確未定義 governance-service;(b) MinIO 列文字由「MinIO(或既有物件儲存)」改「外部既有物件儲存(非本 compose service;coordinator 以 MINIO_WATCH_* env 對接)」,dot 顏色 `#e6b23e` 與右側 `s3` 標籤原樣保留、位置留在 Docker Web-Plane 欄(非 compose service 但屬 web-plane 網路可達範疇,與同欄既有 shared volume 列同屬非-service infra 項,plan 原文亦只要求「改標」非「移欄」);「coordinator 以 MINIO_WATCH_* env 對接」字面對應 `compose.runtime-manager.yml:44-61` 十個 `MINIO_WATCH_*` env 透傳定義(coordinator services 區塊內,全 compose 無 `minio:` top-level service);(c) UnifiedConsole 列文字由「UnifiedConsole 靜態 bundle(由 coordinator 服務)」改「UnifiedConsole = coordinator image 內建 bundle(非獨立 service)」,`/ui` 標籤原樣保留;依據=`compose.runtime-manager.yml:41-43` 註解「CH-E:React UnifiedConsole build(vite base=/ui/)由 coordinator image 內建。不依賴 host ignored artifact web-viewer-sample/dist-ui」＋同檔 `CONSOLE_DIST_DIR: /workspace/console-dist`;三處改寫外,兩欄框體/標頭(compose 檔名標註)/其餘四列(coordinator container、conversion authority、Omniverse Kit + RTX renderer、kit-manager-api、NVIDIA driver、shared volume)逐字未動;div/span 標籤配對經 node 腳本核驗全檔平衡(281/281、547/547,與 task 4.3 基準相同——本次僅移動 1 組既有 div/span 並改 3 處文字內容,未增減任一標籤);carve-out-assertions.md 五項核心 carve-out 錨點(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/§07:575 A5–A10 deferral)所在區塊未受本次編輯影響,本次改動範圍僅 §02 `c2-docker-web-plane`/`c2-host-native-gpu` 兩容器;`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.5 **item 7**：§06 `IfcReadyRecord`/`ConversionJob` status enum 對齊實碼。**DoD/draft-submitted**：§06 區塊 draft diff。(2026-07-19 PASS:draft `c6-ifc-ready-record`(draft:505)與 `c6-conversion-job`(draft:509)兩卡 status 行逐字換為實碼——(a) IfcReadyRecord 卡「status: received→converting→ready」(舊,非實碼;intake job 的 `status` 欄位從無 `ready` 值)→「status: accepted|queued_for_conversion|dispatched|dispatch_failed|dropped_on_restart|failed」,來源=`IfcReadyIntakeStatus`(`bim-review-coordinator/src/types.ts:195-206`,6 值逐字依宣告序全列,型別掛於 `IfcReadyIntakeJob.status`,types.ts:212/214);(b) ConversionJob(ledger) 卡「status: queued|running|done|failed」(舊,非實碼)→「status: detected|queued|converting|ready|failed」,來源=`ConversionLedgerStatus`(`bim-review-coordinator/src/services/conversionLedger.ts:11`,5 值逐字依宣告序全列,型別掛於 `ConversionLedgerRecord.status`,conversionLedger.ts:24);兩實碼型別互為獨立(record vs job 概念在 doc/code 命名交叉——doc 卡名「IfcReadyRecord」實對應 code `IfcReadyIntakeJob`;doc 卡名「ConversionJob」實對應 code `ConversionLedgerRecord`),依 task 要求分開標明來源檔,已各自於卡片內以行內 `<span style="color:#5a7089">(型別名,檔案:行號)</span>` 標註;僅動 status 行,job_id/artifacts/quality/retry_count 等其餘欄位未動(task 範圍僅 enum,非全欄位重寫);grep 驗證舊字串「received→converting→ready」「queued|running|done|failed」全檔歸零(exit 1);div/span 標籤配對經 node 腳本核驗全檔平衡(281/281、549/549,較 task 4.4 基準 547/547 淨增 2——新增 2 對行內來源引用 span);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §06 `c6-ifc-ready-record`/`c6-conversion-job` 兩卡片 status 行);Crosswalk 對號＝design.md §3 24×11 矩陣既有列「7 §06 enum」對映裁1、design.md §4 normative Crosswalk 摘要表「7｜§06 IfcReadyRecord/ConversionJob｜descriptive(tests-delegated)｜2｜1」,本次改寫手法(descriptive 修正委任層錯述,非行為變更)與之一致;`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.6 **item 10**：§08:727 `features/a1..a10` 目錄不存在、14 共用元件 0/14 → 標「目標結構/待抽取」＋補「`src/console/` 現況→目標」遷移方向。**DoD/draft-submitted**：§08 區塊 draft diff。(2026-07-19 PASS:draft `c8-closing-badges` 結構 badge(draft:734)改寫——原「結構 = modular monolith frontend(單一 SPA + features/a1..a10)— 不是 microfrontend,不是十套專案」拆為已達成/目標兩段:「結構=modular monolith(單一 SPA)✔」保留(單一 SPA、非 microfrontend 事實不變)＋新增巢狀強調 span「features/a1..a10 為目標佈局(現況=src/console/ 扁平+console/unified/ 子夾,遷移屬未來實作 PR,非現況)」,尾句「不是 microfrontend,不是十套專案」原樣保留;「共用元件優先抽取(20 張圖重複 UI)」清單標題(draft:694)緊接新增獨立 badge span(draft:695,樣式比照既有 c8-authority-table 誠實半句 draft:620 同款 amber 配色)「待抽取目標;現況 0/14 以此命名存在,功能近義物異名散落(ProvTag/Metric/FlowBar 等)——命名收斂於抽取時裁決」;grep 驗證:舊句「結構 = modular monolith frontend(單一 SPA + features/a1..a10)」全檔歸零(exit 1)、新句三要素(結構=modular monolith(單一 SPA)✔/features/a1..a10 為目標佈局/待抽取目標;現況 0/14)逐一 grep 皆命中 1 次;div/span 標籤配對經 node 腳本核驗全檔平衡(281/281、551/551,較 task 4.5 基準 549/549 淨增 2 對——結構 badge 新增 1 對巢狀強調 span、14 清單新增 1 對獨立 badge span);CRLF 行結尾全檔保留(python byte-level 核驗 748 行、0 條 bare LF);carve-out-assertions.md 五項核心 carve-out 錨點(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/§07:575 A5–A10 deferral)所在區塊未受本次編輯影響,本次改動範圍僅 §08 `c8-closing-badges` 與共用元件清單標題兩處;`npx openspec validate doc-first-canon-v2 --strict` 綠;**gap fix(2026-07-19,fixer)**：本 task 兩處插入文字逐字繼承任務需求原文(非 implementer 新增偏差),但 committed 後帶兩項可驗證現況失真,與 doc-first-canon-v2 誠實鐵律(修復現況宣稱失真)同向矯正——(1) 共用元件清單標題 badge「現況 0/14 以此命名存在」失真:`ProvTag` 確以此精確命名存在(web-viewer-sample/src/console/components.tsx:17 `export function ProvTag`,另在 pages.tsx/GovernanceOverlay.tsx/modelData/ModelDataPage.tsx/console.test.tsx 共 4 檔 import 使用),且同清單第 699 行既有 `ProvTag(既有)` 註記與「0/14」直接自相矛盾;對其餘 13 項(MetricCard/StatusBadge/EvidenceInspector/BimViewport/SplitCompareViewer/ModelTree/DataTable/LifecycleStepper/IssueDrawer/ExportMenu/JobStatusPanel/RoadmapBanner/Skeleton)逐一 grep 均 0 命中→正確計數=1/14;已改為「現況 1/14 以此命名存在(僅 ProvTag,見 components.tsx),其餘 13 項功能近義物異名散落(Metric/FlowBar 等)」,將以精確命名存在的 ProvTag 自「異名散落」例舉移除、僅留確為異名既有物的 Metric(imported GovernanceOverlay.tsx:8/pages.tsx:5)/FlowBar;(2) 結構 badge「現況=src/console/ 扁平+console/unified/ 子夾」僅點名 unified 一子夾失真:實測 src/console/ 除 unified/(11 檔)另有 coordinator/(3)、governance/(12)、modelData/(16)、viewer/(9) 四個既有子夾(root 68 檔仍為多數,故『扁平為主』大方向保留),已改為「以扁平檔為主,另有 coordinator/unified/governance/modelData/viewer 五個既有功能子夾、皆非 aN 對齊」;驗證:grep『0/14』歸零·『1/14』命中 1·『扁平+console/unified/ 子夾』歸零·五子夾名皆入結構 badge,div/span 平衡 281/281·551/551 不變(純文字替換未增減標籤),CRLF 保留(749 CRLF 段·0 bare LF),carve-out 五項未觸及(本次僅動 §08 c8-closing-badges 與共用元件清單標題兩處),`openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.7 **item 11**：§08:679 Task 0–12「對映 §07 CH」無實質 crosswalk → 補對照表或刪宣稱。**DoD/draft-submitted**：§08 區塊 draft diff。(2026-07-19 PASS:二選一採「補表」——draft `c8-task-sequence-table`(draft:686-693)閉合 `</div>` 後、「共用元件優先抽取」header 前,插入新區塊(draft:694-703):(a) 小標頭「Task↔CH Crosswalk(近似對映,非嚴格一對一)」;(b) 新表 `data-canon-id="c8-task-ch-crosswalk"`,3 欄(Task/對應 CH/備註)、5 列,逐項對映 tasks.md 4.7/canon-v2-planB.md Task 6 原文——Task 0↔CH-0/CH-A(基建:harness+Playwright/token+三欄殼)、Task 1–3↔CH-B/CH-C/CH-E(A1–A3 真整合已落地:樹↔聚焦與 hooks 抽取·串流角色權威·console 合併)、Task 4↔CH-E/CH-F(Model Data+Runtime 共用元件:console 合併延伸·BindingComposer)、Task 5–10↔「—(無對應 CH)」(feature 軸,依 R2 三態重詮釋=repo 內可建面全棧一次到位,外接引擎面才 mock)、Task 11–12↔「CH-G+新期」(Task 12 跨頁/E2E 貼近 CH-G URL 收斂現況未做;Task 11 A10 整合儀表板無專屬 CH,擬由未來新期承接,§07 CH-H 家族/內嵌 viewport 期對應 item 18、現況本文件尚未列,未被虛構);(c) 閉合表格後補二軸性質收斂 note badge:「CH=基礎建設軸(§07 分期;CH-0–CH-F 已出貨、CH-G 未做);Task=feature 軸(本節建議實作順序,尚未落地)。二軸非逐項嚴格對應,上表為近似區間對映;Task 5–10 無對應 CH 屬預期現象、非缺漏」;CH 標籤逐一核對 §07 `c7-ch-schedule-table`(draft:566-575)實際 8 期(CH-0/A/B/C/D/E/F/G)與 §08 header 既有宣稱一致,未新增/竄改任一期;`data-canon-id="c8-task-ch-crosswalk"` 經全檔 grep 確認命名不衝突(新增前 0 命中);div/span 標籤配對經 python 腳本核驗全檔平衡(288/288、571/571,較 task 4.6 基準 281/281·551/551 淨增 7 div/20 span,與插入之 1 外層表格 div+6 列 div、1 標頭 span+18 儲存格 span+1 收斂 note span 逐項相符);CRLF 行結尾全檔保留(758 CRLF 段、0 bare LF,python byte-level 核驗);git diff 確認純新增(10 insertions/0 deletions,未觸碰周邊既有行);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §08 `c8-task-sequence-table` 之後的新插入區塊,非任一 carve-out 錨點);Crosswalk 對號＝design.md §3 24×11 矩陣既有列「11 §08 crosswalk｜裁1,裁7」(§4 摘要 crosswalk 僅列 7/24 項,不含本項,屬正常缺列、非缺失);`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.8 **item 12**：§08:709 A4「語意服務另接」→「deterministic 已全棧落地；semantic＝外接 LLM（對應 a4-semantic-search-model-qa change）」＋§08:707 A5–A10「前端完整」誤導改準確。具名外部依賴一律 genericize 為「外接 LLM」。**DoD/draft-submitted**：§08 區塊 draft diff＋無具名廠商依賴。(2026-07-19 PASS,三處改寫逐一核對:[處1] R2 三態同步(f1 認領範疇,先做)——(a) draft `c8-r2-api-tristate`(draft:647-650)pre 區塊四態重寫:`Existing → directly integrate`／`In-canon + repo 內可建 → 後端 + 前端一次建到位(預設,不做 mock 過渡)`／`In-canon + 依賴外接引擎 → 不得已才 mock(掛 ProvTag 誠實標示)`／`Missing(正本沒列) → NOT_BUILT(想做先走 design-canon-change-control R-A1 提案)`,尾行`Never silently invent a production backend`原樣保留;(b) 卡下說明文字(draft:652)同步插入「,依上方 R2 三態分類、非現有契約」,「瀏覽器仍只連 :8004,禁直連 :49101/:49102/:8010。」句逐字未動(Task 9/item 14 範疇,未搶做、未重複改);(c) README draft §3.3(draft:39)R2 子句「existing→直接整合、planned→typed adapter+mock、missing→NOT_BUILT」改同義精簡版「existing→直接整合、in-canon+repo 內可建→後端+前端一次建到位(預設不做 mock 過渡)、in-canon+依賴外接引擎→才准 mock(掛 ProvTag 誠實標示)、missing→NOT_BUILT」,R1/R3/R4 三子句與行尾 `<!-- canon:r-four-iron-rules -->` 錨逐字未動(git diff 確認 README 全檔僅此一行變動)。[處2] §08 真實程度表(`c8-domain-reality-table`,draft:725-733):(a) 標頭(draft:725)「A4–A10 初期真實程度(前端完整;domain engine 需另接)」→「A4–A10 真實程度(A4=hybrid 已部分落地;A5–A10=Concept 稿,前端完整為目標)」;(b) A4 列(draft:727)右欄保留原特徵清單「NL query · Evidence Trace · 3D 高亮」,尾註拆為綠色(比照既有「已實作」配色慣例)「— deterministic 檢索已全棧落地(governance-service/search,repo 內)」+ 灰色(既有「另接」配色慣例)「;semantic 模式=外接 LLM(對應 a4-semantic-search-model-qa change),mock 邊界劃在 LLM 解讀層」;(c) A5/A6/A10 三列補雙標記(amber `planned(全棧)`,比照既有 c6 卡 `planned(class:...)` 配色慣例 + 灰 `external(mock 合法,掛 ProvTag)`)——A5「WS gateway + spatial-tree 掛接:planned(全棧)」+「感測 feed:external(...)」;A6「成本彙整:planned(全棧)」+「成本/排程外部系統:external(...)」;A10「聚合儀表板:planned(全棧)」+「各 domain service:external(...)」;A7/A8/A9(無 in-repo 元件可標)僅補灰色 external 標記——A7「點雲 ICP:external(...)」、A8「合成資料引擎:external(...)」、A9「機器人 runtime:external(...)」;原具名廠商依賴「Replicator」(A8)、「Isaac」(A9)、「P6」(A6)已同步移除,改用任務原文既有之 genericize 詞彙,全檔 grep `Isaac`/`Replicator`/`P6[^0-9]` 三者皆歸零(exit 1)。[處3] Task 表「5–10」列(`c8-task-sequence-table`,draft:691):「A4–A9 contract-first prototype(typed adapter + fixture)」→「A5–A9:repo 內可建面全棧一次到位(預設,不做 mock 過渡);僅外接引擎面可 mock(掛 ProvTag);A4 已超前完成 deterministic 全棧」,左欄任務編號「5–10」依任務原文保留未變(任務重新編號非本 task 範疇)。grep 驗證:舊字串「Planned endpoint」「Existing endpoint」「Missing endpoint」「初期真實程度(前端完整;domain engine 需另接)」「語意服務另接」「A4–A9 contract-first prototype」全檔歸零(exit 1);新字串「a4-semantic-search-model-qa」(命中1)、「design-canon-change-control R-A1」(命中1,落於 pre 區塊)、「planned(全棧)」(命中3,A5/A6/A10)、「external(mock 合法,掛 ProvTag)」(命中6,A5–A10)皆存在;div/span 標籤配對經 python 腳本核驗全檔平衡(288/288、575/575,較 task 4.7 基準 288/288·571/571 淨增 4 span 對——A4/A5/A6/A10 四列各由 1 個舊 span 拆為 2 個新 span,A7/A8/A9 三列維持 1 個 span 不變淨增 0);CRLF 行結尾全檔保留(759 CRLF 段、0 bare LF,較 task 4.7 基準 758 淨增 1——R2 pre 區塊新增 1 行);`git diff --cached --check` 乾淨無 trailing whitespace;carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §08 `c8-r2-api-tristate`/`c8-domain-reality-table`/`c8-task-sequence-table` 三區塊 + README §3.3 單行,非任一 carve-out 錨點);Crosswalk 對號＝design.md §2 裁決表既有列「6｜R2 API 三態（整合/全棧/外接才 mock）；A4 mock 邊界劃在 LLM 解讀層｜R-B5＋item 12」與「8｜A5/A6/A10 逐元件拆分（in-repo 全棧/外接 mock 合法）｜R-B5＋item 12」、design.md §7 風險表既有列「具名外部依賴夾帶｜item 12 一律 genericize『外接 LLM（對應 a4-semantic-search-model-qa）』」,本次改寫手法與之一致;`npx openspec validate doc-first-canon-v2 --strict` 綠。**scope 擴充認領記錄(f1)**：「scope 擴充認領 final-review f1:R2 卡(c8-r2-api-tristate)+README §3.3 R2 子句重寫為 R-B5 新三態(裁決 6/9/10);單一 ownership 歸 item 12」。**已知後續依賴(非本 task blocker,供 task 6.1 收斂參考)**：本次新增之「planned(全棧)」/「external(mock 合法,掛 ProvTag)」標記為任務原文指定之可讀格式,非 R-B5(spec.md:102)規定之封閉列舉 token(`in-repo-fullstack-pending`/`external-mock-legit`);比照 task 4.2 曾因同類非封閉措辭需 gap-fix 之前例,本次刻意不越權代做封閉化——任一 `planned` 標記之封閉 class 標籤掃描屬 tasks.md §6.1「planned 無裸標檢查」之唯一落實範疇(single-ownership),留待該 task 統一處理,非本 task 誤動或缺漏;**gap fix(2026-07-19,fixer)**：上段「刻意不越權、留待 6.1」推理經 review 推翻——A5/A6/A10 三列文字之唯一 R-C2 owner 即 item 12(本 task)本身,與 task 4.2 補自己那列 token 之情境對稱(R-C2 single-ownership,spec.md:147-150);4.2 選擇當場補、4.8 卻把責任推給尚未執行的 6.1(仍 `[ ]`),對同一條 R-B5 SHALL(spec.md:102)執行標準不一致,且 diff 落地後 canon draft 確有 3 處裸 `planned` 違反 spec.md:102(6.1 本身待辦、無已勾選機制保證補上)。本次比照 4.2 house style 就地補封閉 token:html 三處 `planned(全棧)`(A5「WS gateway + spatial-tree 掛接」/A6「成本彙整」/A10「聚合儀表板」)→ `planned(class: in-repo-fullstack-pending)`,語意精確對映 spec.md:100 R2 中段「正本有列＋repo 內可建（governance/coordinator 擴充）→後端＋前端一次建到位」(in-repo＋fullstack＋pending),與 §06 lineage 卡(draft:543)同 token、同 `planned(class: …)` house style;6.1「planned 無裸標檢查」對此 3 處即為 no-op pass、非漏掃。同批 6 個 `external(mock 合法,掛 ProvTag)` 標記(A5–A10)未動:spec.md:102 SHALL 子句字面只點名 `planned` 標記、未涵蓋 external 標記,6.1 remit 亦僅「planned 無裸標」,屬 review 判定之次要項(嚴重度低於上述 3 處),留 6.1/後續依作者判斷是否比照 `external-mock-legit`。驗證:grep `planned(全棧)` 於 draft 歸零(exit 1)、`planned(class: in-repo-fullstack-pending)` 命中 4(§06 lineage 1＋A5/A6/A10 三處)、div/span 標籤配對經 python 腳本核驗全檔平衡 288/288·575/575 不變(純 span 內文字置換,未增減任一標籤)、CRLF 行結尾保留、carve-out-assertions.md §3 合併執行七項全 PASS(本次僅動 §08 `c8-domain-reality-table` 三列 span 內文,非任一 carve-out 錨點)、`openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.9 **item 13**：§04 tests/contracts 僅 2 檔、A 軸 API 零契約 →「最高標準」語意誠實化，A 軸契約標 planned。**DoD/draft-submitted**：§04 區塊 draft diff。(2026-07-19 PASS:draft `#sec4` 標題列(draft:224-228)緊接新增獨立誠實現況 badge(draft:229,樣式比照既有 `c8-authority-table` 誠實半句 draft:620 同款 amber 配色,置於標題 flex row 之外、作為 `#sec4` column flex 容器的獨立直屬子元素,緊接於標題 div 之後、原有空行間距之前)——「現況覆蓋:僅 ifc_ready_payload v1.1.0 與 conversion_result_callback v1.0.0 兩檔+structured-log 子樹;A1–A10 各 API 的契約檔=planned(class: in-repo-fullstack-pending),落地前該 API 的 payload 權威=本節文字+實碼」;委任句「Payload 以 tests/contracts/*.json 為最高標準」(draft:227)逐字未動,純新增不刪改;**現況覆蓋句真實性查證**(對本 worktree 實檔,非主工作區推定):`tests/contracts/` 頂層僅 2 個 JSON 檔(`ifc_ready_payload.json`/`conversion_result_callback.json`)+ 1 個子目錄 `structured-log/`(內含 schema.json/fixtures/測試檔,非 A 軸 payload 契約),兩檔內 `version` 欄位分別為 `1.1.0`/`1.0.0`(python json 讀取確認,與 draft 既有卡片 draft:294/308 標示版本號一致);全庫 grep A1–A10 命名之契約檔 0 命中,證實「A 軸契約檔=planned」為真;**R-B5 封閉 token 選用**:任務原文字面為 `class: repo 內可建`,非 spec.md:102 R2 三態封閉列舉值之一——比照本 tasks.md 同批 item 2(task 4.2)/item 12(task 4.8)已兩度確立之 house style(R-C2 single-ownership,插入 `planned` 標記之 task 當場補封閉 token、不留待 6.1),就地採用 `in-repo-fullstack-pending`(語意對映 spec.md:100 R2 中段「正本有列＋repo 內可建（governance/coordinator 擴充）→後端＋前端一次建到位」),格式逐字比照 draft:543/728/729/733 既有 `planned(class: in-repo-fullstack-pending)` 慣例,非另創新格式;div/span 標籤配對經 python 腳本核驗全檔平衡(288/288、576/576,較 task 4.8 基準 288/288·575/575 淨增 1 span 對——新增之單一獨立 badge span);CRLF 行結尾全檔保留(python byte-level 核驗 0 條 bare LF);`git diff --cached --check` 乾淨無 trailing whitespace;carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任本身逐字不變、鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §04 標題 badge 之後的新插入獨立 span);`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.10 **item 14**：§04 補 element-mapping/for-session 家族（coordinator 直服、轉打 `:49101` 非 governanceProxy→`:49102`）、apply-overlay＝501 client-side-only、A3 create＝201、「PROXY /*」→「顯式白名單」語意、`:49100` WebRTC signaling 為合法瀏覽器直連埠（R2 補列）。此為顯式化既有事實補列（R-B3 判 normative(doc)、不算誤動 carve-out）。**DoD/draft-submitted**：§04 區塊 draft diff。(2026-07-19 PASS:四處補列逐一核對——[1] `c4-coordinator-api` PROXY 列(draft:245)追加獨立誠實 span「顯式白名單轉發(governanceProxy.ts ~30 條、kit 5 條),新後端端點須手動加入白名單,非萬用轉發」(governanceProxy.ts 實測 33 條 app.get/app.post 註冊≈「~30」;kit 5 條=app.ts 內 `/api/kit/health`/`/api/kit/usdc`/`/api/kit/instances/current`(GET)/`/api/kit/instances/current/open`(POST)/`/api/kit/instances/current/close`(POST),grep 精確命中 5);緊接新增獨立 GET 列(draft:246)「/api/governance/element-mapping/for-session/:id — coordinator 直服(不經 governanceProxy.ts),轉打 conversion authority :49101(非 :49102);同名 for-ifc-ready 變體現只見於 sibling 端點(rule-runs、search/model,經 governanceProxy.ts→:49102),element-mapping 本身尚無此變體」——實碼查證:該端點於 `bim-review-coordinator/src/app.ts:3379`(非 governanceProxy.ts)直接註冊,handler 內 `proxyConversionService(response, config.conversionApiBase, ...)` 轉打 :49101(app.ts:3375-3376 註解「host 可達的 host.docker.internal:49101」逐字佐證);任務原文「含 for-ifc-ready 變體」經全庫 grep 查證 element-mapping 本身**無**該變體(唯一 element-mapping 路由即 for-session,無 sibling for-ifc-ready 路由),但 governanceProxy.ts 確有同名家族 sibling `rule-runs/for-ifc-ready/:jobId`(line 266)與 `search/model/for-ifc-ready/:jobId`(line 429)——已據實改寫為精確表述(不虛構 element-mapping 專屬 for-ifc-ready 路由,同時保留「家族含 for-ifc-ready 命名模式」核心事實),不逕自照抄可能失真的任務原文字面;[2a] `c4-governance-api` apply-overlay 列(draft:257)追加 span「→ 501(overlay 實走前端 DataChannel highlightPrimsRequest,client-side)」,逐字對映 `governance-service/diff_engine/api.py:87-91`(`raise HTTPException(status_code=501, detail="3D overlay 為 p15:走 client highlightPrimsRequest,非後端 server-push")`)與同倉 `tests/test_diff_api.py:74` 斷言 `status_code == 501`;[2b] federated-sets 列(draft:259)`(A3)` span 就地延伸為「(A3) → 201(同步 create;A1/A2 為 202 async)」,逐字對映 `governance-service/federation/api.py:63` `@router.post("/api/federated-sets", status_code=201)`,對比 A1 `/api/rule-runs`(`governance-service/app.py:294` `status_code=202`)與 A2 `/api/diffs`(`diff_engine/api.py:42` `status_code=202`)皆為 async;[3] §08 `c8-r2-api-tristate` 卡說明句(draft:654)原句尾追加「例外::49100(WebRTC signaling)與 spectator 埠段、:47998(media)為合法瀏覽器直連面(串流本體)。」,原句「瀏覽器仍只連 :8004,禁直連 :49101/:49102/:8010。」逐字保留、僅接續追加不覆寫;實碼查證:`bim-streaming-server/SYSTEM_DESIGN.md:319-321`「WebRTC is direct...signal port (49100); signaling is not relayed through the coordinator」與 line 482-486「the browser connects directly to the Kit livestream signal port (49100) (stream 47998); there is no coordinator signaling relay」,spectator 埠段見 `bim-streaming-server/scripts/start-streaming-server.ps1:259` preflight 檢查(`spectator ports OK`);四處皆屬顯式化既有事實補列,依 design.md:97 R-B3 Q11 劃線判 normative(doc)、非誤動 carve-out(design.md 原文逐字列出本 item 14 全部子項:apply-overlay=501/A3 create=201/element-mapping→:49101/PROXY /*→白名單/:49100 signaling);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §04 `c4-coordinator-api`/`c4-governance-api` 兩卡與 §08 `c8-r2-api-tristate` 一卡,非任一 carve-out 錨點);div/span 標籤配對經 python 腳本核驗全檔平衡(289/289、581/581,較 task 4.9 基準 288/288·576/576 淨增 1 div/5 span——PROXY 列+1 span、新增 element-mapping 列 1 div+3 span、apply-overlay 列+1 span,federated-sets 列與 §08 R2 句皆為既有 span 文字延伸、0 新增標籤);CRLF 行結尾全檔保留(python byte-level 核驗 761 CRLF 段、0 bare LF,較 task 4.9 基準 760 淨增 1——新增 1 整行);`git diff --cached --check` 乾淨無 trailing whitespace;`npx openspec validate doc-first-canon-v2 --strict` 綠。**scope 誠實記錄**：任務原文「含 for-ifc-ready 變體」若逕自照抄於 element-mapping 列,將宣稱一個實碼不存在的路由(全庫 grep element-mapping + for-ifc-ready 組合 0 命中);已改寫為精確表述,未虛構亦未省略核心事實(該命名家族確含 for-ifc-ready 成員,只是落在 sibling 端點而非 element-mapping 本身)。)
- [x] 4.11 **item 15**：§01 鐵律 3「byte-for-byte」→行為級凍結描述（302/301/參數白名單）；`ui-open-regression.spec` 接 CI 列 follow-up；無測試護欄空窗標 known gap。**DoD/draft-submitted**：§01 區塊 draft diff＋known gap 標註。(2026-07-19 PASS:draft `c1-badges`(draft:107)鐵律 3 badge 由「/ui/open?session= handoff 凍結,byte-for-byte + CI guard」改寫為行為級凍結描述——「/ui/open?session= handoff 凍結(行為級:301/302 精確性、redirect target=viewer origin、參數白名單 streamRole/kitInstanceId 轉發+白名單外一律剝除(如 signalingServer);CI guard=coordinator vitest dev-console.test.ts)。known gap(不是 pass):e2e/ui-open-regression.spec.ts 尚未接 CI——接 CI=follow-up(第一順位)」;行為級三要素(301/302 精確性、redirect target=viewer origin、streamRole/kitInstanceId 轉發+白名單外一律剝除(如 signalingServer))逐條對映 `bim-review-coordinator/tests/dev-console.test.ts` 既有斷言(RK6 case 驗 302/301 且 `/ui/open` 未被 `/ui/console` 301 或任何 `/ui/*` 萬用吞掉、location 斷言含 viewer origin 且 not.toContain evil.example/127.0.0.1、streamRole+kitInstanceId 保留於 location 而 signalingServer 遭剝除),非杜撰;`e2e/ui-open-regression.spec.ts` 確存在(`web-viewer-sample/e2e/`)且 grep `.github/workflows/` 全目錄零命中,證實「尚未接 CI」屬實非虛報;凍結本體(handoff 對象＝`/ui/open?session=`、凍結宣告、CI guard 要求)三要素全數保留,carve-out-assertions.md §2c must-preserve 鏈(grep 鐵律3→/ui/open?session=→凍結)PASS,鐵律1／鐵律2／§04 payload 委任三項 carve-out 對 main 逐字 diff 皆空輸出(未受本次編輯影響);div/span 標籤配對核驗全檔平衡(289/289、581/581,本次僅改單一既有 span 文字內容、未增減任一標籤);CRLF 行結尾全檔保留(762 段、0 bare LF);`npx openspec validate doc-first-canon-v2 --strict` 綠;git diff 恰 1 行(1 add/1 del,唯一差異即此 badge);**gap fix(2026-07-19,fixer)**：上段驗證敘事將三個獨立 it() 區塊的斷言全歸單一「RK6 case」,係 citation 歸屬失真——全檔 grep `RK6` 僅命中 dev-console.test.ts:122(it 描述)與 :134(內部註解)、對應 app.ts:3701 `CH-E/CH-G(RK6 CRITICAL)` 風險標籤,真正帶 RK6 標籤者僅 `it("CH-E/RK6：/ui/console 301 收斂到 /ui，且不以 /ui/* 萬用吞掉凍結的 /ui/open")`(dev-console.test.ts:122-145),其斷言僅止於 /ui/console→301 `/ui`、/ui/open 仍 302 且 location 含 viewer origin+session、/ui 仍 200;敘事「location…not.toContain evil.example/127.0.0.1」實出自另一未標 RK6 的 `it("redirects /ui/open to configured browser-visible viewer URL")`(dev-console.test.ts:101-120);「streamRole+kitInstanceId 保留於 location 而 signalingServer 遭剝除」實出自第三個未標 RK6 的 `it("forwards only whitelisted viewer identity params through /ui/open")`(dev-console.test.ts:238-259)。三組斷言描述之行為均真實存在、各有獨立 it() 覆蓋,故 draft:107「行為級凍結」PASS 判定不變(出貨徽章正文未提及「RK6 case」、內容經核實準確、讀者不受影響),本次僅修正 tasks.md 驗證軌跡內部之測試歸屬標籤;未觸 draft/code/carve-out,`git diff --cached --check` 乾淨、`npx openspec validate doc-first-canon-v2 --strict` 綠;**gap fix(2026-07-19,fixer)**：上句「CRLF 行結尾全檔保留(762 段、0 bare LF)」之 762 係誤植,正確值=761——draft `AI-BIM 前後端設計文件.v2-draft.dc.html` 經 fs 位元組層(python 讀 bytes 計 CRLF 位元組對=761、bare LF=0)與 git blob 層(`git show <ref>:path` blob 為 LF-normalized、共 761 行)雙法核驗,於本 task 三 commit 點 0485a22/a44b892/eaeff4b 及前身 0485a22^ 皆恆為 761,且與 task 4.10(line 48)基準 761 一致、本 task「git diff 恰 1 行(1 add/1 del)」淨零行數變動亦推得應為 761;原敘事其餘部分不重寫,本註記僅更正該計數並確立 task 4.12+ 沿用之正確基準=761、勿再沿用 762)。
- [x] 4.12 **item 16**：README §3.3 `--ec-*`→`--ab-*`/`ai-bim-governance.css`（token 真相源）＋README §1 補列 `ai-bim-governance.css`（production 投影＋真實 import 雙身分）。**DoD/draft-submitted**：README 區塊 draft diff。(2026-07-19 PASS:兩處改寫逐一核對——(a) README draft §3 item 3(四條鐵律)R1 子句(draft:40)「沿用 EdgeConsole 與 `--ec-*` token 單一真相源」→「沿用 EdgeConsole 入口殼；design token 單一真相源＝docs/plans/ai-bim-governance.css（`--ab-*`，production 真實 import）；`--ec-*` token 已退役（`.ec-*` class 命名空間保留於 legacy-console.css）」,R1 其餘子句（技術棧 React+TypeScript+Vite／禁 Vue/Pinia/第二套 SPA/theme）與 R2–R4 逐字未動;(b) README draft §1 檔案清單(draft:13-20)於 Console Hi-Fi 與 support.js 兩列之間新增一列:「`ai-bim-governance.css` | design token 權威（`--ab-*`）：上游 design 系統之 production 投影＋`EdgeConsole.tsx` 真實 import 的雙重身分；手寫正本面，變更控制見 `design-canon-change-control`」，其餘既有列逐字未動。**現況真實性查證**(對主工作區 `web-viewer-sample/src/console/` 實碼,本 worktree 為 doc-only 不帶該目錄):`EdgeConsole.tsx:10` 確有 `import "../../../docs/plans/ai-bim-governance.css";`,`design-token-authority.test.ts:19` 斷言此逐字 import 字串,對應 `openspec/changes/migrate-console-to-hifi-design/specs/console-design-token-authority/spec.md` R1「ai-bim-governance.css SHALL 為 UnifiedConsole 唯一 production design token 權威」,證實「production 投影＋真實 import 雙重身分」屬實;`legacy-console.css:2`「前身 edge-console.css 已 retire；.ec-* class 名保留（測試與元件零改動）」＋全檔 192 行消費 `--ab-*` token、235 行含 `.ec-*` class 選擇器,證實「`.ec-*` class 命名空間保留於 legacy-console.css」屬實;`ec-token-retirement.test.ts`(console/ 目錄下獨立回歸測試,標題「--ec- token 已於 console CSS 全域歸零（edge-console.css 退役，§6.5 BREAKING）」)以 `not.toMatch(/--ec-/)` 對 console CSS 全域凍結零殘留,且該檔註解明文區分「守門對象是 --ec-*『自訂屬性 token』（雙 dash），非 .ec-*『class 命名空間』（單 dash）……後者刻意保留」,逐字印證本次 R1 改寫兩個子句（退役對象＝雙 dash token／保留對象＝單 dash class）皆與現行 product code 測試護欄一致,非杜撰(PR #357/#358 為對應 product code 落地,已合併於 main 0d24fb6 之前)。carve-out-assertions.md §3 合併執行五項全 PASS(本次僅動 §1 檔案清單新增列＋§3 item 3 R1 子句,未觸及任一 carve-out 錨點——[3]README§3.4 後端凍結面／[4]README§3.5 誠實子句原文 diff 皆空、[2a][2b]鐵律1/2／[1]§04 payload 委任／[5]§07:575 皆未受影響);div/span 非本檔(README 為純 markdown 表格＋條列,非 `.dc.html`)不適用該項核驗;git diff 恰 2 個 hunk(§1 表新增 1 列／§3 item 3 R1 子句改寫),`git diff --cached --check` 乾淨無 trailing whitespace;`npx openspec validate doc-first-canon-v2 --strict` 綠(`Change 'doc-first-canon-v2' is valid`)；**gap fix(2026-07-19,fixer)**：上段「現況真實性查證(對主工作區 `web-viewer-sample/src/console/` 實碼,本 worktree 為 doc-only 不帶該目錄)」之查證方法陳述失真——本 worktree（.worktrees/doc-first-canon-v2,branch openspec-doc-first-canon-v2）本身即完整追蹤 `web-viewer-sample/src/console/`（`git ls-files` 計 119 檔,含 EdgeConsole.tsx／legacy-console.css／ec-token-retirement.test.ts／design-token-authority.test.ts,實體檔在磁碟、`git sparse-checkout list` 回報 not sparse）,並非 doc-only;且 `git merge-base HEAD main`＝main tip 0d24fb6、自 merge-base 起零 commit 觸及 web-viewer-sample,四引用檔對 main `git diff` 皆空輸出（與主工作區逐位元組相同）——故現況查證原可、且實質上即於本 worktree 內就地完成,毋須切往主工作區。所援引之具體事實（EdgeConsole.tsx:10 之 ai-bim-governance.css import 字串、legacy-console.css:2 退役註解、legacy-console.css 192 行消費 --ab- token、ec-token-retirement.test.ts 護欄、data.ts:6 之 7 值 Prov）經本 worktree 內獨立複查全部屬實、與 main 一致,故 README §1/§3 draft 改寫之 PASS 判定不變;本註記僅更正驗證軌跡對「worktree 是否帶該目錄」之不實敘述,確立正確查證基準＝本 worktree 本身即含該追蹤目錄、可直接就地核驗（後續 task 勿再沿用「worktree 為 doc-only 不帶該目錄」之未經查證假設;若未來真遇 worktree 與主工作區不同步,須先實測 diff 再定查證基準,不得逕自宣稱不同步而切往他處）。未觸 draft／code／carve-out,`git diff --cached --check` 乾淨、`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [x] 4.13 **item 17**：§03 i18n「字典置 `console/i18n.ts`」→更正為 `fixtures.ts` 現況或明定搬遷。**DoD/draft-submitted**：§03 區塊 draft diff。(2026-07-19 PASS:draft `c3-badge-i18n`(draft:204)由「i18n:zh-Hant 預設 · en 切換;字典置 console/i18n.ts」改寫為「i18n:zh-Hant 預設 · en 切換;runtime(t/getLang/setLang/useLang)在 console/i18n.ts,中央字典現置 console/unified/fixtures.ts(getL/Dict);搬遷至獨立字典檔屬 backlog、非本 change」;實碼查證(對本 worktree 內 `web-viewer-sample/src/console/`,同 task 4.12 已確立之基準——本 worktree 本身即完整追蹤該目錄、非 doc-only):`console/i18n.ts` 確為 runtime 而非字典——匯出 `getLang()`/`setLang()`/`t(zh,en)`/`useLang()` 四符號(i18n.ts:17/19/27/32),檔首註解逐字「不引入 i18n library，不維護中央字典 key」(i18n.ts:5),證實原句「字典置 console/i18n.ts」為失真描述;中央字典實落於 `console/unified/fixtures.ts`——`export interface Dict`(fixtures.ts:24)＋`export function getL(zh: boolean): Dict`(fixtures.ts:44),檔內註解「i18n 字典（1:1 對應 app.js getL）」(fixtures.ts:23)逐字佐證,`getL`/`Dict` 兩符號名亦與改寫文字逐字相符;新文字採「明定搬遷屬 backlog」而非「更正為 fixtures.ts 現況」單一二選一,因兩檔案現況並存(i18n.ts 仍是唯一 runtime、fixtures.ts 仍是唯一字典)、無跡象顯示已排程合併,「搬遷至獨立字典檔」措辭對應 tasks.md 原任務選項之「明定搬遷」分支,不虛構未排程的合併計畫。carve-out-assertions.md §3 相關五項未受本次編輯影響(僅動 §03 單一既有 badge span 文字內容,未觸及任一 carve-out 錨點——鐵律1/2、§04 payload 委任、README§3.4/3.5、§07:575 A5–A10 deferral 皆非本次改動範圍);git diff 恰 1 行(1 add/1 del,唯一差異即此 badge 內文字);`git diff --cached --check` 乾淨無 trailing whitespace;`npx openspec validate doc-first-canon-v2 --strict` 綠(`Change 'doc-first-canon-v2' is valid`)。)
- [x] 4.14 **item 18**：§07 分期補 CH-H 家族（code 已出貨 CH-H1/H2/H3 semantic viewer）＋內嵌 viewport 立新期。**DoD/draft-submitted**：§07 區塊 draft diff。(2026-07-19 PASS:draft `c7-ch-schedule-table`(draft:568)CH-G 列(draft:577)後新增兩列——CH-H(draft:578,期別標籤沿用 cyan `#6fd6ee`,比照 CH-0–CH-F 已落地期別色慣例)「semantic viewer 家族(H1/H2/H3)」/「code 已出貨(viewer/viewer.css、Window.tsx 標籤)」/「既有 e2e(gov-viewer-layout.spec · element-semantics.spec)」;CH-I(draft:579,期別標籤沿用 amber `#e6b23e`,比照 CH-G planned 期別色慣例)「Workspace 內嵌 viewport」/「理想元件樹 RemoteVideo 升格應建;EmbeddedViewer iframe 方案+lease 掛載+signaling 消毒等價防護+spectator 政策(KIT_SPECTATOR_COUNT 預設 0、邀請連結真複製)」/「follow-up embedded-viewport」;僅期別標籤(col1)變色,其餘三欄沿用全表既定色階(`#b9c9da`/`#8aa0b8`/`#5a7089`),與 CH-G 既有行只變 col1 色之慣例一致。**CH-H 現況查證**(本 worktree 本身即完整追蹤 `web-viewer-sample/src/`,同 task 4.12/4.13 已確立基準——非 doc-only、可就地核驗):`console/viewer/viewer.css:1`「CH-H1 semantic viewer」/`:245`「CH-H1b」/`:256`「CH-H2 ③」/`:18`「CH-H3b」/`:22`「CH-H3」與 `Window.tsx:2582`「CH-H1/H3」/`:2584`「CH-H3」,兩檔皆確含 H1/H2/H3 三態標籤(另散見同目錄 MappingTable.tsx/MockViewport.tsx/ModelInfoCard.tsx/IfcSemanticPanel.tsx/StructureStats.tsx,task 原文點名之兩檔皆逐一命中),證實「code 已出貨」非虛稱;**e2e 具名查證**:`web-viewer-sample/e2e/gov-viewer-layout.spec.ts:6` `test.describe("CH-H1 semantic viewer · mock viewport（harness 不空白）")`、`e2e/element-semantics.spec.ts:8` `test.describe("CH-H2：點構件 → ②IFC語意 + ⑥空間（for-session 真實語意）")`,兩檔各自逐字掛 CH-H1/CH-H2 標籤,證實「既有 e2e」非虛稱;CH-H3(liveMode 側欄切換,`MockViewport.tsx:45`/`Window.tsx:2584`)全庫 grep 僅命中 source 標籤、無專屬 e2e spec 檔,E2E 證據欄故僅具名列出實際覆蓋 H1/H2 之兩檔、不虛稱涵蓋 H3,符合本 change 誠實鐵律(不逕自放大既有覆蓋範圍,亦不因此拒列已確實存在之 H1/H2 e2e 證據)。**§03 badge 改寫**：`c3-badge-workspace-handoff`(draft:202)原句「Console 不長 WebRTC — 3D 一律 HandoffButton → /ui/open?session=;Workspace 是 viewer 殼的宿主」改寫為「【目標】Workspace 內嵌 viewport(EmbeddedViewer 跨-origin iframe,video 留 viewer origin;/ui/open 凍結面照舊併存供獨立 viewer/spectator 邀請)。【現況】unified Workspace 為靜態示意,內嵌=CH-I」,雙態句式比照 task 4.3 已定案慣例(`c3-badge-spectator-gate`,draft:203 同款【目標】/【現況】結構,badge 外觀 style 屬性逐字未動、僅置換內文);語意對映 spec delta `R-B6`(documentation-source-of-truth/spec.md:116)「Workspace 3D 內嵌...SHALL 升格為應建目標;§03『Console 不長 WebRTC—3D 一律 HandoffButton』註記 SHALL 判為遺跡改寫;`/ui/open` 凍結面 SHALL 照舊併存」——三要素(內嵌升格為目標／原 badge 判遺跡改寫／`/ui/open` 凍結併存)逐一於新 badge 落地;badge 未複述 R-B6 MUST 級 signaling 消毒防護細節或 spectator/KIT_SPECTATOR_COUNT 條文(該規範已完整落於 spec delta R-B6 本身與 CH-I 表格列,badge 僅摘述方向、不重複 normative 全文,避免同一語意跨處雙重 ownership);「內嵌=CH-I」與上方新增 CH-I 期別列形成 §03↔§07 雙向可追溯。div/span 標籤配對經 node 腳本核驗全檔平衡(291/291、589/589,較 task 4.13 基準 289/289·581/581 淨增 2 div/8 span——CH-H/CH-I 各新增 1 div+4 span,badge 為既有 1 span 純文字替換、未增減任一標籤);CRLF 行結尾全檔保留(763 CRLF 段、0 bare LF,較基準 761 淨增 2,與新增 2 整行相符,python byte-level 核驗);`git diff --cached --check` 乾淨無 trailing whitespace;carve-out-assertions.md §3 合併執行五項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §07 `c7-ch-schedule-table` CH-G 列後新增兩列與 §03 `c3-badge-workspace-handoff` 單一既有 badge 文字,非任一 carve-out 錨點);Crosswalk 對號＝design.md §3 24×11 矩陣既有列「18 §07 CH-H/內嵌期｜裁1,裁3」與 §6a follow-up 表既有列「embedded-viewport（新 CH 期）｜viewer-embed-a1-highlight｜R-B6 內嵌 viewport；§03 註記；item 18」,本次改寫手法(CH-H 標已出貨事實／CH-I 標 planned 並 cross-ref follow-up)與之一致;`npx openspec validate doc-first-canon-v2 --strict` 綠(`Change 'doc-first-canon-v2' is valid`)。**已知後續依賴(非本 task blocker,供 task 5.2 §08 merge-assembly 參考)**：§08 `c8-task-sequence-table` 既有 Task 11–12 列(draft:704,task 4.7 權責落地)內含 forward-reference 括號句「§07 CH-H 家族/內嵌 viewport 期,對應 item 18,現況本文件尚未列」;本 task 完成後 CH-H/CH-I 已實際列入 §07,該括號句「現況本文件尚未列」轉為 stale。依 R-C2 Scenario「任一正本段落的唯一 ownership」(spec.md:147-150,c8-task-sequence-table 屬 task 4.7 權責、非本 task)與 YAGNI,本 task 不就地代改該表,留待 task 5.2 §08 merge-assembly 或後續 gap-fix 收斂消解此 stale 括號句。)
- [x] 4.15 **item 19**：§03 雙軌現況誠實入文（unified＝fixture 殼、真整合活在 legacy 深連結 `#a1-workbench`/`#semantic-search` 無 nav 入口）＋收斂 follow-up。**DoD/draft-submitted**：§03 區塊 draft diff。(2026-07-19 PASS:draft §03 既有徽章列(workspace-handoff/spectator-gate/i18n 三徽章)後新增第四徽章 `c3-badge-dual-track`(draft:205,配色沿用 `c3-badge-spectator-gate` 同款 cyan #6fd6ee——二者共用同一 follow-up `unified-docks-real-api`、主題延伸而非重複;item 3(task 4.3)已佔 `c3-badge-spectator-gate` 單一 ownership,依 R-C2 Scenario「任一正本段落的唯一 ownership」(spec.md:147-150)本 task 另立新徽章、不觸碰既有三徽章任一字):「雙軌現況(2026-07-18):unified 殼(#home/#a1..#a10/#pipeline/#runtime)=Hi-Fi 像素級移植 fixture(data-prov=fixture,不打 /api);真實整合頁活在 legacy 深連結 #a1-workbench(A1)/#semantic-search(A4)等,無 nav 入口。issues 權威入口【目標】=unified #a1?dock=issues;legacy #issues 列退役清單。收斂=follow-up unified-docks-real-api」。**現況真實性查證**(本 worktree 本身即完整追蹤 `web-viewer-sample/src/console/`,同 task 4.12/4.13/4.14 已確立基準——可就地核驗):(a) unified fixture 面——`console/unified/{HomePage,WorkspacePage,PipelinePage,OpsPage,ConceptPage,docks}.tsx` 六檔逐一 grep `data-prov="fixture"` 皆有輸出,`unified.test.tsx:5` 明文契約「UnifiedConsole 為 fixture-first（不打 /api），誠實標記契約 = 面板帶 data-prov="fixture"」＋`unified.test.tsx:101-103` it 斷言逐路由驗證;`PipelinePage.tsx`/`OpsPage.tsx`/`docks.tsx` 內互動一律 `toast("POST/GET /api/... → ...")` 假字串(非真實 `fetch`),「不打 /api」屬實;(b) legacy 深連結＋無 nav 入口——`EdgeConsole.tsx:121-122` `renderBody` 內 `case "a1-workbench"`/`case "semantic-search"` 分渲染 `A1GovernanceWorkbenchPage`/`A4SemanticSearchPage`(legacy 三欄殼);驅動 legacy `ec-nav` 的 `data.ts` `PAGES` 陣列全文 grep `a1-workbench`/`semantic-search` 兩字串 0 命中(僅 `NAV_GROUPS` 分組定義行含此查詢字面外的巧合,經逐條核對 `PAGES` 51 筆項目確認皆非)——證實兩頁在 legacy nav 亦零條目(舊 nav key `a1`/`a4` 已被 `UNIFIED_WS_KEYS` 攔截導去 unified fixture,不再指向 legacy 深連結頁);僅能經 `docks.tsx` `DockLiveLink`(`data-testid="dock-live-link"`,`live===true` 才渲染的情境式「完整工具 ↗」chip,非持續性導覽)、`SessionCrossLinks.tsx`、`ObjectDetailPane.tsx` 之 `buildHandoff("a1-workbench",...)` 等深連結機制到達,「無 nav 入口」屬實且與「深連結」用詞精確對應(非零可達,而是零導覽項);(c) issues 權威裁決——逐字對映既有 R-B6 spec 條文(spec.md:126)「issues 權威入口 MUST 記為 unified `#a1?dock=issues`（目標），legacy `#issues` 入雙軌退役清單（follow-up）」;`#a1?dock=issues` 語法沿用 task 4.1 已核准之 `c3-route-map` 表既有例句(draft:158,非新造語法);legacy `#issues` 現況經查證確有 nav 條目(`data.ts:58` `key: "issues"`,渲染 `IssuesRuleCenterPage`)——與 a1-workbench/semantic-search「零 nav」情境不同,故本徽章對 issues 僅列「退役清單」而非重複宣稱無 nav,避免失真。follow-up `unified-docks-real-api` 沿用既有具名條目(design.md:115『unified-docks-real-api（雙軌收斂）| —（本 change 新提）| item 3/19』),本 task 未新增 follow-up、僅引用既有名稱。div/span 標籤配對經 python 腳本核驗全檔平衡(291/291、590/590,較 task 4.14 基準 291/291·589/589 淨增 1 span 對——新增之單一獨立 badge span);CRLF 行結尾全檔保留(764 CRLF 段、0 bare LF,較基準 763 淨增 1,與新增 1 整行相符,python byte-level 核驗);`git diff` 恰 1 行(1 insertion,0 deletion);carve-out-assertions.md §3 合併執行七項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4 後端凍結面/README§3.5 誠實子句/§07:575 A5–A10 deferral 皆未受本次編輯影響,本次改動僅落於 §03 徽章列新增獨立 span,非任一 carve-out 錨點);Crosswalk 對號＝design.md §3 24×11 矩陣既有列「19 §03 雙軌｜裁1,裁10,裁11」(design.md §4 摘要 crosswalk 僅列 7/24 項,不含本項,屬正常缺列、非缺失);`npx openspec validate doc-first-canon-v2 --strict` 綠(`Change 'doc-first-canon-v2' is valid`)。**wording 微調記錄**：任務原文「Hi-Fi 像素移植 fixture」改「Hi-Fi 像素級移植 fixture」,精確對映 `console/unified/*.tsx` 逐檔檔首註解「像素級移植正本」(`docks.tsx:3`/`ConceptPage.tsx:3`/`fixtures.ts:3`/`HomePage.tsx:3`/`OpsPage.tsx:3`/`UnifiedShell.tsx:3`/`PipelinePage.tsx:3`/`WorkspacePage.tsx:4` 共 8 處逐字一致的既有術語,非杜撰新詞;任務原文 follow-up 名稱外圍反引號已移除,比照全檔既有慣例(body 文字內不用 markdown 反引號,task 4.3 gap fix 已確立)。)
- [x] 4.16 **item 20**：metadata-only blocklist 可繞過→allowlist 化列 follow-up。**DoD/draft-submitted**：正本對應區塊 draft diff＋follow-up 具名。(2026-07-19 PASS:draft `c4-contract-conversion-result-callback` 卡內 HARD RULE(METADATA-ONLY)句後新增一行補註——「現行防護=blocklist(FORBIDDEN_KEYS+USDC magic/長度啟發),可被非黑名單 key 繞過;強化為 allowlist=follow-up metadata-allowlist(planned);鐵律語意不變。」;現行防護機制描述經實碼查證(`bim-review-coordinator/src/services/callbackOutbox.ts:53-85`)——`FORBIDDEN_KEYS` blocklist(usdc/usdc_body 等 6 鍵)+字串值 `USDC_MAGIC` 前綴(PXR-USDC/PK)+長度>4096 啟發式偵測,非黑名單外 key 或未達長度門檻可繞過,描述屬實非杜撰;follow-up 具名=`metadata-allowlist`,對應 design.md §6 crosswalk 既有列「metadata-allowlist｜—（本 change 新提）｜item 20」(非本次新配,沿用既有具名);HARD RULE 原句逐字未動(grep literal count 前後皆=1,diff 僅新增一行、無其餘改動);div/span 標籤配對 node 腳本核驗全檔平衡(div 291/291 不變、span 591/591,較前一 task 基準 590/590 淨增 1 span pair);CRLF 全檔保留(python byte-level 核驗 0 LF-only 行);carve-out-assertions.md §3 合併執行五項全 PASS(§04 payload 委任/鐵律1/鐵律2/鐵律3 must-preserve/README§3.4/README§3.5 誠實子句/§07:575 皆未受本次編輯影響,本次改動落於 `c4-contract-conversion-result-callback` 卡內、非任一 carve-out 錨點);`npx openspec validate doc-first-canon-v2 --strict` 綠)。
- [ ] 4.17 **item 21**：Hi-Fi A4 標 LIVE vs 正本分界依 A4 hybrid 事實改寫。**DoD/draft-submitted**：對應區塊 draft diff。

## 5. 彙整、載體與撈回

- [ ] 5.1 **§03 merge-assembly**：items 1/3/8/13(§04 不同章)/17/19/24 中觸及 §03 者達 draft-submitted 後，組裝單一連貫 §03 全文；DoD＝章節全文 diff 恰為各區塊 diff 之聯集、零新增語意。**DoD/draft-submitted**：§03 全文 diff＝聯集可機器比對。
- [ ] 5.2 **§08 merge-assembly**：items 9/10/11/12/14(§04)/22 中觸及 §08 者達 draft-submitted 後，組裝單一連貫 §08 全文；DoD＝聯集、零新增語意。**DoD/draft-submitted**：§08 全文 diff＝聯集。
- [ ] 5.3 **gap ledger 建立（R-C1）**：獨立 ledger 檔，欄位含 classification/status/verified-as-of=2026-07-17/triage 優先序/符號級錨/adopted-in；21＋3 項 `file:line` 證據集中於此，正本內文零裸行號。CI 驗證掛 `test-agent-governance-check` 延伸。**DoD/draft-submitted**：ledger 完成＋正本內文無裸 file:line。
- [ ] 5.4 **normative Crosswalk 表（R-C2a）**：24 列，欄位＝失真項 ID/章節穩定錨/區塊錨/處置手法(normative(doc)|descriptive(tests-delegated))/可驗 DoD/所屬 Wave/對應裁決編號；每列獨立可勾。**DoD/draft-submitted**：24 列全對號、無缺格。
- [ ] 5.5 **24×11 追溯矩陣（R-C2b）**：證明零缺格；裁決 1 掛「1 MODIFIED＋1 ADDED」。**DoD/draft-submitted**：矩陣零缺格。
- [ ] 5.6 **裁決 7 撈回（R-C4）**：`git show a271e46^` 實查 TARGET-shell.md/TARGET-viewer.md/TARGET-contracts.md ＋ A5–A10 domain 契約（TelemetrySample/WorkOrder/ScheduleActivity/CaptureJob·Deviation/DatasetJob/RobotMission/Scenario/Issue source enum/A8→DatasetJob 例外）；逐句標 source commit、對照 11 條裁決 re-審、三值判定、統一標 planned；撈不到標「來源不可考、不撈回」。**DoD/draft-submitted**：撈回清單附 source file:line＋三值判定結果。
- [ ] 5.7 **Open Decisions 專章（R-B5/OQ）**：OQ-1（asbuilt-partial 不採用、§08 R3 non-normative 註記塊）、OQ-2（NVIDIA 綠授權盲區，僅 design.md＋ledger）；預設不採用、不逕自定案亦不逕自否決。**DoD/draft-submitted**：Open Decisions 表完成＋normative enum 封閉檢查。

## 6. 收斂與驗收

- [ ] 6.1 **planned 無裸標檢查**：正本 v2 任一 `planned` 標記皆附 R2 三態 class；無裁決背書者標 `unclassified` 綁 ledger triage。**DoD/draft-submitted**：無裸 planned。
- [ ] 6.2 **最終 `openspec validate doc-first-canon-v2 --strict` 實跑通過**；11 條裁決逐一對號索引表完成。**DoD/draft-submitted**：validate 輸出附 PR body。
- [ ] 6.3 **PR gate 預備**：pr-review-agent body-evidence 表預填誠實值、Requirement source＝本 change specs delta（doc-only paths 自我滿足）。**DoD/draft-submitted**：PR body 完成。
- [ ] 6.4 **（軟性、非機器 gate）J1–J5 旅程端到端走查**：走查整合後正本 v2 可端到端讀通、跨 task 無互斥；不列入完成 gate。**DoD/draft-submitted**：走查記錄（軟性）。
- [ ] 6.5 archive 前確認使用者對採納/退回已表態（讀正本 v2 version-bump commit 存在性）；未表態不 archive。**DoD/user-adopted**：version-bump commit 存在或退回記錄存在。
