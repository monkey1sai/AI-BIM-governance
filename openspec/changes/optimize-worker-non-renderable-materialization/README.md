# optimize-worker-non-renderable-materialization

Optimize the _worker non-renderable IFC entity materialization phase so canonical 89MB IFC fixtures can produce model.usdc within the configured per-fixture timeout, while preserving all-IFC-entity coverage denominator and stable IFC-to-USD traceability.
