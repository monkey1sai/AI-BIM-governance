# Playwright 原始輸出（trace / video / 失敗截圖）與 HTML 報告體積大且可重生，不入庫。
# E2E 截圖與 summary 預設本機保留、不入庫（已追蹤的既有截圖維持追蹤；
# 確需新增入庫時用 git add -f）。
_output/
_playwright-output/
_launcher/
# Isolated branch launcher mutable state is per-run, regenerated, and must not
# make the evidence publisher's clean-worktree gate reject its own backend run.
state/
report/
vg01-branch-coordinator/
*.png
*.json

# Playwright trace 二進位(large, regenerable):補齊 _output/ 與 report/ 未涵蓋的 trace 檔型別
*.zip
*.trace
*.network
*.stacks
*.jpeg
*.webm
_trace_extract/
