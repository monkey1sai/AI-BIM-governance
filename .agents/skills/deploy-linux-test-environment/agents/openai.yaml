interface:
  display_name: "Deploy Linux Test Environment"
  short_description: "Inspect or explicitly rebuild canonical Linux"
  default_prompt: "Use $deploy-linux-test-environment to inspect canonical Linux deployment readiness without mutation; rebuild only when the user explicitly requests it."
