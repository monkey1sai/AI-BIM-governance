COORDINATOR_API_BASE=http://127.0.0.1:8004
COORDINATOR_SOCKET_URL=http://127.0.0.1:8004
KIT_STREAM_SERVER=auto
KIT_SIGNALING_PORT=49100
KIT_MEDIA_SERVER=auto
KIT_MEDIA_PORT=47998
STREAMING_CONVERSION_API_BASE=http://127.0.0.1:49101
EXTERNAL_INTAKE_WEBHOOK_SECRET=dev-webhook-secret
CLOUD_CALLBACK_BASE_URL=
WEB_VIEWER_URL=http://127.0.0.1:5173
DEV_AUTH_TOKEN=dev-token
VITE_SHOW_DEMO_PANEL=true

# ---- 退役/孤兒 key 備註(勿再使用)----
# CONVERSION_SERVER_API_BASE / S3_STORAGE_BASE / S3_STORAGE_STATIC_BASE: repo 全域無讀取點,已退役,請勿新增。
# 轉檔服務統一用 STREAMING_CONVERSION_API_BASE; storage 用 RUNTIME_STORAGE_ROOT。
# BIM_CONTROL_API_BASE: 屬 bim-review-coordinator 範圍(見該 sub-repo .env.example),勿放 root。
# CONVERSION_API_BASE: 僅 coordinator 的 legacy alias(見 bim-review-coordinator/.env.example);
#   root 請用 STREAMING_CONVERSION_API_BASE。若本機 root .env 仍有上述退役/錯層 key,可刪除不影響 runtime。

# --- A4 Ornith vLLM (local defaults; copy key into untracked .env only) ---
A4_LLM_BASE_URL=http://192.168.10.248:18080/v1
A4_LLM_MODEL=Ornith-1.0-35B
A4_LLM_TIMEOUT_S=120
A4_LLM_ENABLED=true
ORNITH_API_KEY=